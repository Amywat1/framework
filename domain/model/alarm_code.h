/**
 * @file    alarm_code.h
 * @brief   报警码、等级与清除方式定义
 * @author  HUWANGWEI
 * @date    2026-06-26
 *
 * @note    报警码采用 6 位十进制编码：大类(1) + 具体编号(3) + 故障性质(2)，
 *            code = 大类 * 100000 + 编号 * 100 + 性质（编号≤999，性质≤99）。
 *          本头定义：编码宏（ALARM_CODE_MAKE）、大类常量（ALM_C_*）、
 *          故障性质常量（ALM_N_*）、等级/清除/定义类型。
 *          具体部件编号（大类内的流水号）属机型配置层，见 config/machine/。
 *          完整目录与编码规则见 doc/报警编码规范.md。
 *          本头位于 domain/model（共享类型层），config/machine 与 adapters/machine
 *          均可包含；但禁止包含 domain/safety 的实现头文件。
 */

#ifndef DOMAIN_MODEL_ALARM_CODE_H
#define DOMAIN_MODEL_ALARM_CODE_H

#ifdef __cplusplus
extern "C" {
#endif

#include <stdint.h>

/* -------------------------------------------------------------------------
 * 报警码编码宏：6 位十进制 = 大类(1) + 具体编号(3) + 故障性质(2)
 *   code = 大类 * 100000 + 编号 * 100 + 性质   （编号≤999，性质≤99）
 * ------------------------------------------------------------------------- */
#define ALARM_CODE_MAKE(major, index, nature) \
    ((uint32_t)((major) * 100000U + (index) * 100U + (nature)))

/* -------------------------------------------------------------------------
 * 大类常量（ALM_C_*）——报警码第一位，表示信号物理来源所属硬件大类
 * ------------------------------------------------------------------------- */
#define ALM_C_POWER   1U  /**< 动力硬件（电机/泵/风机）*/
#define ALM_C_SENSE   2U  /**< 感知硬件（传感器/光电/限位）*/
#define ALM_C_ACT     3U  /**< 执行元件（气缸/阀/接触器）*/
#define ALM_C_CTRL    4U  /**< 控制硬件（控制器/电源/子板/变频器/驱动器）*/
#define ALM_C_SW      9U  /**< 逻辑/软件 */

/* -------------------------------------------------------------------------
 * 故障性质常量（ALM_N_*）——报警码末两位，表示故障的性质
 * ------------------------------------------------------------------------- */
#define ALM_N_OTHER      0U  /**< 其他/指令 */
#define ALM_N_OVERLOAD   1U  /**< 过载/过流（电机堵转、泵阻力大、电流超标）*/
#define ALM_N_COMM_LOST  2U  /**< 通讯丢失（变频器连不上、信号线断）*/
#define ALM_N_HW_FAULT   3U  /**< 硬件故障（设备内部损坏、变频器报错、电源坏）*/
#define ALM_N_SIG_ERR    4U  /**< 信号异常（传感器误报、光电遮挡、无脉冲）*/
#define ALM_N_TIMEOUT    5U  /**< 动作超时（气缸没伸出、限位没碰到、动作太慢）*/
#define ALM_N_PRESSURE   6U  /**< 压力异常（水压低、气压低）*/
#define ALM_N_LEVEL      7U  /**< 液位异常（缺水、缺蜡）*/
#define ALM_N_SAFETY     9U  /**< 安全触发（急停按下、防撞条/杆碰到）*/

/* 报警描述最大字节数（定长存储，免堆，便于从配置加载） */
#define ALARM_DESC_MAX      48U

/* 报警目录最大条目数（alarm_core 固定容量目录与活跃集上限） */
#define ALARM_CATALOG_MAX   64U

/* -------------------------------------------------------------------------
 * 报警等级（数值越大越严重，用于活跃集聚合取最高）
 * ------------------------------------------------------------------------- */
typedef enum
{
    ALARM_LEVEL_MINOR = 0,   /**< 仅记录上报，不影响运行（安全态 OK）*/
    ALARM_LEVEL_MAJOR,       /**< 降级运行（安全态 WARNING）*/
    ALARM_LEVEL_CRITICAL,    /**< 立即停机（安全态 LOCKOUT）*/
} alarm_level_t;

/* -------------------------------------------------------------------------
 * 报警清除方式
 * ------------------------------------------------------------------------- */
typedef enum
{
    ALARM_CLEAR_AUTO_STATIC = 0, /**< 触发条件消失后自动清除（电平直检类：急停、限位、液位…）*/
    ALARM_CLEAR_LATCHED,         /**< 锁存，须人工复位（过载/通讯/硬件故障/超时类）。
                                  *   本期已定义、暂未启用强制锁存——其复位入口
                                  *   见 doc/报警编码规范.md §5。*/
} alarm_clear_t;

/* -------------------------------------------------------------------------
 * 报警码哨兵
 *   ALARM_CODE_NONE 表示「无活跃报警」，供查询接口返回。
 *   具体报警码由 ALARM_CODE_MAKE(ALM_C_*, 部件编号, ALM_N_*) 构造，
 *   部件编号定义在各机型的 config/machine/m8_alarm_table.h。
 * ------------------------------------------------------------------------- */
typedef enum
{
    ALARM_CODE_NONE = 0U, /**< 无报警 */
} alarm_code_t;

/* -------------------------------------------------------------------------
 * 报警定义（一条报警的完整定义；由机型适配器在 init 时注入 alarm_core）
 * ------------------------------------------------------------------------- */
typedef struct
{
    uint32_t      code;                 /**< 6 位十进制报警码 */
    alarm_level_t level;                /**< 报警等级 */
    alarm_clear_t clear;                /**< 清除方式 */
    char          desc[ALARM_DESC_MAX]; /**< 中文描述（日志/上报用）*/
} alarm_def_t;

#ifdef __cplusplus
}
#endif

#endif /* DOMAIN_MODEL_ALARM_CODE_H */

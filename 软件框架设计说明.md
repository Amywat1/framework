# 嵌入式控制软件框架设计说明

> **说明**：本文为 v2.x 历史目录快照。当前权威框架设计见 [`doc/软件设计框架说明.md`](doc/软件设计框架说明.md)；报警 v4.0 见 [`doc/报警系统设计说明.md`](doc/报警系统设计说明.md)。

**版本**：v2.3（部分章节已过时）  
**适用范围**：基于 Linux 的工业控制器（洗车机、自动化设备等）  
**架构基线**：面向新项目的重构版控制软件框架  
**关键词**：Hexagonal、领域分层、事件驱动、统一调度、真机/仿真双实现

---

## 1. 设计目标

| 目标 | 说明 |
|------|------|
| **业务与外部依赖解耦** | 业务规则不直接依赖硬件 SDK、MQTT、JSON、文件系统 |
| **真机/仿真可切换** | 通过 `ports/ + adapters/` 实现真实硬件与 PC 仿真的可替换 |
| **安全逻辑独立** | 急停、报警、互锁从业务流程中解耦，形成独立安全域 |
| **流程规则可配置** | 洗车配方、步骤规则、线程参数、部署配置分层管理 |
| **线程统一治理** | 业务线程由统一调度层创建和管理；基础设施组件线程通过 `worker_thread_t` 抽象自治 |
| **测试与回放友好** | 统一事件模型，支持场景测试、仿真与事件回放 |

---

## 2. 总体架构原则

### 2.1 分层原则

- `domain/` 只表达业务规则与设备语义
- `application/` 只做编排，不实现底层细节
- `ports/` 只定义边界接口，不包含任何具体实现
- `adapters/` 实现外部世界接入，可依赖 SDK、驱动、文件系统
- `core/` 提供系统骨架能力，不承载业务语义
- `service/` 只放纯软件共享服务

### 2.2 依赖原则

- 业务核心依赖接口，不依赖实现
- 真机与仿真实现是同级适配器，不通过 `#ifdef` 污染业务层
- 安全逻辑优先于业务流程
- 异步输入通过事件传递，同步动作通过直接调用

### 2.3 禁止事项

- 禁止 `domain/` 出现硬件常量、串口名、Modbus 地址、SDK 类型
- 禁止 `application/` 直接调用 `driver/` 或 `adapters/`
- 禁止业务模块（`domain/`、`application/`、`adapters/`）绕过 `core/scheduler` 直接调用 `pthread_create`；基础设施组件（如 `alarm_monitor`）允许通过 `worker_thread_t` 抽象自行管理线程生命周期，无需经过 `scheduler`
- 禁止跨模块直接修改设备状态快照
- 禁止将所有同步调用都改造成事件流
- 禁止 `adapters/` 直接依赖 `domain/` 中的具体实现；跨层访问必须通过 `ports/` 定义的接口进行

---

## 3. 最终目录结构

```text
apps/
  wash_controller/          ← 项目特定入口，不属于可复用 src/
    main.c
    wiring.c                ← M8 专属接线：将具体 adapter 注册进 ports

src/
  core/
    event_bus/
      event_bus.h
      event_bus.c
    scheduler/
      scheduler.h
      scheduler.c
      thread_registry.h
      thread_registry.c
    runtime/
      runtime.h
      runtime.c
      lifecycle.h
    bootstrap/
      bootstrap.h
      bootstrap.c           ← 仅包含与项目无关的启动框架（初始化顺序、错误处理）
                            ← 项目特定装配代码在 apps/wash_controller/wiring.c

  domain/
    model/
      device_state.h
      wash_types.h
      safety_types.h
      command.h
      alarm_code.h
    device/
      brush.h
      brush.c
      gantry.h
      gantry.c
      top_lift.h
      top_lift.c
      water.h
      water.c
      gate.h
      gate.c
    process/
      recipe.h
      recipe.c
      step_engine.h
      step_engine.c
      wash_mode.h
      wash_mode.c
    safety/
      alarm_registry/
      safety_posture/
    telemetry/
      snapshot/
        operational_snapshot.h
        operational_snapshot.c
        safety_snapshot.h
        safety_snapshot.c
        wash_snapshot.h
        wash_snapshot.c

  application/
    command_gateway.h
    command_gateway.c
    op_mode_bridge.h
    op_mode_bridge.c
    operational_projection.h
    operational_projection.c
    safety_projection.h
    safety_projection.c
    wash_projection.h
    wash_projection.c
    recovery_service.h
    recovery_service.c
    self_check_service.h
    self_check_service.c
    orchestrators/
      wash_orchestrator.h
      wash_orchestrator.c
      emergency_handler.h
      emergency_handler.c
      report_scheduler.h
      report_scheduler.c

  ports/
    inbound/
      command/
        command_port.h
    outbound/
      hal/
      cloud/
        report/
          report_port.h
      storage/
    safety/
      alarm_binding_port.h
      safety_action_port.h

  adapters/
    inbound/
      cloud/
        providers/
          snack/
            snack_cloud_command_adapter.*
    outbound/
      hal/
        providers/
          snack/
        sim/
      cloud/
        providers/
          snack/
            snack_cloud_report_adapter.*
      storage/
        json/
    runtime/
      snack/
        snack_mqtt.h

  common/
    point_table/
      point_table.h
      point_table_to_json.c
      point_table_from_json.c
    sw_types.h
    sw_error.h
    ...

tests/                      ← 项目根级，不属于 src/
  unit/
  integration/
  scenario/

tools/                      ← 项目根级，不属于 src/
  simulator/
  replay/
```

---

## 4. 分层职责说明

### 4.1 `core/` —— 系统骨架层

**职责**：

- 统一事件分发
- 统一业务线程创建与管理
- 生命周期与运行阶段管理
- 提供与项目无关的启动框架（初始化顺序、错误处理）

**包含模块**：

| 模块 | 职责 |
|------|------|
| `event_bus/` | 统一发布/订阅异步事件，不做业务判断 |
| `scheduler/` | 统一管理业务线程、周期任务、线程参数 |
| `runtime/` | 系统运行阶段、初始化状态、生命周期标志 |
| `bootstrap/` | 提供启动框架骨架；**不包含**项目特定的 adapter 注册代码，该部分由 `apps/wash_controller/wiring.c` 承担 |

**不负责**：

- 设备业务逻辑
- 流程控制规则
- 硬件实现
- M8 机型特定的模块接线

### 4.2 `domain/` —— 业务核心层

**职责**：

- 定义设备能力语义
- 定义洗车流程规则
- 定义安全规则
- 定义领域状态、命令、事件模型

#### `domain/model/`

| 文件 | 职责 |
|------|------|
| `device_state.h` | 设备状态、运行状态、联网状态枚举 |
| `wash_types.h` | 洗车模式、步骤、退出条件类型 |
| `safety_types.h` | 安全状态、互锁状态、恢复策略 |
| `command.h` | 统一命令类型定义 |
| `alarm_code.h` | 报警码、等级、分类定义 |

> 注：事件类型（`event_type_t` / `event_t`）定义在 `common/event_types.h`，不在 `domain/model/`，避免 `core → domain` 反向依赖。

#### `domain/device/`

| 文件 | 职责 |
|------|------|
| `brush.*` | 刷子语义：选刷、启动、停止、复位 |
| `gantry.*` | 龙门语义：前进、后退、归位、位置查询 |
| `top_lift.*` | 顶刷升降语义：上升、下降、回零 |
| `water.*` | 水路语义：预洗、刷洗、高压、全关 |
| `gate.*` | 挡杆和指示灯语义：放行、拦截、灯态切换 |

#### `domain/process/`

| 文件 | 职责 |
|------|------|
| `recipe.*` | 根据洗车模式提供步骤配方 |
| `step_engine.*` | 单步执行、退出条件判断、步骤推进 |
| `wash_mode.*` | 模式管理与模式级策略 |

#### `domain/safety/`

| 文件 | 职责 |
|------|------|
| `alarm_registry.*` | 报警注册表：活跃实例、PostWash 查询、分组重评估 |
| `safety_posture.*` | 安全姿态聚合：`NOMINAL / LOCKOUT` |

### 4.3 `application/` —— 编排层

**职责**：

- 串联领域模块
- 消费命令与异步事件
- 驱动设备状态机与流程执行
- 聚合状态并触发上报

#### `application/orchestrators/`

| 文件 | 职责 |
|------|------|
| `command_gateway.*` | 外部指令入口：`command_port.inject` → `OperationalMode` 仲裁 → 副作用分发 |
| `op_mode_bridge.*` | 领域/平台事件 → `op_mode_on_xxx()` 喂入聚合 |
| `operational_projection.*` | `EVT_OP_MODE_*` → `operational_snapshot` 运行模式投影 |
| `safety_projection.*` | 报警/安全事件 → `safety_snapshot` 投影 |
| `wash_projection.*` | `EVT_WASH_SESSION_STARTED` → `wash_snapshot` 投影 |
| `wash_orchestrator.*` | 洗车流程编排，管理洗车工作线程 |
| `emergency_handler.*` | LOCKOUT 归位 + 急停延后完备停机 |
| `report_scheduler.*` | 调度云端属性上报 |

#### 命令协作规则（v2.0，已落地）

外部命令（CLI / 云端 / 仿真）统一经 `command_port.inject()` 进入 `command_gateway`：

```
外部命令（CLI / MQTT / 仿真）
        │ command_port.inject()
        ▼
application/command_gateway（入队 event_dispatch，同步等待回执）
        │ op_mode_handle_command()
        ▼
domain/operational_mode（命令矩阵 + 转移表）
        │ ALLOWED 时 dispatch_side_effects()
        ▼
wash_orchestrator / self_check_service / recovery_service / alarm_registry 等
```

具体规则：

- `OperationalMode` 是命令的唯一决策点（命令矩阵 + 附加条件）
- `command_gateway` 只编排调用顺序，不做模式转移决策
- `wash_orchestrator` 通过发布 `EVT_WASH_*` 事件通知流程结果，由 `op_mode_bridge` 喂入聚合
- 手动点动经 `CMD_MANUAL_ACTUATOR` 统一仲裁，不再由项目层单独维护准入判断

### 4.4 `ports/` —— 边界接口层

**职责**：

- 定义领域层与外部世界之间的接口契约
- 不包含任何实现、不依赖任何 SDK

#### `ports/hal/`

| 文件 | 职责 |
|------|------|
| `hal_motion_port.h` | 龙门、刷子、升降动作接口 |
| `hal_io_port.h` | 数字输入输出接口与输入回调接口 |
| `hal_water_port.h` | 水路控制接口 |
| `hal_sensor_port.h` | 限位、编码器、急停、故障查询接口 |
| `hal_indicator_port.h` | 指示灯、蜂鸣器、挡杆接口 |

#### `ports/inbound/` 与 `ports/outbound/cloud/`

| 路径 | 职责 |
|------|------|
| `inbound/command/command_port.h` | 外部命令统一注入接口（CLI、云端下行、仿真控制台等） |
| `outbound/cloud/report/report_port.h` | 云端状态上报接口 |

说明：MQTT 下行解析不属于 port 层，由 `adapters/inbound/cloud` + 项目点位表 + `common/point_table` 完成后再注入 `command_port`。

#### `ports/storage/`

| 文件 | 职责 |
|------|------|
| `param_store.h` | 运行期参数读写接口 |
| `deploy_store.h` | 部署期只读配置接口 |

说明：

- `ports/storage/` 定义的是**底层存储契约**
- 它只描述”如何按键值或结构读写存储”
- 它不直接暴露给大多数业务模块使用
- `service/svc_param/` 在内部依赖 `param_store.h`
- `service/svc_param/` 对上提供**带业务语义的参数服务**
- 因此两者是**上下层封装关系**，不是同一层级的重复抽象

#### `ports/safety/`

| 文件 | 方向 | 实现方 | 调用方 |
|------|------|--------|--------|
| `alarm_binding_port.h` | 入（adapter → domain） | `domain/safety/alarm_registry` | `projects/m8/adapters/alarm/` |
| `safety_action_port.h` | 出（application → adapter） | `adapters/machine/m8/` | `application/emergency_handler`（项目接线） |

说明：

- `alarm_binding_port.h`：`adapters/machine/` 通过此接口注册硬件信号到报警码的映射回调，禁止直接 `#include` `domain/safety/` 头文件
- `safety_action_port.h`：项目编排器（如 `emergency_handler`）通过此接口触发机型特定的复位动作；`domain/safety/` 禁止直接调用此端口
- 两个端口均在 `apps/wash_controller/wiring.c` 中完成实现注入

### 4.5 `adapters/` —— 外部适配器层

**职责**：

- 实现 `ports/` 中定义的接口
- 接入真机硬件、仿真、云平台、CLI、JSON 存储
- 发布外部事件，但不承载业务决策

#### `adapters/hal/`

| 路径 | 职责 |
|------|------|
| `hal/linux_hw/` | 真机硬件实现，内部调用 `driver/` |
| `hal/sim_hw/` | PC 仿真实现，提供替代行为 |

#### `common/point_table/`

| 文件 | 职责 |
|------|------|
| `point_table.h` | 标识符点位表类型与公共 API |
| `point_table_to_json.c` | 遍历点表 get 回调，序列化为 JSON |
| `point_table_from_json.c` | 解析 JSON 并调用 set；提供 `point_table_find_entry` / `point_table_find_entry_at`、`point_table_parse_cjson_value`、`point_apply_result_record_error` |

说明：传输无关的通用工具，不依赖 MQTT 或云平台。项目云端点位登记放在 `projects/<project>/adapters/cloud/`（如 `m8_cloud_model.*`、`m8_cloud_register.*`）；手动机构动作放在 `projects/<project>/adapters/manual/`（`m8_manual_action.*`，运动类经 `CMD_MANUAL_ACTUATOR` 仲裁）。

#### `adapters/inbound/cloud/` 与 `adapters/outbound/cloud/`

| 路径 | 职责 |
|------|------|
| `inbound/cloud/providers/snack/snack_cloud_command_adapter.*` | Snack MQTT 下行：连接、注册 recv 回调 |
| `outbound/cloud/providers/snack/snack_cloud_report_adapter.*` | Snack MQTT 上行：实现 `cloud_report_ops` |

#### `adapters/storage/json/`

| 文件 | 职责 |
|------|------|
| `json_param_store.c` | 运行期参数 JSON 实现 |
| `json_deploy_store.c` | 部署期配置 JSON 实现 |

#### `adapters/ui/`

| 路径 | 职责 |
|------|------|
| `ui/cli/` | CLI 入口与命令映射 |
| `ui/mqtt_cmd/` | MQTT 下行命令解析 |

#### `adapters/machine/m8/`

| 文件 | 职责 |
|------|------|
| `m8_machine_map.h` | M8 机型硬件映射 |
| `m8_alarm_adapt.c` | M8 机型报警绑定与硬件回调适配 |
| `m8_feature_map.h` | M8 功能安装矩阵 |
| `m8_boot_profile.c` | M8 启动默认输出和安全初始化 |

### 4.6 `service/` —— 纯软件共享服务层

**职责**：

- 提供跨模块共享的软件能力
- 不依赖任何硬件驱动和机型实现

#### `service/dev_ctx/`

| 文件 | 职责 |
|------|------|
| `dev_ctx.h` | 统一只读快照与分片写入接口 |
| `dev_ctx.c` | 设备状态投影的线程安全存储 |

#### `service/svc_param/`

| 文件 | 职责 |
|------|------|
| `svc_param.h` | 运行期参数服务接口 |
| `svc_param.c` | 运行期参数服务实现，对上屏蔽存储细节 |

说明：

- `svc_param` 是对 `ports/storage/param_store.h` 的进一步封装
- `param_store` 面向存储实现，提供原始持久化能力
- `svc_param` 面向业务使用，提供有业务语义的参数访问接口
- 示例：
  - `param_store_get("gantry_freq", buf, size)`
  - `svc_param_get_gantry_freq()`
- 业务层优先依赖 `svc_param`，而不是直接依赖 `param_store`

### 4.7 `adapters/hal/providers/snack/` 与 `adapters/hal/linux_hw/` —— 真机底层适配

**职责**：

- `providers/snack/io_exp/` 封装可复用的 io_exp CAN IO 子板 SDK
- `providers/snack/modbus/` 封装 Snack 提供的 Modbus RTU SDK 上的 VFD、语音模块驱动
- 不承载业务语义
- 不进行流程控制

| 文件 | 职责 |
|------|------|
| `providers/snack/io_exp/io_exp_driver.*` | io_exp IO 板读写与原始输入变化通知 |
| `providers/snack/modbus/drv_vfd.*` | VFD 驱动与故障读取 |
| `providers/snack/modbus/drv_voice.*` | 语音模块 Modbus 控制与通信状态管理 |

**位置说明**：

驱动代码放在 `adapters/hal/` 下而非顶层 `src/driver/`，原因：
- io_exp 属于多项目可选 SDK provider，放在 `providers/snack/io_exp/`
- VFD、语音等基于 Snack Modbus SDK 的可复用 provider 放在 `providers/snack/modbus/`
- `sim_hw/` 完全不引用此目录，构建仿真版本时此目录不参与编译

### 4.8 `common/` —— 基础公共模块

**允许放入 `common/` 的内容**：

| 类型 | 示例 |
|------|------|
| 纯数据类型 | `sw_types.h`（`bool`、`uint8_t` 等别名） |
| 统一错误码 | `sw_error.h` |
| 日志接口 | `log.h`（仅声明，不含平台实现） |
| 时间工具 | `time_util.h/c`（与业务无关的时间换算） |
| 事件类型定义 | `event_types.h`（各层共用的事件 ID 枚举） |
| 通用断言/列表 | `assert_util.h`、`list.h` |

**禁止放入 `common/` 的内容**：

- 任何设备语义类型（设备状态、洗车模式、报警码等）
- 任何硬件地址、引脚编号、串口名称
- 任何 SDK 类型或平台 API 头文件
- 任何包含业务判断逻辑的函数实现

> `common/` 是全局依赖汇点，一旦污染将扩散到所有层。新增文件前必须先确认它对任何项目都无意义依赖。

**`event_types.h` 变更治理**：

`event_types.h` 是所有层共用的事件 ID 定义，变更影响范围最广，须遵守以下规则：

- 新增事件 ID 前必须确认：①该事件需要跨模块广播；②不存在等价的已有事件
- 禁止在框架事件名中出现机型名、机构名（如 `BRUSH`、`GANTRY`、`M8`）
- 实例级硬件故障（VFD、编码器等）须经 HAL 回调 → 项目 `alarm_binding` → `EVT_ALARM_TRIGGERED`，不得在 `event_types.h` 为每个实例单独占 ID
- 须区分硬件实例时，使用通用事件 + `param` 传递实例标识（如 IO 子板号、`hal_vfd_id_t`）
- 事件 ID 必须归入对应命名前缀，禁止新增裸 ID：

  | 前缀 | 归属场景 |
  |------|---------|
  | `EVT_HW_` | 硬件异步输入（急停边沿、IO 子板上下线等跨项目通用信号） |
  | `EVT_SAFETY_` | 安全域状态变化（LOCKOUT、CLEARED） |
  | `EVT_ALARM_` | 报警生命周期（激活、清除、批量清除） |
  | `EVT_WASH_` | 洗车流程状态变化（步骤完成、流程终止） |
  | `EVT_CMD_` | 外部命令输入（CLI、MQTT 下行） |
  | `EVT_CLOUD_` | 云连接状态变化 |

### 4.9 `config/` —— 分层配置

| 路径 | 职责 |
|------|------|
| `config/machine/` | 编译期硬件参数：引脚、串口、地址、板数 |
| `config/features/` | 功能安装开关 |
| `config/recipes/` | 洗车配方表 |
| `config/deployment/` | 部署期只读配置 |
| `config/threading/` | 线程优先级、栈大小、周期 |
| `runtime/event_bus/event_bus_config.h` | 事件总线队列深度、每事件 handler 上限 |

### 4.10 `tests/` 与 `tools/`

`tests/` 和 `tools/` 位于项目根目录，不放在 `src/` 内。

| 路径 | 职责 |
|------|------|
| `tests/unit/` | 单元测试 |
| `tests/integration/` | 集成测试 |
| `tests/scenario/` | 场景测试：标准洗、急停、VFD 故障、限位异常 |
| `tools/simulator/` | 仿真运行入口 |
| `tools/replay/` | 事件录制与回放工具 |

---

## 5. 调用规则

### 5.1 允许的依赖方向

```text
domain            -> ports/ + core/event_bus + common/
application       -> domain/ + ports/ + service/dev_ctx + service/svc_param + core/event_bus + common/
adapters/linux_hw -> ports/ + providers/snack/ + core/event_bus + common/
adapters/sim_hw   -> ports/ + core/event_bus + common/
adapters/其他     -> ports/ + core/event_bus + common/
service           -> core/ + common/ + ports/
core              -> common/
providers/snack/  -> common/ + Snack SDK headers
```

> `application/` 允许使用 `service/svc_param` 读取运行期参数（如龙门频率、刷子压力阈值），但禁止直接依赖 `ports/storage/param_store.h`；`service/svc_param` 是业务层访问参数的唯一入口。

> `common/event_types.h` 是所有层访问事件类型的统一入口，因此各层均显式依赖 `common/`。

### 5.2 严格禁止

- `domain/` 直接包含 `driver/` 或 `adapters/`
- `application/` 直接操作驱动、串口、IO、Modbus
- `adapters/` 直接实现业务状态机
- `service/` 依赖任何硬件驱动
- `service/` 直接依赖任何具体存储实现
- 使用事件总线替代所有同步函数调用

---

## 6. 事件总线设计边界

### 6.1 适合走事件总线的场景

- 硬件异步输入：急停、限位、IO 输入变化、VFD 故障
- 外部命令输入：CLI、MQTT 下行
- 安全状态广播：`LOCKOUT`、`CLEARED`
- 跨编排器通知：步骤完成、流程终止、上报触发

### 6.2 不适合走事件总线的场景

- 同一用例内部的顺序步骤推进
- 简单瞬时动作，如开阀、关阀、指示灯切换
- 组件内部状态查询
- 不需要广播的局部同步控制

### 6.3 事件总线职责边界

- 事件总线用于处理**异步事件**与**跨模块广播**
- 事件总线不替代明确的同步接口
- 事件总线不承载复杂业务状态

---

## 7. 模块依赖图

### 7.1 静态依赖总图

```text
                        ┌──────────────────────┐
                        │      common/         │
                        │ 类型/错误码/日志工具 │
                        └──────────┬───────────┘
                                   │
                    ┌──────────────┼──────────────┐
                    │              │              │
                    ▼              ▼              ▼
          ┌────────────────┐  ┌───────────┐
          │    core/       │  │  ports/   │
          │ 事件/调度/启动 │  │ 边界接口  │
          └──────┬─────────┘  └─────┬─────┘
                 │                  │
                 │                  │
                 ▼                  ▼
          ┌────────────────┐  ┌──────────────┐
          │   service/     │  │   domain/    │
          │ dev_ctx/参数   │  │ 业务核心规则 │
          └──────┬─────────┘  └──────┬───────┘
                 │                   │
                 │                   ▼
                 │          ┌────────────────┐
                 │          │ application/   │
                 │          │ 编排与用例层   │
                 │          └──────┬─────────┘
                 │                 │
                 └─────────────────┘
                                   │
                                   ▼
                           ┌───────────────┐
                           │  adapters/    │
                           │ 外部实现与适配 │
                           └──────┬────────┘
                                  │
                  ┌───────────────┼────────────────┐
                  │               │                │
                  ▼               ▼                ▼
           hal/providers     inbound/outbound      storage/json
           hal/sim           cloud/providers/snack  projects/.../adapters/cloud
                  │
                  ▼
           providers/snack/     ← 仅真机目标依赖，sim 不拉入 inbound cloud MQTT
```

### 7.2 目录级依赖关系

```text
domain            -> ports/ + core/event_bus + common/
application       -> domain/ + ports/ + service/dev_ctx + service/svc_param + core/event_bus + common/
adapters/linux_hw -> ports/ + providers/snack/ + core/event_bus + common/
adapters/sim_hw   -> ports/ + core/event_bus + common/
adapters/其他     -> ports/ + core/event_bus + common/
service           -> core/ + common/ + ports/
core              -> common/
providers/snack/  -> common/ + Snack SDK headers
```

### 7.3 启动装配依赖图

```text
bootstrap
  ├─ 初始化 common 基础能力
  ├─ 初始化 core/event_bus
  ├─ 初始化 core/scheduler
  ├─ 注册 ports <- adapters 实现（wiring.c 负责）
  │    ├─ hal ports          <- adapters/outbound/hal
  │    ├─ cloud report port  <- adapters/outbound/cloud/providers/snack
  │    ├─ command port       <- application/command_gateway（bootstrap 注册）
  │    ├─ storage ports      <- adapters/outbound/storage/json
  │    └─ safety_action_port <- m8_alarm_adapt（复位动作实现）
  ├─ 初始化 service/svc_param
  ├─ 初始化 service/dev_ctx
  ├─ 初始化 domain/safety/alarm_registry
  ├─ 初始化 projects/m8/adapters/alarm/m8_alarm_init
  │    └─ 通过 alarm_binding_port 向 alarm_registry 注册硬件信号回调
  ├─ 初始化 projects/m8/wiring/m8_alarm_reeval_bridge
  ├─ 初始化 domain/command_gateway/operational_mode
  ├─ 初始化 application/command_gateway
  ├─ 初始化 application/op_mode_bridge
  ├─ 初始化 application/operational_projection
  ├─ 初始化 application/safety_projection
  ├─ 初始化 application/wash_projection
  ├─ 初始化 application/wash_orchestrator
  ├─ 初始化 runtime/platform/safety_thread
  └─ 启动 scheduler 中登记的所有线程
```

### 7.4 运行时同步调用图

```text
application/usecases
        │
        ▼
application/orchestrators
        │
        ├──► domain/process  --->  domain/device  --->  ports/hal  --->  adapters/hal
        │
        └──► domain/safety   --->  ports/safety/alarm_binding_port（查询互锁状态）
                             --->  ports/safety/safety_action_port（触发复位动作）
```

说明：

- 同步调用用于顺序流程推进和即时动作控制
- `domain/device` 与 `domain/process` 只依赖 `ports/`，不依赖任何 adapter 实现
- `service/dev_ctx` 组合 `domain/telemetry/snapshot/` 三分域读模型，不参与动作链路
- `domain/safety/` 禁止直接调用 `ports/cloud` 或 `ports/storage`；安全状态上报由 `report_scheduler` 订阅事件后处理

### 7.5 运行时异步事件图

```text
硬件输入 / 云下行 / CLI
        │
        ▼
adapters/*
        │ 发布外部事件
        ▼
core/event_bus
        ├──────────────► domain/safety/safety_posture
        ├──────────────► application/op_mode_bridge
        ├──────────────► application/emergency_handler
        ├──────────────► application/wash_orchestrator（订阅方）
        └──────────────► application/report_scheduler
```

### 7.6 状态读模型依赖图

```text
operational_projection ──► operational_snapshot ──┐
safety_projection      ──► safety_snapshot       ──┼──► service/dev_ctx ───► report_scheduler
wash_projection        ──► wash_snapshot         ──┘                      ├──► CLI 查询
cloud_link_port（读穿）────────────────────────────┘                      ├──► 仿真观测
                                                                          └──► 测试断言
```

说明：

- 领域真源由 `application/*_projection` 订阅事件后写入 `domain/telemetry/snapshot/`
- `dev_ctx` 组合三分域 snapshot，`cloud_connected` 读穿 connection port
- `domain/` 与 `adapters/` 只负责发布事件，不直接写 snapshot
- 外部模块读取 `dev_ctx_snapshot()` 或各 snapshot getter
- `dev_ctx` 是组合只读入口，不是业务流程驱动器

### 7.7 安全域依赖图

```text
machine/m8_alarm_adapt
        │ 通过端口注册硬件信号回调（禁止直接依赖 domain/safety/）
        ▼
ports/safety/alarm_binding_port.h   ← 接口契约（注册报警触发回调）
        │
        ▼
domain/safety/alarm_registry        ← 实现 alarm_binding_port
        │
        └──► domain/safety/safety_posture
                   │
                   └──► 发布 EVT_SAFETY_LOCKOUT / EVT_SAFETY_CLEARED

application/safety_projection
        │ 订阅报警事件
        └──► safety_snapshot_update(...)

application/emergency_handler
        │ 订阅 EVT_SAFETY_LOCKOUT
        └──► wash_orchestrator_abort(...) + 安全归位
```

说明：

- `adapters/machine/m8/` 只允许依赖 `ports/safety/alarm_binding_port.h`，禁止直接 `#include` `domain/safety/` 的头文件
- `alarm_binding_port.h` 定义硬件信号到报警码的注册接口，由 `domain/safety/alarm_registry` 实现并在 `bootstrap` 阶段注入
- `safety_projection` 仅负责读模型投影；安全动作由 `emergency_handler` 等项目编排器执行

### 7.8 真机/仿真替换图

```text
                ports/hal
                   │
         ┌─────────┴─────────┐
         │                   │
         ▼                   ▼
adapters/hal/linux_hw   adapters/hal/sim_hw
         │                   │
         ▼                   ▼
      driver/*            模拟实现
```

说明：

- `domain/` 与 `application/` 不感知当前接入的是哪一种 HAL 实现
- 真机与仿真切换仅发生在 `bootstrap/wiring` 与构建配置层

---

## 8. 线程模型

### 8.1 统一规则

线程分为两类，管理方式不同：

**业务线程**（`safety_thread`、`wash_worker_thread`、`event_dispatch_thread` 等）：
- 必须由 `core/scheduler` 统一创建和管理
- 禁止在 `domain/`、`application/`、`adapters/` 中直接调用 `pthread_create`
- 线程参数统一定义在 `config/threading/thread_config.h`

**基础设施组件线程**（如 `alarm_monitor` 的 IO 线程、检测线程）：
- 允许通过 `worker_thread_t` 抽象自行创建和管理线程生命周期
- 禁止直接调用 `pthread_create`，必须通过 `worker_thread_t` 接口（`worker_thread_start`/`worker_thread_join`/`worker_thread_destroy`）
- 这类组件只依赖 `worker_thread_t` 抽象，不依赖 `scheduler`，因此可独立移植到其他项目

### 8.2 推荐线程清单

| 线程 | 调度策略 | 职责 |
|------|----------|------|
| `event_dispatch_thread` | SCHED_OTHER | 分发异步事件、消费 command_gateway 队列 |
| `safety_thread` | SCHED_FIFO | 硬件急停边沿检测 + 快速动力切断 |
| `io_poll_thread` | SCHED_OTHER | IO 轮询与输入采样 |
| `wash_worker_thread` | SCHED_OTHER | 洗车流程专用线程 |
| `cloud_thread` | SCHED_OTHER | 云连接与上报 |

### 8.3 洗车流程线程策略

- 保留独立的 `wash_worker_thread`
- 线程内部顺序执行洗车步骤
- 步骤完成、流程终止通过事件通知外界
- 不要求将整个洗车流程强行事件化

**启动触发机制**：

`wash_worker_thread` 持续运行但大部分时间处于等待状态，由 `wash_orchestrator` 在收到启动指令后触发：

```c
// 推荐方式：wash_orchestrator 通过直接调用触发，而非通过事件总线
// 理由：启动洗车是确定的同步指令，不需要广播，无需事件中转
wash_orchestrator_start(mode);  // command_gateway 仲裁 ALLOWED 后由副作用分发调用
```

`wash_worker_thread` 内部通过 `worker_run_ctx_wait()` 阻塞等待，`wash_orchestrator_start_session()` 调用 `worker_thread_notify()` 将其唤醒，线程醒来后检查会话状态并开始执行步骤。

**故障中断机制**：

洗车线程在每个步骤的等待循环中必须周期性检查中止标志，而不是无限阻塞等待硬件反馈：

```c
// 步骤等待示意（伪代码）
while (!step_exit_condition_met()) {
    if (wash_abort_requested()) {   // 检查 wash_orchestrator 中止标志
        publish_event(EVT_WASH_ABORTED);
        return;
    }
    worker_run_ctx_wait(run_ctx, STEP_CHECK_INTERVAL_MS);  // 带超时等待
}
```

`emergency_handler` 收到 `EVT_SAFETY_LOCKOUT` 后调用 `wash_orchestrator_abort()`，洗车线程在下一个检查点响应并退出，再通过事件通知外界流程已终止。

### 8.4 内存管理策略

**分阶段规则**：

| 阶段 | 允许操作 | 禁止操作 |
|------|---------|---------|
| 启动阶段（bootstrap） | `calloc` 进行一次性模块对象分配 | 分配后释放再重新分配 |
| 运行阶段（scheduler_run 之后） | 仅使用静态/栈内存 | 任何 `malloc` / `calloc` / `free` |

**分配失败处理**：

启动阶段任何内存分配失败均视为致命错误，必须立即停止初始化并记录错误，禁止以空指针继续运行。

**对象所有权**：

- 模块通过 `_create()` 函数分配自身对象，通过 `_destroy()` 函数释放
- 调用方持有指针但不负责直接 `free`，必须通过模块提供的 `_destroy()` 接口归还所有权
- 禁止跨模块直接 `free` 他人分配的内存

---

## 9. `dev_ctx` 设计

### 9.1 定位

`dev_ctx` 是**组合只读状态快照**，用于：

- 云端上报
- CLI 查询
- 仿真观测
- 测试断言
- 诊断工具读取

它不是系统骨架层的一部分，因此放在 `service/dev_ctx/`，不放在 `core/runtime/`。

### 9.2 建议结构

```c
typedef struct {
    operational_mode_t operational_mode;
    bool               service_enabled;
    bool               estop_active;
    wash_mode_t        wash_mode;
    bool               cloud_connected;
    safety_posture_t   safety_posture;
    bool               blocking_active;
    uint32_t           top_alarm_code;
    unsigned           active_alarm_count;
    alarm_instance_t   active_list[ALARM_ACTIVE_MAX];
} device_context_t;
```

### 9.3 更新规则

`dev_ctx` 不持有独立缓存，无公开 `set_*` API。分域读模型由投影模块维护：

- `operational_projection` → `operational_snapshot`（`EVT_OP_MODE_CHANGED` / `EVT_OP_MODE_CONTEXT_SYNC`）
- `safety_projection` → `safety_snapshot`（`EVT_ALARM_*`）
- `wash_projection` → `wash_snapshot`（`EVT_WASH_SESSION_STARTED`）
- `cloud_connected`：`dev_ctx_snapshot()` 读穿 `cloud_link_port.is_online()`

补充规则：

- `domain/` 层模块不得直接写 snapshot
- `adapters/` 层模块不得直接写 snapshot
- `domain/` 与 `adapters/` 只能通过事件总线发布状态变化
- `application/*_projection` 订阅相关事件后更新 snapshot

统一读取接口：

- `dev_ctx_snapshot()`（组合视图）
- `operational_snapshot_get()` / `safety_snapshot_get()` / `wash_snapshot_get()`（分域副本，跨线程安全）

---

## 10. 报警与安全设计

### 10.1 `alarm_registry` 的职责

- 维护报警配置表与活跃实例列表
- 处理激活/清除与等级判断
- 提供 PostWash 查询（`has_blocking_active`）
- 按 `motion_reeval_group_id_t` 重评估 ON_MOTION 报警

### 10.2 `m8_alarm_adapt` 的职责

- 通过 `ports/safety/alarm_binding_port.h` 向 `alarm_registry` 注册硬件信号到报警码的映射回调

### 10.3 `safety_posture` 的职责

- 综合活跃报警的 `response_strategy`
- 输出 `NOMINAL / LOCKOUT` 两态
- 向应用层广播安全状态变化

### 10.4 设计原则

- 报警核心属于 `domain/safety/`
- 机型报警绑定属于 `adapters/machine/m8/`，通过端口与 domain 层交互，两者无直接 include 依赖
- 安全规则优先级高于业务流程
- 复位与安全动作由 `application/emergency_handler` 等项目编排器执行；`domain/safety/` 不直接触发硬件动作

---

## 11. 配置分层规则

### 11.1 编译期配置

位置：`config/machine/`

包含：

- 引脚号
- 串口名
- Modbus 地址
- IO 板数量

特点：

- 改动后需要重新编译

### 11.2 功能开关配置

位置：`config/features/`

包含：

- 顶刷是否安装
- 侧刷是否安装
- 某水路是否安装

### 11.3 配方配置

位置：`config/recipes/`

包含：

- 标准洗步骤表
- 快洗步骤表
- 每步动作参数与退出条件

### 11.4 部署期配置

位置：`config/deployment/`

包含：

- 设备 SN
- 站点 ID
- 服务器地址
- Topic 命名参数

特点：

- 出厂写入
- 运行期只读

### 11.5 线程配置

位置：`config/threading/`

包含：

- 线程优先级
- 栈大小
- 轮询周期
- 队列长度

---

## 12. 真机与仿真切换策略

### 12.1 切换原则

- `domain/`、`application/`、`service/` 层代码不因真机/仿真而变化
- 通过 `bootstrap/wiring` 在启动阶段注册不同 adapter 实现

### 12.2 真机实现

- 由 `adapters/hal/linux_hw/` 提供
- 内部依赖 `adapters/hal/providers/snack/`（io_exp、Modbus 等 Snack SDK provider）

### 12.3 仿真实现

- 由 `adapters/hal/sim_hw/` 提供
- 提供可观测、可注入、可脚本化的行为

---

## 13. 致命错误处理与系统复位策略

### 13.1 错误分级

| 级别 | 定义 | 示例 | 处理方式 |
|------|------|------|---------|
| 可恢复错误 | 单次操作失败，可重试 | 参数读取失败、云上报超时 | 重试有限次，记录日志 |
| 业务级故障 | 需要进入 LOCKOUT 状态 | VFD 故障、急停、限位异常 | 触发安全域流程，等待人工复位 |
| 致命错误 | 系统无法继续安全运行 | 启动期内存分配失败、关键初始化失败、看门狗超时 | 安全关停输出，记录原因，复位系统 |

### 13.2 看门狗策略

- 硬件看门狗由 `core/scheduler` 的主循环每个 control tick 喂狗
- 看门狗超时时间必须大于最长单次 tick 处理时间的 3 倍，具体值定义在 `config/threading/thread_config.h`
- 若 `scheduler` 主循环因死锁或长时间阻塞停止喂狗，看门狗触发硬件复位
- 后台线程（`alarm_monitor`）不负责喂狗，其卡死由 `scheduler` 层面的超时监控检测

### 13.3 Fail-Safe 状态（硬件安全默认值）

当系统进入 `LOCKOUT` 或触发致命错误时，硬件输出必须切换到预定义的安全状态：

| 输出类型 | 安全默认值 |
|---------|-----------|
| 龙门运动 | 立即停止，抱闸 |
| 刷子电机 | 立即停止 |
| 水路电磁阀 | 全部关闭 |
| 挡杆 | 保持当前状态（不做额外动作） |
| 指示灯 | 切换为故障状态灯 |

Fail-Safe 状态的输出动作由 `adapters/machine/m8/m8_boot_profile.c` 定义，在 `safety_action_port` 的 `enter_failsafe()` 接口中执行。

### 13.4 致命错误处理流程

```
致命错误触发（assert 失败 / 初始化失败 / 看门狗）
        │
        ▼
记录错误原因到持久化存储（非易失性，复位后可读）
        │
        ▼
通过 safety_action_port.enter_failsafe() 执行硬件安全关停
        │
        ▼
等待看门狗复位，或主动调用系统复位接口
```

禁止在致命错误路径上：
- 调用 `malloc` / `free`
- 获取互斥锁（可能死锁）
- 执行任何业务逻辑判断

### 13.5 启动期初始化失败策略

- 任何模块初始化失败，`bootstrap` 立即停止后续初始化
- 失败原因记录到持久存储，供复位后诊断
- 系统不得以部分初始化状态进入运行阶段

---

## 14. 测试与演进建议

### 14.1 测试优先级

1. 先补 `domain/safety/` 与 `domain/process/` 的单元测试
2. 再补 `application/` 的场景测试
3. 最后补真机与仿真一致性验证

### 14.2 推荐首批场景测试

- 标准洗全流程
- 快洗全流程
- 洗车中急停
- VFD 故障触发停机
- 限位异常
- 云断线后恢复

### 14.3 演进边界

- 新增云平台 provider：扩展 `adapters/inbound/cloud/providers/` 与 `adapters/outbound/cloud/providers/`
- 新增机型云端点位：扩展 `projects/<project>/adapters/cloud/`（如 `m8_cloud_model.*`）；手动点动扩展 `adapters/manual/`
- 新增存储方式：仅扩展 `adapters/storage/`
- 新增洗车模式：优先扩展 `config/recipes/` 与 `domain/process/`

---

## 15. 一句话总结

本框架采用 **`core + domain + application + ports + adapters + service`** 的分层方式，将**业务规则**与**外部世界**彻底隔离，确保控制软件在**安全性、可测试性、可演进性、真机/仿真切换能力**之间取得平衡，作为新项目的推荐架构基线。

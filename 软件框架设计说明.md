# 嵌入式控制软件框架设计说明

**版本**：v2.0  
**适用范围**：基于 Linux 的工业控制器（洗车机、自动化设备等）  
**架构基线**：面向新项目的重构版控制软件框架  
**关键词**：Hexagonal、领域分层、事件驱动、统一调度、真机/仿真双实现

---

## 1. 设计目标

| 目标 | 说明 |
|------|------|
| **业务与外部依赖解耦** | 业务规则不直接依赖硬件 SDK、MQTT、JSON、文件系统 |
| **真机/仿真可切换** | 通过 `ports/ + adapters/` 实现真实硬件与 PC 仿真的可替换 |
| **安全逻辑独立** | 急停、报警、互锁从业务流程中解耦，形成独立安全域 |
| **流程规则可配置** | 洗车配方、步骤规则、线程参数、部署配置分层管理 |
| **线程统一治理** | 线程由统一调度层创建和管理，禁止模块私自起线程 |
| **测试与回放友好** | 统一事件模型，支持场景测试、仿真与事件回放 |

---

## 2. 总体架构原则

### 2.1 分层原则

- `domain/` 只表达业务规则与设备语义
- `application/` 只做编排，不实现底层细节
- `ports/` 只定义边界接口，不包含任何具体实现
- `adapters/` 实现外部世界接入，可依赖 SDK、驱动、文件系统
- `core/` 提供系统骨架能力，不承载业务语义
- `service/` 只放纯软件共享服务

### 2.2 依赖原则

- 业务核心依赖接口，不依赖实现
- 真机与仿真实现是同级适配器，不通过 `#ifdef` 污染业务层
- 安全逻辑优先于业务流程
- 异步输入通过事件传递，同步动作通过直接调用

### 2.3 禁止事项

- 禁止 `domain/` 出现硬件常量、串口名、Modbus 地址、SDK 类型
- 禁止 `application/` 直接调用 `driver/` 或 `adapters/`
- 禁止模块自行创建线程
- 禁止跨模块直接修改设备状态快照
- 禁止将所有同步调用都改造成事件流

---

## 3. 最终目录结构

```text
./
  core/
    event_bus/
      event_bus.h
      event_bus.c
    scheduler/
      scheduler.h
      scheduler.c
      thread_registry.h
      thread_registry.c
    runtime/
      runtime.h
      runtime.c
      lifecycle.h
    bootstrap/
      bootstrap.h
      bootstrap.c
      wiring.h

  domain/
    model/
      device_state.h
      wash_types.h
      safety_types.h
      command.h
      alarm_code.h
    device/
      brush.h
      brush.c
      gantry.h
      gantry.c
      top_lift.h
      top_lift.c
      water.h
      water.c
      gate.h
      gate.c
    process/
      recipe.h
      recipe.c
      step_engine.h
      step_engine.c
      wash_mode.h
      wash_mode.c
    safety/
      alarm_core.h
      alarm_core.c
      interlock.h
      interlock.c
      safety_fsm.h
      safety_fsm.c

  application/
    orchestrators/
      device_fsm.h
      device_fsm.c
      wash_orchestrator.h
      wash_orchestrator.c
      safety_supervisor.h
      safety_supervisor.c
      report_aggregator.h
      report_aggregator.c
    usecases/
      start_wash.h
      start_wash.c
      stop_wash.h
      stop_wash.c
      reset_fault.h
      reset_fault.c
      home_device.h
      home_device.c

  ports/
    hal/
      hal_motion_port.h
      hal_io_port.h
      hal_water_port.h
      hal_sensor_port.h
      hal_indicator_port.h
    cloud/
      report_port.h
      command_port.h
    storage/
      param_store.h
      deploy_store.h

  adapters/
    hal/
      linux_hw/
        hal_motion_linux.c
        hal_io_linux.c
        hal_water_linux.c
        hal_sensor_linux.c
        hal_indicator_linux.c
      sim_hw/
        hal_motion_sim.c
        hal_io_sim.c
        hal_water_sim.c
        hal_sensor_sim.c
        hal_indicator_sim.c
    cloud/
      aliyun/
        aliyun_report_adapter.c
        aliyun_command_adapter.c
        aliyun_topics.h
    storage/
      json/
        json_param_store.c
        json_deploy_store.c
    ui/
      cli/
        cli_adapter.c
        cli_commands.c
      mqtt_cmd/
        mqtt_command_parser.c
    machine/
      m8/
        m8_machine_map.h
        m8_alarm_adapt.c
        m8_feature_map.h
        m8_boot_profile.c

  service/
    dev_ctx/
      dev_ctx.h
      dev_ctx.c
    svc_param/
      svc_param.h
      svc_param.c

  config/
    machine/
      m8_machine_config.h
    features/
      m8_features.h
    recipes/
      standard_wash_recipe.h
      quick_wash_recipe.h
    deployment/
      device.json
    threading/
      thread_config.h

  driver/
    drv_io.h
    drv_io.c
    drv_vfd.h
    drv_vfd.c
    drv_stepper.h
    drv_stepper.c

  common/
    sw_types.h
    sw_error.h
    log.h
    time_util.h
    time_util.c
    assert_util.h
    list.h
    version.h
    event_types.h

  tests/
    unit/
    integration/
    scenario/

  tools/
    simulator/
    replay/
```

---

## 4. 分层职责说明

### 4.1 `core/` —— 系统骨架层

**职责**：

- 统一事件分发
- 统一线程创建与管理
- 生命周期与运行阶段管理
- 模块初始化装配与依赖接线

**包含模块**：

| 模块 | 职责 |
|------|------|
| `event_bus/` | 统一发布/订阅异步事件，不做业务判断 |
| `scheduler/` | 统一管理线程、周期任务、线程参数 |
| `runtime/` | 系统运行阶段、初始化状态、生命周期标志 |
| `bootstrap/` | 按依赖顺序装配系统并注册所有实现 |

**不负责**：

- 设备业务逻辑
- 流程控制规则
- 硬件实现

### 4.2 `domain/` —— 业务核心层

**职责**：

- 定义设备能力语义
- 定义洗车流程规则
- 定义安全规则
- 定义领域状态、命令、事件模型

#### `domain/model/`

| 文件 | 职责 |
|------|------|
| `device_state.h` | 设备状态、运行状态、联网状态枚举 |
| `wash_types.h` | 洗车模式、步骤、退出条件类型 |
| `safety_types.h` | 安全状态、互锁状态、恢复策略 |
| `command.h` | 统一命令类型定义 |
| `alarm_code.h` | 报警码、等级、分类定义 |

> 注：事件类型（`event_type_t` / `event_t`）定义在 `common/event_types.h`，不在 `domain/model/`，避免 `core → domain` 反向依赖。

#### `domain/device/`

| 文件 | 职责 |
|------|------|
| `brush.*` | 刷子语义：选刷、启动、停止、复位 |
| `gantry.*` | 龙门语义：前进、后退、归位、位置查询 |
| `top_lift.*` | 顶刷升降语义：上升、下降、回零 |
| `water.*` | 水路语义：预洗、刷洗、高压、全关 |
| `gate.*` | 挡杆和指示灯语义：放行、拦截、灯态切换 |

#### `domain/process/`

| 文件 | 职责 |
|------|------|
| `recipe.*` | 根据洗车模式提供步骤配方 |
| `step_engine.*` | 单步执行、退出条件判断、步骤推进 |
| `wash_mode.*` | 模式管理与模式级策略 |

#### `domain/safety/`

| 文件 | 职责 |
|------|------|
| `alarm_core.*` | 报警核心引擎：防抖、等级、恢复规则 |
| `interlock.*` | 互锁规则判断，决定动作是否允许 |
| `safety_fsm.*` | 安全状态机：`OK / WARNING / LOCKOUT` |

### 4.3 `application/` —— 编排层

**职责**：

- 串联领域模块
- 消费命令与异步事件
- 驱动设备状态机与流程执行
- 聚合状态并触发上报

#### `application/orchestrators/`

| 文件 | 职责 |
|------|------|
| `device_fsm.*` | 顶层设备状态机 |
| `wash_orchestrator.*` | 洗车流程编排，管理洗车工作线程 |
| `safety_supervisor.*` | 监听安全事件并统一干预业务编排 |
| `report_aggregator.*` | 聚合状态并调用云上报接口 |

#### `application/usecases/`

| 文件 | 职责 |
|------|------|
| `start_wash.*` | 启动洗车用例 |
| `stop_wash.*` | 停止洗车用例 |
| `reset_fault.*` | 故障复位用例 |
| `home_device.*` | 回零/归位用例 |

### 4.4 `ports/` —— 边界接口层

**职责**：

- 定义领域层与外部世界之间的接口契约
- 不包含任何实现、不依赖任何 SDK

#### `ports/hal/`

| 文件 | 职责 |
|------|------|
| `hal_motion_port.h` | 龙门、刷子、升降动作接口 |
| `hal_io_port.h` | 数字输入输出接口与输入回调接口 |
| `hal_water_port.h` | 水路控制接口 |
| `hal_sensor_port.h` | 限位、编码器、急停、故障查询接口 |
| `hal_indicator_port.h` | 指示灯、蜂鸣器、挡杆接口 |

#### `ports/cloud/`

| 文件 | 职责 |
|------|------|
| `report_port.h` | 状态上报接口 |
| `command_port.h` | 外部命令接入接口 |

#### `ports/storage/`

| 文件 | 职责 |
|------|------|
| `param_store.h` | 运行期参数读写接口 |
| `deploy_store.h` | 部署期只读配置接口 |

说明：

- `ports/storage/` 定义的是**底层存储契约**
- 它只描述“如何按键值或结构读写存储”
- 它不直接暴露给大多数业务模块使用
- `service/svc_param/` 在内部依赖 `param_store.h`
- `service/svc_param/` 对上提供**带业务语义的参数服务**
- 因此两者是**上下层封装关系**，不是同一层级的重复抽象

### 4.5 `adapters/` —— 外部适配器层

**职责**：

- 实现 `ports/` 中定义的接口
- 接入真机硬件、仿真、云平台、CLI、JSON 存储
- 发布外部事件，但不承载业务决策

#### `adapters/hal/`

| 路径 | 职责 |
|------|------|
| `hal/linux_hw/` | 真机硬件实现，内部调用 `driver/` |
| `hal/sim_hw/` | PC 仿真实现，提供替代行为 |

#### `adapters/cloud/aliyun/`

| 文件 | 职责 |
|------|------|
| `aliyun_report_adapter.c` | 阿里云状态上报实现 |
| `aliyun_command_adapter.c` | 阿里云下行命令接收与注入 |
| `aliyun_topics.h` | Topic 与属性模型字段定义 |

#### `adapters/storage/json/`

| 文件 | 职责 |
|------|------|
| `json_param_store.c` | 运行期参数 JSON 实现 |
| `json_deploy_store.c` | 部署期配置 JSON 实现 |

#### `adapters/ui/`

| 路径 | 职责 |
|------|------|
| `ui/cli/` | CLI 入口与命令映射 |
| `ui/mqtt_cmd/` | MQTT 下行命令解析 |

#### `adapters/machine/m8/`

| 文件 | 职责 |
|------|------|
| `m8_machine_map.h` | M8 机型硬件映射 |
| `m8_alarm_adapt.c` | M8 机型报警绑定与硬件回调适配 |
| `m8_feature_map.h` | M8 功能安装矩阵 |
| `m8_boot_profile.c` | M8 启动默认输出和安全初始化 |

### 4.6 `service/` —— 纯软件共享服务层

**职责**：

- 提供跨模块共享的软件能力
- 不依赖任何硬件驱动和机型实现

#### `service/dev_ctx/`

| 文件 | 职责 |
|------|------|
| `dev_ctx.h` | 统一只读快照与分片写入接口 |
| `dev_ctx.c` | 设备状态投影的线程安全存储 |

#### `service/svc_param/`

| 文件 | 职责 |
|------|------|
| `svc_param.h` | 运行期参数服务接口 |
| `svc_param.c` | 运行期参数服务实现，对上屏蔽存储细节 |

说明：

- `svc_param` 是对 `ports/storage/param_store.h` 的进一步封装
- `param_store` 面向存储实现，提供原始持久化能力
- `svc_param` 面向业务使用，提供有业务语义的参数访问接口
- 示例：
  - `param_store_get("gantry_freq", buf, size)`
  - `svc_param_get_gantry_freq()`
- 业务层优先依赖 `svc_param`，而不是直接依赖 `param_store`

### 4.7 `driver/` —— 驱动层

**职责**：

- 封装最底层的 IO、VFD、步进电机驱动
- 不承载业务语义
- 不进行流程控制

| 文件 | 职责 |
|------|------|
| `drv_io.*` | IO 板读写与原始输入变化通知 |
| `drv_vfd.*` | VFD 驱动与故障读取 |
| `drv_stepper.*` | 步进电机脉冲驱动 |

### 4.8 `config/` —— 分层配置

| 路径 | 职责 |
|------|------|
| `config/machine/` | 编译期硬件参数：引脚、串口、地址、板数 |
| `config/features/` | 功能安装开关 |
| `config/recipes/` | 洗车配方表 |
| `config/deployment/` | 部署期只读配置 |
| `config/threading/` | 线程优先级、栈大小、周期、队列长度 |

### 4.9 `tests/` 与 `tools/`

| 路径 | 职责 |
|------|------|
| `tests/unit/` | 单元测试 |
| `tests/integration/` | 集成测试 |
| `tests/scenario/` | 场景测试：标准洗、急停、VFD 故障、限位异常 |
| `tools/simulator/` | 仿真运行入口 |
| `tools/replay/` | 事件录制与回放工具 |

---

## 5. 调用规则

### 5.1 允许的依赖方向

```text
domain        -> ports/ + core/event_bus + common/
application   -> domain/ + ports/ + service/dev_ctx + common/
adapters      -> ports/ + driver/ + core/event_bus + common/
service       -> core/ + common/ + ports/
core          -> common/
```

> `common/event_types.h` 是所有层访问事件类型的统一入口，因此各层均显式依赖 `common/`。

### 5.2 严格禁止

- `domain/` 直接包含 `driver/` 或 `adapters/`
- `application/` 直接操作驱动、串口、IO、Modbus
- `adapters/` 直接实现业务状态机
- `service/` 依赖任何硬件驱动
- `service/` 直接依赖任何具体存储实现
- 使用事件总线替代所有同步函数调用

---

## 6. 事件总线设计边界

### 6.1 适合走事件总线的场景

- 硬件异步输入：急停、限位、IO 输入变化、VFD 故障
- 外部命令输入：CLI、MQTT 下行
- 安全状态广播：`LOCKOUT`、`CLEARED`
- 跨编排器通知：步骤完成、流程终止、上报触发

### 6.2 不适合走事件总线的场景

- 同一用例内部的顺序步骤推进
- 简单瞬时动作，如开阀、关阀、指示灯切换
- 组件内部状态查询
- 不需要广播的局部同步控制

### 6.3 事件总线职责边界

- 事件总线用于处理**异步事件**与**跨模块广播**
- 事件总线不替代明确的同步接口
- 事件总线不承载复杂业务状态

---

## 7. 模块依赖图

### 7.1 静态依赖总图

```text
                        ┌──────────────────────┐
                        │      common/         │
                        │ 类型/错误码/日志工具 │
                        └──────────┬───────────┘
                                   │
                    ┌──────────────┼──────────────┐
                    │              │              │
                    ▼              ▼              ▼
          ┌────────────────┐  ┌───────────┐  ┌───────────────┐
          │    core/       │  │  ports/   │  │   driver/     │
          │ 事件/调度/启动 │  │ 边界接口  │  │ 底层驱动实现  │
          └──────┬─────────┘  └─────┬─────┘  └──────┬────────┘
                 │                  │               │
                 │                  │               │
                 ▼                  ▼               │
          ┌────────────────┐  ┌──────────────┐      │
          │   service/     │  │   domain/    │      │
          │ dev_ctx/参数   │  │ 业务核心规则 │      │
          └──────┬─────────┘  └──────┬───────┘      │
                 │                   │              │
                 │                   ▼              │
                 │          ┌────────────────┐      │
                 │          │ application/   │      │
                 │          │ 编排与用例层   │      │
                 │          └──────┬─────────┘      │
                 │                 │                │
                 └─────────────────┼────────────────┘
                                   │
                                   ▼
                           ┌───────────────┐
                           │  adapters/    │
                           │ 外部实现与适配 │
                           └──────┬────────┘
                                  │
                  ┌───────────────┼────────────────┐
                  │               │                │
                  ▼               ▼                ▼
           hal/linux_hw      cloud/aliyun      storage/json
           hal/sim_hw        ui/cli            machine/m8
```

### 7.2 目录级依赖关系

```text
domain        -> ports/ + core/event_bus
application   -> domain/ + ports/ + service/dev_ctx
adapters      -> ports/ + driver/ + core/event_bus
service       -> core/ + common/ + ports/
core          -> common/
driver        -> common/
```

### 7.3 启动装配依赖图

```text
bootstrap
  ├─ 初始化 common 基础能力
  ├─ 初始化 core/event_bus
  ├─ 初始化 core/scheduler
  ├─ 注册 ports <- adapters 实现
  │    ├─ hal ports     <- hal/linux_hw 或 hal/sim_hw
  │    ├─ cloud ports   <- cloud/aliyun
  │    └─ storage ports <- storage/json
  ├─ 初始化 service/svc_param
  ├─ 初始化 service/dev_ctx
  ├─ 初始化 domain/safety/alarm_core
  ├─ 初始化 adapters/machine/m8/m8_alarm_adapt
  ├─ 初始化 application/device_fsm
  ├─ 初始化 application/wash_orchestrator
  └─ 启动 scheduler 中登记的所有线程
```

### 7.4 运行时同步调用图

```text
application/usecases
        │
        ▼
application/orchestrators
        │
        ▼
domain/process  --->  domain/device  --->  ports/hal  --->  adapters/hal  --->  driver
        │
        └──────────>  domain/safety  --->  ports/storage / ports/cloud
```

说明：

- 同步调用用于顺序流程推进和即时动作控制
- `domain/device` 与 `domain/process` 只依赖 `ports/`，不依赖任何 adapter 实现
- `service/dev_ctx` 只作为状态读取与写入投影，不参与动作链路

### 7.5 运行时异步事件图

```text
硬件输入 / 云下行 / CLI
        │
        ▼
adapters/*
        │ 发布外部事件
        ▼
core/event_bus
        ├──────────────► domain/safety/safety_fsm
        ├──────────────► application/device_fsm
        ├──────────────► application/wash_orchestrator
        └──────────────► application/report_aggregator
```

### 7.6 状态读模型依赖图

```text
device_fsm -------------------┐
wash_orchestrator ------------┼──► service/dev_ctx ───► report_aggregator
safety_supervisor ------------┤                      ├──► CLI 查询
report_aggregator ------------┘                      ├──► 仿真观测
                                                     └──► 测试断言
```

说明：

- `dev_ctx` 的写入统一收口到 `application/` 编排器
- `domain/` 与 `adapters/` 只负责发布事件，不直接写 `dev_ctx`
- 外部模块只允许读取 `dev_ctx_snapshot()`
- `dev_ctx` 是统一状态投影，不是业务流程驱动器

### 7.7 安全域依赖图

```text
machine/m8_alarm_adapt
        │ 硬件信号映射
        ▼
domain/safety/alarm_core
        │
        ├──► domain/safety/interlock
        └──► domain/safety/safety_fsm
                   │
                   └──► 发布 EVT_SAFETY_LOCKOUT / EVT_SAFETY_CLEARED

application/safety_supervisor
        │ 订阅安全域事件
        ├──► dev_ctx_set_safety_state(...)
        └──► dev_ctx_set_alarm_state(...)
```

### 7.8 真机/仿真替换图

```text
                ports/hal
                   │
         ┌─────────┴─────────┐
         │                   │
         ▼                   ▼
adapters/hal/linux_hw   adapters/hal/sim_hw
         │                   │
         ▼                   ▼
      driver/*            模拟实现
```

说明：

- `domain/` 与 `application/` 不感知当前接入的是哪一种 HAL 实现
- 真机与仿真切换仅发生在 `bootstrap/wiring` 与构建配置层

---

## 8. 线程模型

### 8.1 统一规则

- 所有线程必须由 `core/scheduler` 创建
- 禁止各模块内部直接 `pthread_create`
- 所有线程参数统一定义在 `config/threading/thread_config.h`

### 8.2 推荐线程清单

| 线程 | 调度策略 | 职责 |
|------|----------|------|
| `event_dispatch_thread` | SCHED_OTHER | 分发异步事件 |
| `io_poll_thread` | SCHED_OTHER | IO 轮询与输入采样 |
| `device_fsm_thread` | SCHED_OTHER | 顶层设备状态机 |
| `wash_worker_thread` | SCHED_OTHER | 洗车流程专用线程 |
| `cloud_thread` | SCHED_OTHER | 云连接与上报 |
| `stepper_thread` | SCHED_FIFO | 步进电机脉冲执行（高精度，实时优先级）|

### 8.3 洗车流程线程策略

- 保留独立的 `wash_worker_thread`
- 线程内部顺序执行洗车步骤
- 步骤完成、流程终止、故障中断通过事件通知外界
- 不要求将整个洗车流程强行事件化

---

## 9. `dev_ctx` 设计

### 9.1 定位

`dev_ctx` 是**统一只读状态快照**，用于：

- 云端上报
- CLI 查询
- 仿真观测
- 测试断言
- 诊断工具读取

它不是系统骨架层的一部分，因此放在 `service/dev_ctx/`，不放在 `core/runtime/`。

### 9.2 建议结构

```c
typedef struct {
    dev_state_t     dev_state;
    safety_state_t  safety_state;
    wash_mode_t     wash_mode;
    wash_step_t     wash_step;
    int32_t         gantry_pos;
    bool            cloud_connected;
    bool            has_alarm;
    uint16_t        alarm_code;
    uint32_t        updated_ms;
} device_context_t;
```

### 9.3 写入规则

禁止直接暴露结构体可写指针，必须通过分片写接口更新：

- `dev_ctx_set_device_state()`：仅 `application/device_fsm` 更新
- `dev_ctx_set_safety_state()`：仅 `application/safety_supervisor` 更新
- `dev_ctx_set_wash_step()`：仅 `application/wash_orchestrator` 更新
- `dev_ctx_set_cloud_status()`：仅 `application/report_aggregator` 更新
- `dev_ctx_set_alarm_state()`：仅 `application/safety_supervisor` 更新

补充规则：

- `domain/` 层模块不得直接写 `dev_ctx`
- `adapters/` 层模块不得直接写 `dev_ctx`
- `domain/` 与 `adapters/` 只能通过事件总线发布状态变化
- `application/` 订阅相关事件后，再更新 `dev_ctx`

统一读取接口：

- `dev_ctx_snapshot()`

---

## 10. 报警与安全设计

### 10.1 `alarm_core` 的职责

- 维护报警配置表
- 处理防抖
- 处理激活/清除
- 处理等级判断
- 处理恢复策略

### 10.2 `m8_alarm_adapt` 的职责

- 将机型硬件输入映射为报警触发
- 将 VFD 故障、IO 输入、急停信号绑定到报警核心
- 执行机型特定的急停复位动作

### 10.3 `safety_fsm` 的职责

- 综合报警与互锁状态
- 输出 `OK / WARNING / LOCKOUT`
- 向应用层广播安全状态变化

### 10.4 设计原则

- 报警核心属于 `domain/safety/`
- 机型报警绑定属于 `adapters/machine/m8/`
- 安全规则优先级高于业务流程

---

## 11. 配置分层规则

### 11.1 编译期配置

位置：`config/machine/`

包含：

- 引脚号
- 串口名
- Modbus 地址
- IO 板数量

特点：

- 改动后需要重新编译

### 11.2 功能开关配置

位置：`config/features/`

包含：

- 顶刷是否安装
- 侧刷是否安装
- 某水路是否安装

### 11.3 配方配置

位置：`config/recipes/`

包含：

- 标准洗步骤表
- 快洗步骤表
- 每步动作参数与退出条件

### 11.4 部署期配置

位置：`config/deployment/`

包含：

- 设备 SN
- 站点 ID
- 服务器地址
- Topic 命名参数

特点：

- 出厂写入
- 运行期只读

### 11.5 线程配置

位置：`config/threading/`

包含：

- 线程优先级
- 栈大小
- 轮询周期
- 队列长度

---

## 12. 真机与仿真切换策略

### 12.1 切换原则

- `domain/`、`application/`、`service/` 层代码不因真机/仿真而变化
- 通过 `bootstrap/wiring` 在启动阶段注册不同 adapter 实现

### 12.2 真机实现

- 由 `adapters/hal/linux_hw/` 提供
- 内部依赖 `driver/`

### 12.3 仿真实现

- 由 `adapters/hal/sim_hw/` 提供
- 提供可观测、可注入、可脚本化的行为

---

## 13. 测试与演进建议

### 13.1 测试优先级

1. 先补 `domain/safety/` 与 `domain/process/` 的单元测试  
2. 再补 `application/` 的场景测试  
3. 最后补真机与仿真一致性验证

### 13.2 推荐首批场景测试

- 标准洗全流程
- 快洗全流程
- 洗车中急停
- VFD 故障触发停机
- 限位异常
- 云断线后恢复

### 13.3 演进边界

- 新增云平台：仅扩展 `adapters/cloud/`
- 新增机型：仅扩展 `adapters/machine/`
- 新增存储方式：仅扩展 `adapters/storage/`
- 新增洗车模式：优先扩展 `config/recipes/` 与 `domain/process/`

---

## 14. 一句话总结

本框架采用 **`core + domain + application + ports + adapters + service`** 的分层方式，将**业务规则**与**外部世界**彻底隔离，确保控制软件在**安全性、可测试性、可演进性、真机/仿真切换能力**之间取得平衡，作为新项目的推荐架构基线。

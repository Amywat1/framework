# 嵌入式控制软件框架设计说明

**版本**：v1.0  
**适用范围**：基于 Linux 的工业控制器（洗车机、自动化设备等）  
**编制依据**：M8 摩托车洗车机控制器重构实践

---

## 1. 设计目标

| 目标 | 说明 |
|------|------|
| **可移植** | 硬件相关代码集中在 `bsp/` 和 `driver/`，业务层不依赖具体硬件 |
| **可配置** | 机型差异通过配置文件（`config/`）和功能开关解决，不修改引擎代码 |
| **可测试** | PC 模拟层（`sim/`）替换硬件驱动，在开发机上验证业务逻辑 |
| **可维护** | 每层职责单一，依赖方向单向向下，禁止跨层引用 |

---

## 2. 整体分层架构

```
┌────────────────────────────────────────────────────────────────┐
│  app/                   业务层                                  │
│    app_service          设备顶层状态机 + 订单队列                 │
│    <app_xxx>            具体业务流程（洗车/加工/输送等）           │
├────────────────────────────────────────────────────────────────┤
│  component/             组件语义层                              │
│    comp_<硬件组合体>     将多个驱动封装为一个有语义的操作单元        │
├────────────────────────────────────────────────────────────────┤
│  service/               公共服务层                              │
│    svc_alarm            报警引擎（通用）                         │
│    svc_param            运行时参数管理                           │
├────────────────────────────────────────────────────────────────┤
│  bsp/                   板级适配层                              │
│    bsp_osal             硬件抽象（统一对外接口）                  │
│    bsp_alarm            机型特定报警逻辑（注入 svc_alarm）        │
│    bsp_init             硬件初始化序列                           │
├────────────────────────────────────────────────────────────────┤
│  driver/                驱动层（最稳定，几乎不改动）              │
│    drv_motor  drv_io  drv_water  drv_vfd  ...                  │
└────────────────────────────────────────────────────────────────┘
              ↕  BUILD_SIM=ON 时替换 driver/ + bsp/
┌────────────────────────────────────────────────────────────────┐
│  sim/                   PC 模拟层                               │
│    sim_driver           模拟 IO / 电机 / VFD                    │
│    sim_console          命令行交互（注入信号、下单、查状态）        │
└────────────────────────────────────────────────────────────────┘

config/
  alarm_config.h       报警码静态配置表
  machine_config.h     机型特性开关宏

common/
  log.h                日志宏（附文件名 + 行号）
  time_data.h          时间戳与计时工具
  kv_config.h          JSON 文件 KV 配置管理器
  list.h               侵入式单链表
```

### 依赖方向规则

```
app  →  component  →  service  →  bsp  →  driver
 └──────────────────→  service  ──────────────────┘

禁止：任何下层引用上层头文件
禁止：跨层直接调用（如 app 直接调 driver 函数）
允许：通过回调函数向上通知（事件驱动）
```

---

## 3. 各层详细说明

### 3.1 driver/ — 驱动层

**职责**：封装底层外设 SDK，提供最细粒度的硬件操作。

**原则**：
- 每个 `.c` 对应一类外设（电机、IO 板、水泵、VFD…）
- 只暴露操作函数和事件回调注册接口，不含业务判断
- 事件通过注册的 `callback` 向上通知，驱动层不关心处理逻辑

**典型接口风格**：
```c
// 操作接口
int drv_motor_run(int id, int vel);
int drv_motor_hold(int id);
int drv_motor_stop(int id);

// 状态查询
int drv_motor_get_pos(int id);
MotorState_t drv_motor_get_state(int id);

// 事件回调注册（驱动内部检测到异常时调用）
void drv_motor_register_event_cb(void (*cb)(int id, MotorEvent_t event, int param));
```

---

### 3.2 bsp/ — 板级适配层

**职责**：将 driver 层的多个外设组合成机型相关的逻辑单元，对上层提供统一的硬件接口（`bsp_osal`）。

#### 3.2.1 bsp_osal — 硬件抽象

- 唯一的硬件访问入口，上层所有硬件操作通过此层
- 定义机型全局枚举（`Type_DriverIndex_Enum`、`Type_SignalType_Enum` 等）
- 提供 IO 读写、电机驱动、VFD 控制、信号滤波等统一接口

**IO 子板寻址规则**：
```c
// 引脚编码：board_id * 100 + pin
#define BOARD_ID_RESOLUTION(id)  ((id) / 100)
#define PIN_ID_RESOLUTION(id)    ((id) % 100)

// 示例
#define BOARD2_INPUT_TOP_BRUSH_ERR   (201)   // 2号板，第1个输入
#define BOARD2_OUTPUT_SAFE_RELAY_RESET (202) // 2号板，第2个输出
```

#### 3.2.2 bsp_alarm — 机型报警适配

**职责**：包含所有机型特定的报警逻辑，通过回调注入通用报警引擎（`svc_alarm`）。

**包含内容**：
- `m8_signal_poll()`：每 10ms 读取 IO 信号，映射为报警触发（IO 信号 → 报警码）
- `osal_error_event_callback()`：驱动事件 → 报警码映射（动作超时、通讯失败、电流异常等）
- `m8_emc_reset()`：安全继电器复位动作序列
- `m8_vfd_read()`：读取 VFD 内部故障代码

```c
// bsp_alarm.c 的初始化入口
int bsp_alarm_init(void)
{
    svc_alarm_register_poll_fn(m8_signal_poll);
    svc_alarm_register_emc_reset_fn(m8_emc_reset);
    svc_alarm_register_vfd_read_fn(m8_vfd_read);
    osal_error_callback_regist(osal_error_event_callback);
    return 0;
}
```

#### 3.2.3 bsp_init — 初始化序列

**初始化顺序严格不可变**：
```c
cli_init()
  → io_init("can0")
  → sleep(2)              // 等待 IO 子板上电稳定
  → osal_init()           // 驱动层全部初始化
  → svc_alarm_init()      // 报警引擎（启动检测/上报线程）
  → bsp_alarm_init()      // 注册 M8 特定回调（必须在 osal_init 之后）
  → module_init()         // 业务模块初始化
  → service_init()        // 网络/MQTT 初始化
```

---

### 3.3 service/ — 公共服务层

#### 3.3.1 svc_alarm — 报警引擎

**设计核心**：数据与引擎分离。

```
config/alarm_config.h      静态配置（const，只读）
  ↓ 编译期注入
service/svc_alarm.c        运行时引擎（通用逻辑）
  ↑ 运行期注入
bsp/bsp_alarm.c            机型特定行为（回调）
```

**报警触发的两条路径**：

```
① 信号轮询路径（适用于 IO 电平类报警，需防抖）：
   bsp_alarm.c::poll_fn()
     → svc_alarm_set_raw_trigger(code, trigger, justNotice)
     → alarm_check_thread：每10ms累计 statusCnt
     → statusCnt >= trigger_ms/10 → isErr=true

② 事件驱动路径（适用于驱动层事件，驱动层已防抖）：
   driver 检测到异常 → osal_error_event_callback()
     → set_error_state(code, true/false, justNotice)   直接置位
```

**justNotice 降级机制**：

当硬件功能被配置为"未安装"时，对应报警不停机，仅上报：
```c
// 顶刷未配置时，顶刷相关报警自动降级为 NOTICE
svc_alarm_set_raw_trigger(8019,
    io_triggered,
    !flags->isConfigBrushTop);  // justNotice = 硬件未安装
```

**报警配置表格式**（`config/alarm_config.h`）：
```c
typedef struct {
    uint16_t     code;        // 报警代码
    int          trigger_ms;  // 触发延时（0=立即）
    AlarmLevel_t level;       // ERROR / WARNING / NOTICE
    int          recover_ms;  // 恢复延时
    AlarmRecover_t recover;   // AUTO | DRIVE | MANUAL（可组合）
    const char  *desc;        // 描述（日志用）
} AlarmEntry_t;

static const AlarmEntry_t g_alarm_table[] = {
    {8100, 500, ALARM_ERROR, 1000, ALARM_RECOVER_AUTO,  "紧急停止"},
    // 急停必须放第 0 行，ALARM_EMC_TABLE_IDX = 0
    ...
};
```

**三级报警处理逻辑**：

| 等级 | 行为 |
|------|------|
| `ALARM_ERROR` | 立即停机，上报，需手动归位后清除 |
| `ALARM_WARNING` | 上报，当前工作区车辆完成后停机 |
| `ALARM_NOTICE` | 仅上报，不停机 |

**状态标志结构**（由 app 层写入，bsp_alarm 读取）：
```c
typedef struct {
    bool isMachineClose;         // 机器关闭运营（停止轮询）
    bool isDevRunSta;            // 设备洗车中
    bool isDevStopSta;           // 设备停机
    bool isMqttConnected;        // 是否联网（触发上报）
    bool isSetElectricalReset;   // 安全继电器复位请求
    bool isLifterCheckedZero;    // 升降已校零

    // 硬件功能安装标志（决定报警是否降级为 NOTICE）
    bool isConfigBrushTop;
    bool isConfigBrushFrontLeft;
    // ...

    // 洗车区域工作状态（决定有条件报警是否检测）
    WashAreaStatus_t washArea;
} SvcAlarmFlags_t;
```

#### 3.3.2 svc_param — 参数管理

**职责**：统一封装运行时参数的读写，底层使用 `kv_config`（JSON 文件）。

```c
// 参数注册
svc_param_register("washMode",    PARAM_INT,  &g_wash_mode);
svc_param_register("brushSpeed",  PARAM_INT,  &g_brush_speed);

// 读写
svc_param_load_all();     // 从文件读取所有参数
svc_param_save("washMode"); // 持久化单个参数
```

---

### 3.4 component/ — 组件语义层

**职责**：将"多个驱动的组合"封装为一个有业务语义的操作单元，屏蔽底层驱动细节。

**设计原则**：
- 每个组件对应一类可独立操作的物理组合体
- 对外只暴露语义操作（`close`/`open`/`reset`），不暴露底层驱动 ID
- 组件内部处理驱动协调、超时保护、状态跟踪

#### comp_brush — 刷子组件

```c
// 一把刷子 = 旋转 VFD + 横移电机 + 电流跟随算法
typedef enum {
    BRUSH_TOP, BRUSH_FL, BRUSH_FR, BRUSH_BL, BRUSH_BR, BRUSH_FRONT_SKIRT,
    BRUSH_COUNT
} BrushId_t;

void brush_rotate(BrushId_t id, int vel);    // 旋转（vel>0 正转，0 停）
void brush_close(BrushId_t id);              // 合拢到工作位
void brush_open(BrushId_t id);               // 展开到安全位
void brush_reset(BrushId_t id);              // 归位（展开 + 停转）
bool brush_is_open(BrushId_t id);
int  brush_get_current(BrushId_t id);

// 电流跟随：由 wash_engine 每 30ms 调用
// target_current: 目标电流；press_high/low: 进退阈值
void brush_follow_tick(BrushId_t id, int target_current, int press_high, int press_low);
```

#### comp_conveyor — 输送带组件

```c
typedef enum { CONVEYOR_1, CONVEYOR_2, CONVEYOR_3, CONVEYOR_COUNT } ConveyorId_t;

void conveyor_run(ConveyorId_t id, int speed);  // 1=低速 2=中速 3=高速
void conveyor_stop(ConveyorId_t id);
int  conveyor_get_pos(ConveyorId_t id);         // 编码器脉冲累计值
void conveyor_reset_pos(ConveyorId_t id);       // 清零（归位后调用）
```

#### comp_gate / comp_dryer / comp_water

轻量封装，模式相同。对外只暴露开/关/状态查询，隐藏驱动索引和超时逻辑。

---

### 3.5 app/ — 业务层

#### 3.5.1 app_service — 顶层状态机

**职责**：设备级 FSM + 订单队列 + MQTT 属性模型。精简目标约 400 行。

```
设备 FSM 状态：
  INIT → IDLE ↔ RUN ↔ COMPLETE → STOP
                 ↓（报警触发）
               FAULT
```

**MQTT 属性模型**：
- 每 500ms 对比上次上报值，有变化时推送
- 云端下发 JSON → 解码为命令枚举 → 写入命令队列 → FSM 执行

#### 3.5.2 wash_engine — 位置驱动洗车引擎

**核心概念**：M8 洗车动作由车辆在输送带上的**编码器脉冲位置**触发，而非纯时序步骤。

```c
// 每辆车的运行上下文
typedef struct {
    bool     is_active;
    int      mode;           // 洗车模式
    int      car_head_pos;   // 车头当前编码器位置
    int      car_length;     // 车身长度（脉冲）
    uint32_t zone_entered;   // 已激活区域位图
    uint32_t zone_exited;    // 已退出区域位图
} CarContext_t;

#define WASH_CAR_SLOTS_MAX  3   // 最多 3 辆车同时在线

// 引擎接口
int  wash_engine_car_enter(int mode);            // 新车入场
void wash_engine_car_update(int slot, int pos);  // 每 30ms 更新位置
void wash_engine_car_exit(int slot);             // 车辆离场
void wash_engine_follow_tick(void);              // 刷子电流跟随（30ms 周期）
void wash_engine_emergency_stop(void);           // 全部停止
```

#### 3.5.3 wash_config — 区域动作配置表

```c
// 一条区域动作记录
typedef struct {
    int  comp_type;      // COMP_BRUSH / COMP_WATER / COMP_DRYER / COMP_GATE
    int  comp_id;        // 对应 BrushId_t / DryerId_t 等
    int  zone_start;     // 车头到达此脉冲偏移时触发 entry_cmd
    int  zone_end;       // 车尾离开此脉冲偏移时触发 exit_cmd（-1=不处理）
    int  entry_cmd;      // 进入命令（如 BRUSH_CMD_CLOSE）
    int  exit_cmd;       // 离开命令（如 BRUSH_CMD_OPEN）
    bool use_follow;     // 是否开启电流跟随
    int  follow_current; // 跟随目标电流
} WashZoneAction_t;

// 各模式在 wash_config.c 中定义，wash_engine.c 消费
extern const WashZoneAction_t g_zones_standard[];
extern const int              g_zones_standard_count;
```

---

### 3.6 sim/ — PC 模拟层

**目的**：在没有硬件的情况下验证 `wash_engine`、`app_service` 等业务逻辑。

**切换方式（CMakeLists.txt）**：
```cmake
option(BUILD_SIM "Build PC simulator" OFF)
if(BUILD_SIM)
    target_sources(${PROJECT_NAME} PRIVATE
        sim/sim_main.c sim/sim_driver.c sim/sim_console.c)
else()
    target_sources(${PROJECT_NAME} PRIVATE ${driver_src} ${bsp_src})
endif()
```

**sim_driver.c** 实现与 `driver/` 相同的函数签名：
- `drv_io_set_output()` → 更新内存映射，打印日志
- `osal_drive_hold()` → 启动软件定时器，N ms 后自动置对应到位信号
- `osal_is_signal_filter_trigger()` → 查询 sim 内部信号状态表

**sim_console 命令**：
```
inject SIGNAL_XXX 1     注入信号（模拟 IO 触发）
order standard          下一个标准洗车单
fault 6001              注入指定报警码
status                  查看设备状态、报警、车辆位置
```

---

## 4. 目录结构规范

```
项目根/
├── app/
│   ├── app_main.cpp         入口（框架回调 app_main()，非 main()）
│   ├── app_service.c/h      顶层 FSM + 订单队列
│   ├── wash_engine.c/h      位置驱动洗车引擎
│   └── wash_config.c/h      机型洗车区域动作表
├── component/
│   ├── comp_brush.c/h
│   ├── comp_conveyor.c/h
│   ├── comp_gate.c/h
│   ├── comp_dryer.c/h
│   └── comp_water.c/h
├── service/
│   ├── svc_alarm.c/h        通用报警引擎
│   └── svc_param.c/h        参数管理
├── bsp/
│   ├── bsp_osal.c/h         硬件抽象接口
│   ├── bsp_alarm.c/h        机型报警适配
│   ├── bsp_init.c/h         初始化序列
│   └── bsp_snack.cpp/h      第三方 SDK C++ 封装
├── driver/
│   ├── drv_motor.c/h
│   ├── drv_io.c/h
│   ├── drv_water.c/h
│   └── ...
├── config/
│   ├── alarm_config.h       报警码静态配置（const 表，不可执行）
│   └── machine_config.h     机型特性开关宏
├── common/
│   ├── log.h                LOG_ERROR/WARN/INFO/DEBUG
│   ├── time_data.h          get_time_stamp() / get_diff_ms()
│   ├── kv_config.h          JSON KV 配置管理
│   └── list.h               侵入式单链表
├── sim/
│   ├── sim_main.c
│   ├── sim_driver.c/h
│   └── sim_console.c/h
└── doc/
```

---

## 5. 命名规范（参照 QRS-0002-2026）

| 类型 | 规则 | 示例 |
|------|------|------|
| 文件名 | 全小写 + 下划线 | `bsp_alarm.c` |
| 函数名 | `模块_动词_对象` | `brush_follow_tick()` |
| 全局变量 | `g_` 前缀 | `g_alarm_table` |
| 文件级静态变量 | `s_` 前缀 | `s_poll_fn` |
| 宏/枚举值 | 全大写 + 下划线 | `ALARM_RECOVER_AUTO` |
| 结构体类型 | `大驼峰_t` | `AlarmEntry_t` |
| 枚举类型 | `大驼峰_t` | `AlarmLevel_t` |

---

## 6. 关键设计模式

### 6.1 回调注入模式（依赖倒置）

通用引擎不依赖具体机型代码，机型特定逻辑通过回调注入：

```c
// service/svc_alarm.h — 定义回调类型
typedef void (*AlarmSignalPollFn)(void);
typedef void (*AlarmEmcResetFn)(void);

// service/svc_alarm.c — 存储回调
static AlarmSignalPollFn s_poll_fn = NULL;
void svc_alarm_register_poll_fn(AlarmSignalPollFn fn) { s_poll_fn = fn; }

// bsp/bsp_alarm.c — 注入实现
static void m8_signal_poll(void) { /* M8 特定 IO 读取 */ }
bsp_alarm_init() { svc_alarm_register_poll_fn(m8_signal_poll); }
```

### 6.2 数据与引擎分离模式

静态配置（const 只读表）放 `config/`，运行时引擎放 `service/`，机型实现放 `bsp/`：

```
config/alarm_config.h    ← 只有数据，没有逻辑
         ↓ 编译期
service/svc_alarm.c      ← 只有逻辑，没有机型硬编码
         ↑ 运行期
bsp/bsp_alarm.c          ← 只有机型代码
```

### 6.3 两条报警路径模式

| 路径 | 适用场景 | 是否防抖 |
|------|----------|----------|
| `svc_alarm_set_raw_trigger()` | IO 电平信号（周期采样） | ✅ 由引擎防抖 |
| `set_error_state()` | 驱动事件（动作超时、通讯失败） | ❌ 直接置位 |

### 6.4 向后兼容垫片模式

重构时保留旧头文件作为垫片，新旧代码可并存过渡：

```c
// service/svc_fault.h（旧头文件 → 改为垫片）
#include "svc_alarm.h"                         // 包含新头文件

typedef SvcAlarmFlags_t  Type_ErrStateFlag_Def; // 类型别名
typedef WashAreaStatus_t Type_AreaWorkingInfo_Def;

// API 映射（inline，零开销）
static inline int error_state_init(void) { return svc_alarm_init(); }
static inline Type_ErrStateFlag_Def *err_need_flag_handle(void) {
    return svc_alarm_flags();
}
```

旧文件 `svc_fault.c` 通过 CMake 过滤排除：
```cmake
list(FILTER src EXCLUDE REGEX ".*/svc_fault\\.c$")
```

---

## 7. 新项目适配指南

### Step 1：移植 driver/ 和 bsp/

1. 根据硬件修改 `driver/` 各外设驱动
2. 修改 `bsp_osal.h` 中的枚举（`Type_DriverIndex_Enum`、`Type_SignalType_Enum` 等）
3. 修改 `bsp_osal.c` 中的 IO 引脚映射宏（`BOARD_X_INPUT_XXX`）

### Step 2：填写 config/alarm_config.h

按以下格式添加所有报警码，急停放第 0 行：
```c
// code  trigger_ms  level         recover_ms  recover           desc
{8100,   500,  ALARM_ERROR,  1000, ALARM_RECOVER_AUTO,  "紧急停止"},
{XXXX,   0,    ALARM_WARNING, 0,   ALARM_RECOVER_DRIVE|ALARM_RECOVER_MANUAL, "描述"},
```

### Step 3：实现 bsp/bsp_alarm.c

1. 实现 `signal_poll()`：遍历所有 IO 信号类报警，调 `svc_alarm_set_raw_trigger()`
2. 实现 `event_callback()`：处理各模块的驱动事件，调 `set_error_state()`
3. 实现 `emc_reset()`：安全继电器或急停复位动作
4. 在 `bsp_alarm_init()` 中注册所有回调

### Step 4：实现 component/

根据设备的物理硬件组合体逐个实现，每个组件独立文件，对外只暴露语义接口。

### Step 5：实现 app/ 业务逻辑

- `app_service.c`：实现设备 FSM 和订单管理
- 具体业务文件：消费 `component/` 接口，专注业务流程描述

### Step 6：实现 sim/ 并验证

实现 `sim_driver.c` 的信号模拟，在 PC 上用 `sim_console` 进行功能验证，再部署到硬件。

---

## 8. 常见反模式（避免）

| 反模式 | 正确做法 |
|--------|----------|
| app 层直接调 `drv_motor_run()` | 通过 `comp_conveyor` 或 `bsp_osal` 调用 |
| 报警码硬编码散落在业务代码中 | 统一放 `config/alarm_config.h` |
| 引擎代码中出现具体 IO 引脚号 | 移入 `bsp/bsp_alarm.c` 的回调 |
| 在 `driver/` 中写业务判断逻辑 | 业务判断放 `component/` 或 `app/` |
| 超时值硬编码在流程代码中 | 通过 `get_error_overtime(code)` 读配置表 |
| 功能未安装时注释掉报警代码 | 使用 `justNotice=true` 降级，不修改代码 |
| 所有配置读写散落在各业务文件 | 统一通过 `svc_param` 管理 |

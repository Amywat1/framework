# M8 代码问题修复文档

**版本**：v1.0  
**日期**：2026-04-14  
**分支**：feature/refactor-framework  

---

## 问题汇总

| 编号 | 类型 | 文件 | 严重度 | 状态 |
|------|------|------|--------|------|
| BUG-01 | Bug | `domain/safety/alarm_core.c` | 高 | 待修 |
| BUG-02 | Bug | `domain/device/gantry.c` | 高 | 待修 |
| BUG-03 | Bug | `adapters/machine/m8/m8_alarm_adapt.c` | 高 | 待修 |
| BUG-04 | Bug | `adapters/machine/m8/m8_alarm_adapt.c` | 中 | 待修 |
| BUG-05 | Bug | `config/machine/m8_signal_table.h` | 中 | 待修 |
| ROB-01 | 鲁棒性 | `application/orchestrators/wash_orchestrator.c` | 中 | 待修 |
| ROB-02 | 鲁棒性 | `domain/process/step_engine.c` | 中 | 待修 |
| ROB-03 | 鲁棒性 | `domain/device/motor.c` | 中 | 待修 |
| DES-01 | 设计 | `adapters/hal/linux_hw/hal_sensor_linux.c` | 低 | 待修 |
| DES-02 | 设计 | `adapters/hal/linux_hw/hal_motion_linux.c` | 低 | 待修 |
| NEW-01 | Bug | `adapters/machine/m8/m8_boot_profile.c` | 高 | 待修 |
| NEW-02 | Bug | `adapters/hal/linux_hw/hal_sensor_linux.c` | 高 | 待修 |
| NEW-03 | Bug | `driver/drv_io.c` | 高 | 待修 |
| NEW-04 | 设计 | `adapters/hal/linux_hw/hal_motion_linux.c` | 中 | 待修 |

---

## BUG-01：`alarm_core_tick_ms` 锁外读取 `just_notice`

**文件**：`domain/safety/alarm_core.c`，第 244–245 行

**问题描述**

`alarm_core_tick_ms()` 在锁外发布事件时调用 `get_effective_level(idx)`，该函数读取 `s_rt[idx].just_notice`。而 `s_rt` 在锁释放（第 239 行 `pthread_mutex_unlock`）之后才被读取，此时 `s_alarm_adapt_poll()` 或其他线程可能正在通过 `alarm_core_set_raw_trigger()` 写入 `just_notice`，构成数据竞争，属于 C11 未定义行为。

```c
/* alarm_core.c 第 239–248 行（当前）*/
pthread_mutex_unlock(&s_mutex);

for (i = 0; i < triggered_count; i++)
{
    int idx = find_idx(triggered_codes[i]);
    alarm_level_t lvl = (idx >= 0) ? get_effective_level(idx) : ...;  // ← 锁外读 just_notice
    (void)event_publish(EVT_ALARM_TRIGGERED, ...);
}
```

**修复方案**

在持锁期间捕获 `level`，与 `code` 一起存入临时数组，锁外只做发布。

```c
typedef struct { uint16_t code; alarm_level_t lvl; } pending_alarm_t;
pending_alarm_t triggered[ALARM_TABLE_SIZE];
int triggered_count = 0;

pthread_mutex_lock(&s_mutex);
for (i = 0; i < ALARM_TABLE_SIZE; i++)
{
    /* ...防抖计时逻辑不变... */
    if (/* 触发条件 */)
    {
        triggered[triggered_count].code = cfg->code;
        triggered[triggered_count].lvl  = get_effective_level(i);  // ← 锁内读取
        triggered_count++;
    }
}
pthread_mutex_unlock(&s_mutex);

for (i = 0; i < triggered_count; i++)
{
    (void)event_publish(EVT_ALARM_TRIGGERED, triggered[i].code);
    LOG_WARN("alarm_core: TRIGGERED code=%u level=%d",
             triggered[i].code, triggered[i].lvl);
}
```

---

## BUG-02：`gantry.c` 解引用未经检查的 HAL 指针

**文件**：`domain/device/gantry.c`，`gantry_at_fwd_limit()` 和 `gantry_at_rev_limit()`

**问题描述**

两个函数直接对 `hal_sensor_get_ops()` 返回值调用成员函数，未检查指针是否为 NULL。在仿真构建或端口未注册时，`hal_sensor_get_ops()` 返回 NULL，导致空指针解引用崩溃。

```c
/* 当前代码（问题示例）*/
bool gantry_at_fwd_limit(void)
{
    return hal_sensor_get_ops()->gantry_at_fwd_limit();  // ← ops 可能为 NULL
}
```

**修复方案**

```c
bool gantry_at_fwd_limit(void)
{
    const hal_sensor_ops_t *ops = hal_sensor_get_ops();
    if (ops == NULL) { return false; }
    return ops->gantry_at_fwd_limit();
}

bool gantry_at_rev_limit(void)
{
    const hal_sensor_ops_t *ops = hal_sensor_get_ops();
    if (ops == NULL) { return false; }
    return ops->gantry_at_rev_limit();
}
```

---

## BUG-03：`m8_emc_reset` 未检查 HAL 指针

**文件**：`adapters/machine/m8/m8_alarm_adapt.c`，`m8_emc_reset()`

**问题描述**

`m8_emc_reset()` 直接调用 `motion->gantry_stop()` 等成员函数，但 `hal_motion_get_ops()` 和 `hal_indicator_get_ops()` 均可能在端口未注册时返回 NULL。急停复位序列在故障最严重的场景下执行，此处 NULL 解引用会导致二次崩溃。

```c
/* 当前代码（问题位置）*/
static void m8_emc_reset(void)
{
    const hal_motion_ops_t    *motion    = hal_motion_get_ops();
    const hal_indicator_ops_t *indicator = hal_indicator_get_ops();
    // motion 和 indicator 直接调用，无 NULL 检查
    (void)motion->gantry_stop();
    ...
}
```

**修复方案**

```c
static void m8_emc_reset(void)
{
    const hal_motion_ops_t    *motion    = hal_motion_get_ops();
    const hal_indicator_ops_t *indicator = hal_indicator_get_ops();

    LOG_INFO("m8_alarm_adapt: EMC reset sequence start");

    if (motion != NULL)
    {
        (void)motion->gantry_stop();
        (void)motion->brush_stop();
        usleep(200U * 1000U);
        (void)motion->gantry_fault_reset();
        (void)motion->brush_fault_reset();
    }

    (void)water_all_off();

    if (indicator != NULL)
    {
        (void)indicator->rod_close();
        (void)indicator->entry_light_set(HAL_LIGHT_RED);
    }

    LOG_INFO("m8_alarm_adapt: EMC reset sequence done");
}
```

---

## BUG-04：`m8_signal_poll` 覆盖 `m8_signal_filter_tick` 设置的防抖状态

**文件**：`adapters/machine/m8/m8_alarm_adapt.c`，`m8_signal_poll()` 非仿真路径

**问题描述**

真机构建中，`m8_signal_filter_tick()` 通过计数防抖后调用 `alarm_core_set_raw_trigger(ALARM_CODE_GANTRY_FWD_LIM, ...)` 设置 `raw_triggered`。而 `m8_signal_poll()` 在同一轮询周期内又调用 `alarm_core_set_state(ALARM_CODE_GANTRY_FWD_LIM, fwd && rev, false)`，`alarm_core_set_state()` 会覆写 `raw_triggered`，导致 `alarm_core_tick_ms()` 防抖计时归零，两路逻辑相互干扰。

```c
/* 当前代码（真机路径）*/
bool fwd = sensor->gantry_at_fwd_limit();
bool rev = sensor->gantry_at_rev_limit();
alarm_core_set_state(ALARM_CODE_GANTRY_FWD_LIM, (fwd && rev), false);  // ← 覆写 filter 结果
alarm_core_set_state(ALARM_CODE_GANTRY_REV_LIM, (fwd && rev), false);
```

**修复方案**

真机路径的"双限位同时触发"组合报警应改用 `alarm_core_set_raw_trigger()`，与 filter 使用同一条路径，由 `alarm_core_tick_ms()` 统一做防抖处理：

```c
/* 修复后 */
bool fwd = sensor->gantry_at_fwd_limit();
bool rev = sensor->gantry_at_rev_limit();
bool both_lim = fwd && rev;
alarm_core_set_raw_trigger(ALARM_CODE_GANTRY_FWD_LIM, both_lim, false);
alarm_core_set_raw_trigger(ALARM_CODE_GANTRY_REV_LIM, both_lim, false);
```

---

## BUG-05：`m8_signal_table.h` 限位信号 `alarm_code` 配置冲突

**文件**：`config/machine/m8_signal_table.h`

**问题描述**

`m8_signal_table` 中 `M8_SIG_GANTRY_FWD_LIM` 和 `M8_SIG_GANTRY_REV_LIM` 条目的 `alarm_code` 字段被设置为非零值（`ALARM_CODE_GANTRY_FWD_LIM` / `ALARM_CODE_GANTRY_REV_LIM`）。`m8_signal_filter_tick()` 对每个 `alarm_code != 0` 的信号都会在状态变化时调用 `alarm_core_set_raw_trigger()`，而 BUG-04 描述的 `m8_signal_poll()` 真机路径也处理了同一报警码。两路同时写入同一报警，造成逻辑重复且行为不可预测。

**修复方案**

龙门限位报警由 `m8_signal_poll()` 的组合逻辑（双限位同时触发）负责，`m8_signal_table` 中这两个信号的 `alarm_code` 应设为 `0`，避免 filter 路径单独激活：

```c
/* m8_signal_table.h（修复后示例）*/
{ M8_SIG_GANTRY_FWD_LIM, M8_DI_GANTRY_FWD_LIM, true,  3, 3,
  EVT_HW_GANTRY_FWD_LIM_ACT, EVT_HW_GANTRY_FWD_LIM_REL, 0 },  // ← alarm_code=0
{ M8_SIG_GANTRY_REV_LIM, M8_DI_GANTRY_REV_LIM, true,  3, 3,
  EVT_HW_GANTRY_REV_LIM_ACT, EVT_HW_GANTRY_REV_LIM_REL, 0 },  // ← alarm_code=0
```

---

## ROB-01：`wash_orchestrator` 信号量操作未处理 EINTR

**文件**：`application/orchestrators/wash_orchestrator.c`，约第 49 行和第 177 行

**问题描述**

`sem_wait()` 在被信号中断时返回 `-1` 并设置 `errno = EINTR`，属于正常情况而非错误。当前代码对此未做处理，导致洗车流程在意外信号到来时直接跳过等待逻辑，引发状态机进入错误状态或序列缺步。`sem_post()` 返回值也未检查。

**修复方案**

```c
/* sem_wait 使用 EINTR 重试 */
int ret;
do {
    ret = sem_wait(&s_start_sem);
} while (ret == -1 && errno == EINTR);

if (ret != 0)
{
    LOG_ERROR("wash_orchestrator: sem_wait failed errno=%d", errno);
    return;
}

/* sem_post 检查返回值 */
if (sem_post(&s_start_sem) != 0)
{
    LOG_ERROR("wash_orchestrator: sem_post failed errno=%d", errno);
}
```

---

## ROB-02：`step_engine` 静默忽略水路控制错误

**文件**：`domain/process/step_engine.c`，`apply_step()` 函数

**问题描述**

`apply_step()` 中所有 `water_prewash_on/off()`、`water_brush_on/off()`、`water_highpres_on/off()` 调用均使用 `(void)` 强制丢弃返回值。HAL 层写 IO 失败时（CAN 子板离线、驱动返回错误），水路状态与实际硬件状态不同步，且无任何报告，导致洗车在无水状态下继续执行。

**修复方案**

```c
/* 示例：prewash_on 步骤 */
sw_err_t ret = water_prewash_on();
if (ret != SW_OK)
{
    LOG_ERROR("step_engine: water_prewash_on failed ret=%d, aborting step", (int)ret);
    (void)event_publish(EVT_WASH_STEP_FAILED, (uint32_t)ret);
    return ret;
}
```

对所有 `water_*_on/off` 调用统一改为检查返回值，失败时发布 `EVT_WASH_STEP_FAILED` 并中止当前步骤。

---

## ROB-03：`motor.c` 编码器计数器回绕未处理 + 魔法数字

**文件**：`domain/device/motor.c`

**问题描述**

1. **计数器回绕**：编码器脉冲计数读值为无符号硬件计数器，`diff = read_value - s_last_value` 在计数器回绕（0xFFFFFFFF → 0）时得到巨大正数，导致位置计算错误甚至电机过冲。

2. **魔法数字**：惯性保护窗口使用裸常量 `1000U`（毫秒），无任何注释说明，含义不透明，后续维护容易误改。

**修复方案**

```c
/* 1. 回绕处理（以32位计数器为例）*/
#define ENCODER_COUNTER_MAX  0xFFFFFFFFU

static int32_t calc_encoder_diff(uint32_t curr, uint32_t prev)
{
    if (curr >= prev)
    {
        return (int32_t)(curr - prev);
    }
    /* 回绕 */
    return (int32_t)((ENCODER_COUNTER_MAX - prev) + curr + 1U);
}

/* 2. 替换魔法数字 */
/* motor.c 顶部或 m8_machine_config.h */
#define CFG_MOTOR_INERTIA_WINDOW_MS    1000U   /* 停止后编码器惯性检测窗口（ms）*/
```

---

## DES-01：`hal_sensor_linux` 直接调用驱动层绕过端口接口

**文件**：`adapters/hal/linux_hw/hal_sensor_linux.c`，第 99 行

**问题描述**

`hal_sensor_linux_register()` 直接调用 `drv_io_register_board_error_cb(io_board_status_cb)`，跳过了 `hal_io_port` 接口。这违反六边形架构的层级规则（adapters 应仅通过 ports 访问驱动），且和 `hal_io_linux` 使用相同的单槽回调，存在相互覆盖风险（见 NEW-02）。

**修复方案**

IO 子板在线/离线事件的发布职责统一归属于 `hal_io_linux` 适配器。`hal_sensor_linux` 删除直接注册，改为订阅 `EVT_HW_IO_OFFLINE` / `EVT_HW_IO_ONLINE` 事件，或由 wiring 层统一安排回调注册顺序。

---

## DES-02：`hal_motion_linux` 升降限位使用裸驱动读取

**文件**：`adapters/hal/linux_hw/hal_motion_linux.c`，第 111 行和第 141 行

**问题描述**

`m8_lift_up_start()` 和 `m8_lift_down_start()` 在步进批次循环内通过 `drv_io_di_read(M8_DI_LIFT_UP_LIM)` / `drv_io_di_read(M8_DI_LIFT_DOWN_LIM)` 直接读取原始 IO，绕过 `m8_signal_filter` 的防抖逻辑。单次噪声脉冲即可提前终止升降动作，同时这两处代码直接依赖 driver 层，违反分层规则。

**修复方案**

```c
/* 替换原始读取为经过防抖的 signal 状态 */
/* m8_lift_up_start 第111行 */
if (m8_signal_is_active(M8_SIG_LIFT_UP_LIM))
{
    LOG_INFO("hal_motion: lift_up reached top limit");
    break;
}

/* m8_lift_down_start 第141行 */
if (m8_signal_is_active(M8_SIG_LIFT_DOWN_LIM))
{
    LOG_INFO("hal_motion: lift_down reached bottom limit");
    break;
}
```

---

## NEW-01：启动安全态未落硬件

**文件**：`adapters/machine/m8/m8_boot_profile.c`

**问题描述**

`m8_boot_profile_init()` 的执行顺序：
1. `m8_wait_io_ready()` 通过 SDK 接口 `io_online_get()` 确认 IO 子板就绪
2. `m8_assert_safe_outputs()` 调用 `drv_io_flush_outputs_now()` 写安全状态

但 `drv_io_flush_outputs_now()` 的实现只对 `s_board_online[i] == true` 的子板执行写操作：

```c
/* drv_io.c drv_io_flush_outputs_now() */
online[i] = s_board_online[i];
// ...
/* 只对在线子板执行写操作，避免 CAN 超时阻塞 */
```

而 `s_board_online` 仅由 `drv_io_poll_loop()` 在收到子板响应后置 `true`。`m8_boot_profile_init()` 在 bootstrap 第 8 步执行，此时调度器（第 24 步启动）和 `io_rw_thread` 均未运行，`s_board_online` 全为 `false`。结果：`drv_io_flush_outputs_now()` 跳过所有子板，安全状态从未写到硬件。

**修复方案**

在确认子板在线后（`io_online_get()` 返回正值），同步更新 `s_board_online`，使 `flush_outputs_now()` 能正常写出。推荐在 `drv_io.c` 增加一个启动专用函数：

```c
/* drv_io.h 新增 */
/**
 * @brief  启动阶段同步标记子板在线（io_rw_thread 启动前调用）
 * @param  board_id  子板编号（1-based）
 */
void drv_io_mark_board_online(int board_id);
```

```c
/* drv_io.c 实现 */
void drv_io_mark_board_online(int board_id)
{
    if ((board_id >= 1) && (board_id < (int)IO_BOARD_MAX))
    {
        pthread_mutex_lock(&s_output_mutex);
        s_board_online[board_id] = true;
        pthread_mutex_unlock(&s_output_mutex);
    }
}
```

```c
/* m8_boot_profile.c m8_wait_io_ready() 修改 */
if (all_online)
{
    /* 同步更新 drv_io 在线状态，确保 flush_outputs_now 能写出 */
    for (int j = 1; j <= M8_IO_BOARD_COUNT; j++)
    {
        drv_io_mark_board_online(j);
    }
    LOG_INFO("m8_boot: all IO boards online in %u ms", elapsed_ms);
    return SW_OK;
}
```

---

## NEW-02：IO 子板错误回调单槽被多处覆盖

**文件**：`adapters/hal/linux_hw/hal_sensor_linux.c`（第 99 行）、`adapters/hal/linux_hw/hal_io_linux.c`、`driver/drv_io.c`（第 69 行）

**问题描述**

`drv_io.c` 只维护一个全局函数指针 `s_board_error_cb`：

```c
static void (*s_board_error_cb)(int board_id, bool offline) = NULL;
```

`drv_io_register_board_error_cb()` 每次调用都直接覆盖前一个注册值，没有链式机制。
`hal_sensor_linux_register()` 和 `hal_io_linux` 各自调用一次注册，后注册的覆盖先注册的，先注册方永远收不到回调，导致 IO 离线/恢复事件丢失。

**修复方案**

将 IO 子板状态事件的发布职责统一收归 `hal_io_linux`：

1. 删除 `hal_sensor_linux.c` 第 99 行的直接注册调用 `drv_io_register_board_error_cb(io_board_status_cb)`
2. `hal_io_linux` 的回调负责向 `event_bus` 发布 `EVT_HW_IO_OFFLINE` / `EVT_HW_IO_ONLINE`
3. 需要感知 IO 在线状态的其他模块改为订阅这些事件，而非直接持有回调槽

如需多个接收方，可在 `drv_io.c` 将单槽改为固定长度链式数组：

```c
#define DRV_IO_MAX_ERROR_CBS    4
static void (*s_board_error_cbs[DRV_IO_MAX_ERROR_CBS])(int, bool);
static int s_board_error_cb_count = 0;

void drv_io_register_board_error_cb(void (*cb)(int, bool))
{
    if (s_board_error_cb_count < DRV_IO_MAX_ERROR_CBS)
    {
        s_board_error_cbs[s_board_error_cb_count++] = cb;
    }
}
```

---

## NEW-03：`s_input_buf` / `s_board_online` 多线程数据竞争

**文件**：`driver/drv_io.c`

**问题描述**

`drv_io.c` 中存在两处 C11 数据竞争（未定义行为）：

**竞争点1：`s_input_buf`**

| 写方 | 读方 |
|------|------|
| `io_rw_thread` → `drv_io_poll_loop()` 第 308 行：`s_input_buf[i] = read_val` | `wash_worker_thread` → `drv_io_di_read()` 通过 `hal_motion_linux.c` 升降限位检测 |

两个线程对同一 `volatile unsigned int` 并发读写，无任何同步原语保护。`volatile` 在 C11 中不提供线程安全保证，编译器和 CPU 可以重排操作。

**竞争点2：`s_board_online`**

`drv_io_poll_loop()` 在不持 `s_output_mutex` 的情况下写 `s_board_online[i] = true/false`（第 269/292 行），而 `drv_io_flush_outputs_now()` 在持锁时读取 `s_board_online[i]`（第 456 行），`drv_io_is_board_online()` 也在无锁情况下读取（第 504 行）。

**修复方案**

将 `s_input_buf` 和 `s_board_online` 改为 `_Atomic` 类型（C11），或将所有读写均纳入 `s_output_mutex` 保护范围：

```c
/* 方案A：使用 C11 原子类型 */
#include <stdatomic.h>
static _Atomic unsigned int s_input_buf[IO_BOARD_MAX];
static _Atomic bool         s_board_online[IO_BOARD_MAX];

/* 读写改为原子操作 */
atomic_store_explicit(&s_input_buf[i], read_val, memory_order_release);
unsigned int val = atomic_load_explicit(&s_input_buf[i], memory_order_acquire);

atomic_store_explicit(&s_board_online[i], true, memory_order_release);
bool online = atomic_load_explicit(&s_board_online[i], memory_order_acquire);
```

```c
/* 方案B：扩展 s_output_mutex 保护范围（简单但粒度粗）*/
/* drv_io_poll_loop 中写 s_board_online 前加锁 */
pthread_mutex_lock(&s_output_mutex);
s_board_online[i] = true;
pthread_mutex_unlock(&s_output_mutex);
```

---

## NEW-04：升降限位读取路径不一致

**文件**：`adapters/hal/linux_hw/hal_motion_linux.c`（第 111、141 行）vs `adapters/hal/linux_hw/hal_sensor_linux.c`（第 59–60 行）

**问题描述**

同一组升降限位 IO 信号存在两种不同的读取路径：

| 位置 | 函数 | 读取方式 | 防抖 |
|------|------|----------|------|
| `hal_sensor_linux.c:59` | `m8_lift_at_top()` | `m8_signal_is_active(M8_SIG_LIFT_UP_LIM)` | 3-count 防抖 |
| `hal_sensor_linux.c:60` | `m8_lift_at_bottom()` | `m8_signal_is_active(M8_SIG_LIFT_DOWN_LIM)` | 3-count 防抖 |
| `hal_motion_linux.c:111` | 升降运动批次循环 | `drv_io_di_read(M8_DI_LIFT_UP_LIM)` | 无防抖（原始值）|
| `hal_motion_linux.c:141` | 升降运动批次循环 | `drv_io_di_read(M8_DI_LIFT_DOWN_LIM)` | 无防抖（原始值）|

步进电机批次循环中的原始读取会因单次噪声脉冲（信号毛刺）误判限位到达，提前中止运动，导致顶刷升降位置偏差。同时直接调用 `drv_io` 违反 adapters 层不直接依赖 driver 层的分层规则。

**修复方案**

将 `hal_motion_linux.c` 升降批次循环中的原始 IO 读取替换为经过防抖的 signal 状态：

```c
/* hal_motion_linux.c m8_lift_up_start() 第111行 */
/* 修改前 */
if (drv_io_di_read(M8_DI_LIFT_UP_LIM))

/* 修改后 */
if (m8_signal_is_active(M8_SIG_LIFT_UP_LIM))

/* hal_motion_linux.c m8_lift_down_start() 第141行 */
/* 修改前 */
if (drv_io_di_read(M8_DI_LIFT_DOWN_LIM))

/* 修改后 */
if (m8_signal_is_active(M8_SIG_LIFT_DOWN_LIM))
```

需要在 `hal_motion_linux.c` 增加头文件引用：

```c
#include "adapters/machine/m8/m8_signal_filter.h"
```

并删除对 `driver/drv_io.h` 的直接依赖（如该文件不再有其他 drv_io 调用）。

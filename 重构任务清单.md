# M8 控制软件重构任务清单

**基准架构**：见《软件框架设计说明.md》v2.0  
**重构策略**：依赖顺序从底向上，分阶段推进，每阶段完成后可独立验证

---

## 旧文件归宿速查

| 旧文件 | 去向 |
|--------|------|
| `app/app_fsm.c` | → `application/orchestrators/device_fsm.c` |
| `app/app_wash_engine.c` | → `application/orchestrators/wash_orchestrator.c` + `domain/process/step_engine.c` |
| `app/app_wash_steps.c` | → `domain/process/recipe.c` + `config/recipes/` |
| `app/app_cloud.c` | → `adapters/cloud/aliyun/` + `adapters/ui/mqtt_cmd/` |
| `app/app_main.cpp` | → `core/bootstrap/bootstrap.c` + `core/scheduler/` |
| `component/comp_brush.c` | → `domain/device/brush.c` |
| `component/comp_gantry.c` | → `domain/device/gantry.c` |
| `component/comp_top_lift.c` | → `domain/device/top_lift.c` |
| `component/comp_water.c` | → `domain/device/water.c` |
| `bsp/bsp_hal.c` | → `adapters/hal/linux_hw/hal_*_linux.c`（分拆） |
| `bsp/bsp_alarm.c` | → `adapters/machine/m8/m8_alarm_adapt.c` |
| `bsp/bsp_init.c` | → `core/bootstrap/bootstrap.c` + `adapters/machine/m8/m8_boot_profile.c` |
| `service/svc_alarm.c` | → `domain/safety/alarm_core.c`（引擎部分） |
| `service/svc_param.c` | → `service/svc_param/svc_param.c`（保留，轻改） |
| `config/alarm_config.h` | → `domain/safety/alarm_core.h` 内嵌 + `domain/model/alarm_code.h` |
| `config/machine_config.h` | → `config/machine/m8_machine_config.h` + `config/features/m8_features.h` |
| `driver/drv_io.c` | → `driver/drv_io.c`（保留，串口互斥补丁） |
| `driver/drv_vfd.c` | → `driver/drv_vfd.c`（保留，增加 bus mutex） |
| `driver/drv_stepper.c` | → `driver/drv_stepper.c`（nanosleep 精度提升，保持同步接口）+ `core/scheduler` 注册 `stepper_thread` |
| `common/*` | → `common/`（保留，补充工具文件） |
| `middleware/snack_wrapper.cpp` | → 拆分：MQTT/云端初始化 → `adapters/cloud/aliyun/`，CLI 注册 → `adapters/ui/cli/`，snack 框架入口薄封装留在 `core/bootstrap/bootstrap.c` |

---

## 阶段零：准备工作

- [ ] **Z-1** 创建 `feature/refactor-framework` 分支，在新分支上操作
- [ ] **Z-2** 按《软件框架设计说明》目录结构，创建所有空目录和 `.gitkeep` 占位文件

```
src/
  core/event_bus/  core/scheduler/  core/runtime/  core/bootstrap/
  domain/model/  domain/device/  domain/process/  domain/safety/
  application/orchestrators/  application/usecases/
  ports/hal/  ports/cloud/  ports/storage/
  adapters/hal/linux_hw/  adapters/hal/sim_hw/
  adapters/cloud/aliyun/  adapters/storage/json/
  adapters/ui/cli/  adapters/ui/mqtt_cmd/
  adapters/machine/m8/
  service/dev_ctx/  service/svc_param/
  config/machine/  config/features/  config/recipes/
  config/deployment/  config/threading/
  driver/  common/
  tests/unit/  tests/integration/  tests/scenario/
  tools/simulator/  tools/replay/
```

- [ ] **Z-3** 更新 `CMakeLists.txt` 骨架，按新目录组织 target（内容先留空，后续阶段补充）
- [ ] **Z-4** 更新 `.clang-format`（路径变更，规则不变）

---

## 阶段一：类型定义层（纯 .h，无实现）

> 依赖：无  
> 验证方式：编译通过，无 warning

### 1.1 `domain/model/` —— 领域类型

- [ ] **1-1** `domain/model/device_state.h`
  - 来源：新建（内容来自 `app/app_fsm.h` 的 `DevState_t`、`DevCmd_t`）
  - 内容：`DevState_t`（INIT/IDLE/RUN/COMPLETE/FAULT/STOP）、`DevCmd_t`（ORDER/STOP/RESUME/RESET/HOME）

- [ ] **1-2** `domain/model/wash_types.h`
  - 来源：新建（内容来自 `app/app_wash_engine.h`、`app/app_wash_steps.h`）
  - 内容：`WashMode_t`、`WashStep_t`、`WashExitCond_t`（位置/限位/超时/组合）

- [ ] **1-3** `domain/model/safety_types.h`
  - 来源：新建（内容来自 `service/svc_alarm.h` 的 `AlarmLevel_t`、恢复策略宏）
  - 内容：`safety_state_t`（OK/WARNING/LOCKOUT）、`alarm_level_t`、`alarm_recover_t`（uint32_t 位掩码类型）

- [ ] **1-4** `domain/model/command.h`
  - 来源：新建
  - 内容：`cmd_t` 统一命令结构体（type + payload），覆盖 CLI/MQTT/按钮输入

- [ ] **1-6** `domain/model/alarm_code.h`
  - 来源：整理自 `config/alarm_config.h`
  - 内容：所有报警码常量（8001–8030），每个附注释说明含义

### 1.2 `ports/` —— 边界接口

- [ ] **1-7** `ports/hal/hal_motion_port.h`
  - 内容：`hal_motion_ops_t` 函数指针表
    - `gantry_fwd(freq)` / `gantry_rev(freq)` / `gantry_stop()`
    - `brush_run(freq)` / `brush_stop()` / `brush_select(sel)`
    - `lift_up_start()` / `lift_down_start(pulses)`（异步，完成后发事件）
  - 注册：`void hal_motion_register(const hal_motion_ops_t *ops)`

- [ ] **1-8** `ports/hal/hal_sensor_port.h`
  - 内容：`hal_sensor_ops_t`
    - `gantry_at_fwd_limit()` / `gantry_at_rev_limit()`
    - `lift_at_top()` / `lift_at_bottom()`
    - `is_estop_active()`
    - `get_gantry_pos()` / `reset_gantry_pos()`
    - `get_vfd_fault_code(hal_vfd_id_t)`

- [ ] **1-9** `ports/hal/hal_io_port.h`
  - 内容：`hal_io_ops_t`
    - `do_set(pin, val)` / `di_read(pin)`
    - `register_input_cb(cb)` / `register_board_error_cb(cb)`

- [ ] **1-10** `ports/hal/hal_water_port.h`
  - 内容：`hal_water_ops_t`
    - `pump_set(on)` / `curtain_set(on)` / `foam_set(on)`
    - `brush_water_set(on)` / `highpres_set(on)` / `all_off()`

- [ ] **1-11** `ports/hal/hal_indicator_port.h`
  - 内容：`hal_indicator_ops_t`
    - `entry_light_set(state)` / `rod_open()` / `rod_close()`

- [ ] **1-12** `ports/cloud/report_port.h`
  - 内容：`cloud_report_ops_t`
    - `report_state(ctx)` / `is_connected()`

- [ ] **1-13** `ports/cloud/command_port.h`
  - 内容：`cloud_command_ops_t`
    - `register_command_cb(cb)`（收到 MQTT 命令后回调）

- [ ] **1-14** `ports/storage/param_store.h`
  - 内容：`param_store_ops_t` + 注册/获取接口
    - ops 成员：`get(key, buf, size)` / `set(key, val_str)` / `save()` / `load()`
    - 注册接口：`void param_store_register(const param_store_ops_t *ops)`
    - 获取接口：`const param_store_ops_t *param_store_get_ops(void)`

- [ ] **1-15** `ports/storage/deploy_store.h`
  - 内容：`deploy_store_ops_t` + 注册/获取接口
    - ops 成员：`get(key, buf, size)`（只读）
    - 注册接口：`void deploy_store_register(const deploy_store_ops_t *ops)`
    - 获取接口：`const deploy_store_ops_t *deploy_store_get_ops(void)`

### 1.3 `common/` —— 基础事件类型（无层级限制，所有层均可引用）

- [ ] **1-5** `common/event_types.h`
  - 来源：新建（放 `common/` 而非 `core/` 或 `domain/`：`common` 本无层级限制，既避免 `core → domain`，也避免 `core` 承载业务语义）
  - 内容：`event_type_t` 完整枚举 + `event_t` 结构体
    - 硬件事件：`EVT_HW_ESTOP_ON/OFF`、`EVT_HW_GANTRY_FWD_LIM`、`EVT_HW_GANTRY_REV_LIM`、`EVT_HW_LIFT_UP_LIM`、`EVT_HW_LIFT_DOWN_LIM`、`EVT_HW_ENCODER_TICK`、`EVT_HW_IO_OFFLINE`、`EVT_HW_VFD_BRUSH_FAULT`、`EVT_HW_VFD_GANTRY_FAULT`
    - 组件完成事件：`EVT_COMP_GANTRY_DONE`、`EVT_COMP_LIFT_DONE`、`EVT_COMP_HOME_DONE`、`EVT_COMP_BRUSH_STARTED`
    - 安全事件：`EVT_SAFETY_LOCKOUT`、`EVT_SAFETY_WARNING`、`EVT_SAFETY_CLEARED`
    - 命令事件：`EVT_CMD_ORDER`、`EVT_CMD_STOP`、`EVT_CMD_RESUME`、`EVT_CMD_RESET`、`EVT_CMD_HOME`
    - 报警事件：`EVT_ALARM_TRIGGERED`、`EVT_ALARM_CLEARED`
    - 云端事件：`EVT_CLOUD_CONNECTED`、`EVT_CLOUD_DISCONNECTED`
    - 流程事件：`EVT_WASH_STEP_DONE`、`EVT_WASH_DONE`、`EVT_WASH_ABORTED`
  - `event_t` 结构体：
    ```c
    typedef struct {
        event_type_t type;
        uint32_t     param;        /* 载荷：报警码、错误码、步骤号等 */
        uint32_t     timestamp_ms; /* event_bus 入队时自动填充，调用方无需设置 */
    } event_t;
    ```
  - 说明：`timestamp_ms` 由 `event_bus` 在入队时打入，调用方发布时忽略该字段；handler 收到后可用于调试和回放

---

## 阶段二：`core/event_bus`

> 依赖：阶段一（`common/event_types.h`，即任务 1-5）  
> 验证方式：单元测试（publish → subscribe 收到事件，顺序正确）

- [ ] **2-1** `core/event_bus/event_bus.h`
  - 内容：
    ```c
    /* event_t 定义在 common/event_types.h */
    /* handler 入参含 timestamp，只读；调用方发布时无需填写 timestamp */
    typedef void (*event_handler_t)(const event_t *evt);
    sw_err_t event_bus_init(uint32_t queue_size);
    /* 发布接口：调用方只传 type + param，timestamp 由 event_bus 入队时自动打入 */
    sw_err_t event_publish(event_type_t type, uint32_t param);
    sw_err_t event_subscribe(event_type_t type, event_handler_t handler);
    void     event_bus_dispatch_loop(void);  /* 由 scheduler 创建的线程调用 */
    ```

- [ ] **2-2** `core/event_bus/event_bus.c`
  - 内容：固定大小环形队列（`util_fifo` 或重新实现）、pthread mutex 保护、提供 `event_bus_dispatch_loop()` 阻塞循环
  - 要求：不使用 malloc，队列大小编译期配置（`config/threading/thread_config.h`）
  - 注意：**event_bus 不私自创建线程**；`event_dispatch_thread` 由 `core/scheduler` 注册和创建，线程函数入口调用 `event_bus_dispatch_loop()`
  - 入队实现：`event_publish()` 内部将 `{type, param}` 写入队列，同时调用 `time_util_get_ms()` 填充 `timestamp_ms`（`time_util` 须在 `event_bus_init()` 之前初始化，见 bootstrap 顺序）

- [ ] **2-3** `tests/unit/test_event_bus.c`
  - 用例：publish 后 subscribe handler 被调用；队列满时返回 `SW_ERR_OVERFLOW`；多 subscriber 各自收到事件

---

## 阶段三：`driver/` 清理 + `adapters/hal/linux_hw/` + `adapters/machine/m8/`

> 依赖：阶段一（ports/ 接口）  
> 验证方式：此阶段 `diag` CLI 尚未实现（在阶段六），使用**临时验证程序**或**保留旧 `bsp` CLI 过渡**；阶段六完成后用 `diag do / diag di` 替换

### 3.1 `driver/` 轻量改动

- [ ] **3-1** `driver/drv_vfd.c` — 增加串口总线互斥锁
  - 问题：两台 VFD 共享 `/dev/ttyS1`，多线程并发读写会冲突
  - 修改：`driver/drv_vfd.c` 内部持有 `pthread_mutex_t s_modbus_bus_mutex`，所有 Modbus 读写调用前加锁，上层（`hal_motion_linux.c`）无感知

- [ ] **3-2** `driver/drv_stepper.c` — 提升脉冲精度（保持同步接口）
  - 问题：当前 `drv_stepper_move()` 用 `usleep` 阻塞，Linux 调度误差可达数毫秒
  - 修改：改用 `nanosleep` 提升精度，接口保持同步，driver 层不私自创建线程
  - 异步化由上层负责：在 `core/scheduler` 中注册 `stepper_thread`（`SCHED_FIFO` 优先级），线程内部等待步进命令，执行完毕后通过 `event_bus` 发布完成事件（`EVT_COMP_LIFT_DONE`）

- [ ] **3-3** `driver/drv_io.c` — 无业务逻辑改动，确认接口干净

### 3.2 `adapters/hal/linux_hw/` —— 真机 HAL 实现

将 `bsp/bsp_hal.c` 按职责拆分为 5 个文件：

- [ ] **3-4** `adapters/hal/linux_hw/hal_motion_linux.c`
  - 来源：`bsp_hal.c` 的龙门/刷子/升降控制逻辑
  - 实现：`hal_motion_ops_t`（调用 `drv_vfd` 和 `drv_stepper`）
  - 注意：刷子接触器互锁逻辑（切换时 stop → 等 200ms → 切接触器）保留在此，不上移

- [ ] **3-5** `adapters/hal/linux_hw/hal_sensor_linux.c`
  - 来源：`bsp_hal.c` 的限位/急停/编码器读取
  - 实现：`hal_sensor_ops_t`（调用 `drv_io`，包含 gantry pos 原子计数）

- [ ] **3-6** `adapters/hal/linux_hw/hal_io_linux.c`
  - 来源：`bsp_hal.c` 的 IO 透传部分
  - 实现：`hal_io_ops_t`（薄封装 `drv_io`）

- [ ] **3-7** `adapters/hal/linux_hw/hal_water_linux.c`
  - 来源：`bsp_hal.c` 的水路控制
  - 实现：`hal_water_ops_t`（调用 `drv_io` DO 输出）

- [ ] **3-8** `adapters/hal/linux_hw/hal_indicator_linux.c`
  - 来源：`bsp_hal.c` 的入口灯光/挡杆控制
  - 实现：`hal_indicator_ops_t`（调用 `drv_io` DO 输出）

### 3.3 `adapters/machine/m8/` —— M8 机型适配

- [ ] **3-9** `adapters/machine/m8/m8_machine_map.h`
  - 来源：`driver/drv_io.h` 的引脚枚举 + `config/machine_config.h` 的硬件参数
  - 内容：M8 专属引脚映射、CAN 总线参数、串口参数、Modbus 地址（使用宏，有注释说明对应图纸编号）

- [ ] **3-10** `adapters/machine/m8/m8_feature_map.h`
  - 来源：`config/machine_config.h` 的 `CFG_xxx_INSTALLED` 系列宏
  - 内容：M8 功能安装矩阵（顶刷/侧刷/升降/各水路/入口灯/挡杆）

- [ ] **3-12** `adapters/machine/m8/m8_boot_profile.c`
  - 来源：`bsp/bsp_init.c` 的安全初始化部分
  - 内容：上电后将所有 DO 置安全状态（输出关断）、等待 IO 子板就绪（替换原来的硬等待 `sleep(2)` 为轮询检测）

---

## 阶段四：`domain/safety/` + `domain/device/` + `domain/process/`

> 依赖：阶段一（类型定义）、阶段二（event_bus）、ports/ 接口  
> 验证方式：单元测试（不依赖真实硬件）

### 4.1 `domain/safety/`

- [ ] **4-1** `domain/safety/alarm_core.h` / `alarm_core.c`
  - 来源：`service/svc_alarm.c`（提取纯引擎部分，去掉硬件回调注册）
  - 内容：
    - 报警配置表（内嵌原 `config/alarm_config.h`）
    - 防抖逻辑（`alarm_core_set_raw_trigger()`）
    - 直报逻辑（`alarm_core_set_state()`）
    - 等级判断（ERROR/WARNING/NOTICE）
    - 恢复策略（AUTO/DRIVE/MANUAL）
    - 对外发布 `EVT_ALARM_TRIGGERED` / `EVT_ALARM_CLEARED` 事件

- [ ] **4-2** `domain/safety/interlock.h` / `interlock.c`
  - 来源：新建（逻辑散在原 `comp_brush.c`、`bsp_hal.c`）
  - 内容：互锁条件判断（如：接触器切换前必须停 VFD；运动中禁止切换刷子）
  - 接口：`sw_err_t interlock_check_motion(motion_type_t type)` — 返回 OK 或错误码

- [ ] **4-3** `domain/safety/safety_fsm.h` / `safety_fsm.c`
  - 来源：新建
  - 内容：
    - 订阅 `EVT_ALARM_TRIGGERED`、`EVT_HW_ESTOP_ON` 等事件
    - 状态转移：OK → WARNING → LOCKOUT
    - 发布 `EVT_SAFETY_LOCKOUT` / `EVT_SAFETY_CLEARED`
  - 注意：safety_fsm **只发事件**，不直接写 `dev_ctx`（由 `safety_supervisor` 更新）

- [ ] **4-4** `tests/unit/test_alarm_core.c`
  - 用例：防抖触发、AUTO 恢复、MANUAL 复位、等级判断

- [ ] **4-5** `tests/unit/test_safety_fsm.c`
  - 用例：急停 → LOCKOUT、VFD 故障 → WARNING、复位 → OK

### 4.2 `domain/device/`

所有设备组件：用 `ports/hal/` 接口替换原来对 `bsp_hal.c` 的直接调用

- [ ] **4-6** `domain/device/brush.h` / `brush.c`
  - 来源：`component/comp_brush.c`
  - 修改：`hal_brush_*()` 调用改为通过 `hal_motion_ops` 接口
  - 保留：接触器切换状态机（HAL 层已实现互锁，这里只管语义）
  - 异步化：`brush_start()` 启动后发 `EVT_COMP_BRUSH_STARTED`

- [ ] **4-7** `domain/device/gantry.h` / `gantry.c`
  - 来源：`component/comp_gantry.c`
  - 修改：调用改为 `hal_motion_ops` + `hal_sensor_ops` 接口
  - 保留：原子位置计数（`encoder_tick()` 由 IO 回调触发）
  - 异步化：`gantry_home_start()` 非阻塞，归位完成后发 `EVT_COMP_HOME_DONE`

- [ ] **4-8** `domain/device/top_lift.h` / `top_lift.c`
  - 来源：`component/comp_top_lift.c`
  - 修改：调用改为 `hal_motion_ops` + `hal_sensor_ops` 接口
  - 异步化：`lift_up_start()` / `lift_down_start()` 非阻塞，完成后发 `EVT_COMP_LIFT_DONE`

- [ ] **4-9** `domain/device/water.h` / `water.c`
  - 来源：`component/comp_water.c`
  - 修改：调用改为 `hal_water_ops` 接口
  - 保留同步接口（水阀是瞬时开关，无需异步化）

- [ ] **4-10** `domain/device/gate.h` / `gate.c`
  - 来源：新建（逻辑散在原 `bsp_hal.c` 和 `app_fsm.c`）
  - 内容：`gate_allow()` / `gate_block()` / `gate_set_light(state)`（调用 `hal_indicator_ops`）

### 4.3 `domain/process/`

- [ ] **4-11** `domain/process/recipe.h` / `recipe.c`
  - 来源：`app/app_wash_steps.c`
  - 内容：`WashStepConfig_t` 结构体，标准洗/快洗步骤表，`recipe_get(mode, &steps, &count)` 接口
  - 步骤配置迁移到 `config/recipes/*.h`，recipe.c 直接 `#include` 装配，属于编译期静态数据

- [ ] **4-12** `domain/process/step_engine.h` / `step_engine.c`
  - 来源：`app/app_wash_engine.c` 的步骤执行逻辑
  - 内容：单步应用（启动各执行机构）、退出条件判断、超时保护
  - 执行方式：**同步单步执行器**，在 `wash_worker_thread` 内部被调用，执行单步直到退出条件满足或超时，返回 `sw_err_t` 结果；不主动发布事件，由 `wash_orchestrator` 根据返回值决定推进下一步或中止流程

- [ ] **4-13** `domain/process/wash_mode.h` / `wash_mode.c`
  - 来源：新建（逻辑散在 `app_wash_engine.c`、`svc_param.c`）
  - 内容：当前洗车模式管理、模式切换校验

- [ ] **4-14** `tests/unit/test_step_engine.c`
  - 用例：单步正常完成（限位触发）、单步超时、步骤中急停

### 4.4 `adapters/machine/m8/` —— 报警适配（依赖 alarm_core，放在阶段四末）

- [ ] **4-15** `adapters/machine/m8/m8_alarm_adapt.c`
  - 来源：`bsp/bsp_alarm.c`
  - 依赖：`domain/safety/alarm_core`（任务 4-1，必须先完成）
  - 内容：
    - `m8_signal_poll()`：IO 扫描，调用 `alarm_core_set_raw_trigger()`
    - `m8_emc_reset()`：急停复位序列（停龙门/刷子 → 关水路 → 复位 VFD → 红灯+推杆）
    - `m8_vfd_fault_read()`：读取两台 VFD 故障码
    - 初始化时注册以上回调到 `alarm_core`

---

## 阶段五：`service/` + `application/` + `core/scheduler` + `core/bootstrap`

> 依赖：以上所有阶段  
> 验证方式：仿真环境跑通完整洗车流程

### 5.1 `service/`

- [ ] **5-1** `service/dev_ctx/dev_ctx.h` / `dev_ctx.c`
  - 来源：新建
  - 内容：
    - `device_context_t` 结构体（见《框架设计说明》§9.2）
    - 分片写接口（5 个 `dev_ctx_set_*()` 函数，各自只允许特定调用方使用）
    - 只读接口：`dev_ctx_snapshot()`
    - 线程安全：内部 `pthread_mutex` 保护

- [ ] **5-2** `service/svc_param/svc_param.h` / `svc_param.c`
  - 来源：`service/svc_param.c`（轻量改造）
  - 修改：底层持久化改为调用 `param_store_ops` 接口，不再直接操作 cJSON 文件
  - 保留：业务语义参数访问接口（`svc_param_get_gantry_freq()` 等）

### 5.2 `core/scheduler/`

- [ ] **5-3** `core/scheduler/thread_registry.h` / `thread_registry.c`
  - 内容：线程注册表，记录各线程的函数指针、优先级、栈大小、周期
  - 接口：`thread_register(name, fn, prio, stack_size)`

- [ ] **5-4** `core/scheduler/scheduler.h` / `scheduler.c`
  - 内容：读取线程注册表，统一调用 `pthread_create`，设置 `sched_param`
  - 接口：`scheduler_start_all()`

### 5.3 `application/orchestrators/`

- [ ] **5-5** `application/orchestrators/safety_supervisor.h` / `safety_supervisor.c`
  - 来源：新建
  - 内容：
    - 订阅 `EVT_SAFETY_LOCKOUT` → 调 `dev_ctx_set_safety_state(LOCKOUT)`，向 `device_fsm` 投递停机命令
    - 订阅 `EVT_SAFETY_CLEARED` → 调 `dev_ctx_set_safety_state(OK)`
    - 订阅 `EVT_ALARM_TRIGGERED` / `EVT_ALARM_CLEARED` → 调 `dev_ctx_set_alarm_state()`

- [ ] **5-6** `application/orchestrators/device_fsm.h` / `device_fsm.c`
  - 来源：`app/app_fsm.c`（改写为事件驱动，去掉轮询 `hal_is_estop_active()`）
  - 修改：
    - 急停检测改为订阅 `EVT_SAFETY_LOCKOUT` 事件（不再主动轮询）
    - 状态转移通过事件触发，减少轮询
    - 状态变化后调 `dev_ctx_set_device_state()`

- [ ] **5-7** `application/orchestrators/wash_orchestrator.h` / `wash_orchestrator.c`
  - 来源：`app/app_wash_engine.c`（拆出流程编排，步骤执行逻辑留在 `domain/process/step_engine`）
  - 内容：
    - `wash_worker_thread` 由 `core/scheduler` 统一创建，orchestrator 持有信号量用于唤醒线程
    - 工作线程内部循环调用 `step_engine` 同步执行每一步，根据返回值顺序推进配方
    - 订阅 `EVT_SAFETY_LOCKOUT` 设置原子中断标志，工作线程每步检测后退出
    - 流程完成/中断后发 `EVT_WASH_DONE` / `EVT_WASH_ABORTED`
    - 调 `dev_ctx_set_wash_step()`

- [ ] **5-8** `application/orchestrators/report_aggregator.h` / `report_aggregator.c`
  - 来源：`app/app_cloud.c` 的上报部分
  - 内容：
    - 定时（500ms）读取 `dev_ctx_snapshot()` 并调用 `report_port` 接口上报
    - 订阅 `EVT_CLOUD_CONNECTED` / `EVT_CLOUD_DISCONNECTED` → 调 `dev_ctx_set_cloud_status()`

### 5.4 `application/usecases/`

以下 4 个用例文件均为薄封装，校验前置条件后转发命令事件：

- [ ] **5-9** `application/usecases/start_wash.c`
  - 内容：检查 safety_state == OK，检查 dev_state == IDLE，发布 `EVT_CMD_ORDER`

- [ ] **5-10** `application/usecases/stop_wash.c`
  - 内容：发布 `EVT_CMD_STOP`

- [ ] **5-11** `application/usecases/reset_fault.c`
  - 内容：检查 dev_state == FAULT，调 `alarm_core_manual_reset()`，发布 `EVT_CMD_RESET`

- [ ] **5-12** `application/usecases/home_device.c`
  - 内容：发布 `EVT_CMD_HOME`

### 5.5 `core/bootstrap/`

- [ ] **5-13** `core/bootstrap/wiring.h` / `wiring.c`
  - 内容：依赖注入——将 adapter 实现注册到 port 接口表
    ```c
    // 真机接线
    hal_motion_register(&g_linux_hw_motion_ops);
    hal_sensor_register(&g_linux_hw_sensor_ops);
    // ...
    param_store_register(&g_json_param_store_ops);
    ```
  - 仿真接线在 `wiring_sim.c` 中提供，CMake `BUILD_SIM` 选择

- [ ] **5-14** `core/bootstrap/bootstrap.h` / `bootstrap.c`
  - 来源：`bsp/bsp_init.c` + `app/app_main.cpp`
  - 内容（严格顺序）：
    1. 初始化 `time_util`（event_bus 入队时需要打时间戳，必须最先就绪）
    2. 初始化 `core/event_bus`
    3. 调用 `wiring()` 注册所有 adapter
    4. 初始化 `service/svc_param`（加载参数文件）
    5. 初始化 `service/dev_ctx`
    6. 调用 `m8_boot_profile_init()`（IO 子板就绪检测，替换 sleep(2)）
    7. 初始化 `domain/safety/alarm_core`
    8. 调用 `m8_alarm_adapt_init()`（注册机型报警回调）
    9. 初始化 `application/device_fsm`
    10. 初始化 `application/wash_orchestrator`
    11. 初始化 `application/safety_supervisor`
    12. 初始化 `application/report_aggregator`
    13. 调用 `scheduler_start_all()`（启动所有线程）

---

## 阶段六：外部适配器

> 依赖：阶段一（ports/）、阶段二（event_bus）  
> 验证方式：云端收到上报、CLI 命令可用

### 6.1 `adapters/cloud/aliyun/`

- [ ] **6-1** `adapters/cloud/aliyun/aliyun_report_adapter.c`
  - 来源：`app/app_cloud.c` 的上报部分
  - 实现：`cloud_report_ops_t`（调用 snack MQTT SDK）

- [ ] **6-2** `adapters/cloud/aliyun/aliyun_command_adapter.c`
  - 来源：`app/app_cloud.c` 的命令接收部分
  - 内容：MQTT 订阅与消息接收，收到消息后调用 `mqtt_command_parser` 解析为 `cmd_t`，再发布到 event_bus；只负责 MQTT 协议层，不含命令语义解析

- [ ] **6-3** `adapters/cloud/aliyun/aliyun_topics.h`
  - 内容：阿里云 Topic 字符串定义、属性字段名

### 6.2 `adapters/storage/json/`

- [ ] **6-4** `adapters/storage/json/json_param_store.c`
  - 来源：`service/svc_param.c` 中 cJSON 读写部分
  - 实现：`param_store_ops_t`

- [ ] **6-5** `adapters/storage/json/json_deploy_store.c`
  - 来源：新建（原部署配置混在 params.json 中）
  - 实现：`deploy_store_ops_t`，读取 `config/deployment/device.json`

### 6.3 `adapters/ui/`

- [ ] **6-6** `adapters/ui/cli/cli_adapter.c`
  - 来源：`app/app_main.cpp` 的 CLI 注册部分
  - 内容：注册 CLI 命令域到 snack CLI（使用新域名，不沿用旧架构命名）
    - `device`：设备状态查询、命令下发（替换旧 `app` 域）
    - `safety`：报警/急停状态查询与复位（替换旧 `alarm` 域）
    - `param`：参数读写（保留）
    - `diag`：硬件直控调试（替换旧 `bsp` 域）

- [ ] **6-7** `adapters/ui/cli/cli_commands.c`
  - 来源：各模块的 debug 命令处理函数
  - 内容：命令解析 → 调用 usecase 或直接读 `dev_ctx_snapshot()`

- [ ] **6-8** `adapters/ui/mqtt_cmd/mqtt_command_parser.c`
  - 来源：`app/app_cloud.c` 的命令解析部分
  - 内容：纯 JSON 文本 → `cmd_t` 解析器，**不依赖 MQTT SDK**；由 `aliyun_command_adapter` 调用，未来其他命令来源（BLE、本地按钮）也可复用
  - 职责边界：只做格式解析，不做 MQTT 订阅，不直接发布事件（由调用方发布 `EVT_CMD_*`）

### 6.4 `adapters/hal/sim_hw/` —— PC 仿真实现

- [ ] **6-9** `adapters/hal/sim_hw/hal_motion_sim.c`
- [ ] **6-10** `adapters/hal/sim_hw/hal_io_sim.c`
- [ ] **6-11** `adapters/hal/sim_hw/hal_water_sim.c`
- [ ] **6-12** `adapters/hal/sim_hw/hal_sensor_sim.c`
- [ ] **6-13** `adapters/hal/sim_hw/hal_indicator_sim.c`
  - 以上 5 个文件：实现对应的 ops 接口，内部维护虚拟状态（可打印输出动作日志），支持通过 `tools/simulator/` 注入传感器信号

---

## 阶段七：配置层重组

> 可在任意阶段并行推进，纯文件整理

- [ ] **7-1** `config/machine/m8_machine_config.h`
  - 来源：`config/machine_config.h` 中的硬件参数部分（CAN/串口/Modbus/IO 板数）

- [ ] **7-2** `config/features/m8_features.h`
  - 来源：`config/machine_config.h` 中的 `CFG_xxx_INSTALLED` 功能开关

- [ ] **7-3** `config/recipes/standard_wash_recipe.h`
  - 来源：`app/app_wash_steps.c` 的标准洗步骤表

- [ ] **7-4** `config/recipes/quick_wash_recipe.h`
  - 来源：`app/app_wash_steps.c` 的快洗步骤表

- [ ] **7-5** `config/deployment/device.json`
  - 内容：`{ "deviceSn": "", "siteId": "", "mqttServer": "", "mqttPort": 1883 }`
  - 说明：出厂写入，运行期只读，不放入参数服务

- [ ] **7-6** `config/threading/thread_config.h`
  - 内容：6 条线程的优先级、栈大小、周期配置（集中管理）
    - `event_dispatch_thread`（SCHED_OTHER）
    - `io_poll_thread`（SCHED_OTHER）
    - `device_fsm_thread`（SCHED_OTHER）
    - `wash_worker_thread`（SCHED_OTHER）
    - `cloud_thread`（SCHED_OTHER）
    - `stepper_thread`（SCHED_FIFO，高精度步进脉冲）

---

## 阶段八：测试补齐

- [ ] **8-1** `tests/unit/test_alarm_core.c`（见 4-4）
- [ ] **8-2** `tests/unit/test_safety_fsm.c`（见 4-5）
- [ ] **8-3** `tests/unit/test_step_engine.c`（见 4-14）
- [ ] **8-4** `tests/unit/test_event_bus.c`（见 2-3）
- [ ] **8-5** `tests/unit/test_dev_ctx.c` — 分片写入、只读快照、多线程并发读写
- [ ] **8-6** `tests/scenario/scenario_standard_wash.c` — 标准洗全流程（仿真 HAL）
- [ ] **8-7** `tests/scenario/scenario_estop.c` — 洗车中急停 → LOCKOUT → 复位 → IDLE
- [ ] **8-8** `tests/scenario/scenario_vfd_fault.c` — VFD 故障触发停机 → 复位
- [ ] **8-9** `tests/scenario/scenario_limit_error.c` — 限位异常报警触发与自动恢复

---

## 收尾工作

- [ ] **C-1** `CMakeLists.txt` 完整重写，支持 `BUILD_SIM` 切换真机/仿真
- [ ] **C-2** 删除旧目录：`app/`、`component/`、`bsp/`（内容已全部迁移）
- [ ] **C-3** 依赖检查：用 `grep -r "drv_io\|drv_vfd\|drv_stepper\|DO_\|DI_\|ttyS\|can0" src/domain/` 确认 domain 层无硬件常量和驱动直调
- [ ] **C-4** 依赖检查：用 `grep -r "drv_io\|drv_vfd\|drv_stepper" src/application/` 确认 application 层无驱动调用
- [ ] **C-5** 更新 `CLAUDE.md` 中的目录架构说明
- [ ] **C-6** 更新 `common/sw_version.h` 版本号至 v0.2.0

---

## 任务统计

| 阶段 | 任务数 | 关键输出 |
|------|--------|----------|
| 零：准备 | 4 | 目录结构、git 分支 |
| 一：类型定义 | 15 | domain/model/ + ports/ |
| 二：event_bus | 3 | core/event_bus（含测试）|
| 三：驱动+适配 | 12 | adapters/hal/linux_hw/ + machine/m8/ |
| 四：领域核心 | 15 | domain/safety/ + device/ + process/ + m8_alarm_adapt |
| 五：服务+编排 | 14 | service/ + application/ + bootstrap |
| 六：外部适配 | 13 | cloud/ + ui/ + sim_hw/ |
| 七：配置重组 | 6 | config/ 五层分离 |
| 八：测试补齐 | 9 | 4 单元测试 + 4 场景测试 |
| 收尾 | 6 | 清理 + 验证 + 文档 |
| **合计** | **96** | |

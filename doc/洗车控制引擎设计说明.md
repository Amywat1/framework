# 洗车控制引擎设计说明

**版本**：v1.0  
**状态**：已落地（领域模型 + 表达式引擎 + tick 运行时 + JSON 加载器 + 仿真 IO 后端 + 应用编排链）  
**最后同步代码**：2026-07-12（`domain/wash/engine`、`domain/wash/model`、`engine_program_json`、`engine_io_sim`、`engine_program_loader_port`、`wash_orchestrator`、`recovery_service`、`emergency_handler`）  
**适用范围**：`domain/wash/engine/`、`domain/wash/model/`、`adapters/outbound/storage/json/engine_program_json.*`、`adapters/outbound/hal/sim/engine_io_sim.*`、`ports/outbound/storage/engine_program_loader_port.*`、`application/orchestrators/wash_orchestrator.*`、`application/recovery_service.*`、`application/safety_deferred_stop.*`、`application/orchestrators/emergency_handler.*`  
**架构基线**：Ports & Adapters + 配置驱动流程引擎 + tick 推进  
**关键词**：engine_program、phase/lane/step、entry_guard、exit_guard、interlock、engine_expr、engine_io、JSON loader

---

## 1. 设计目标与核心理念

洗车控制引擎负责把“洗车方案”从硬编码流程拆成可配置的阶段、通道、步骤和动作。领域层只解释方案模型并按 tick 推进状态，不直接依赖具体 IO 板、变频器、电机 SDK 或文件系统。

### 1.1 设计目标

- **流程配置化**：洗车过程由 `engine_program_t` 描述，运行时只读方案模型。
- **硬件解耦**：引擎通过 `engine_io_ops_t` 按名称读 DI、读轴、写 DO。
- **表达式驱动**：进入条件、退出条件、触发条件、联锁条件和控制输出值均可由 `engine_expr` 编译并重复求值。
- **tick 推进**：外部固定周期调用 `engine_tick(dt_ms)`，引擎不创建线程。
- **加载与执行分离**：JSON 加载器只负责解析、编译表达式和语义校验；运行时只接收已构建的 `engine_program_t`。
- **安全优先**：每个 tick 先扫描 interlock，再推进 phase/step；`halt_all` 会清零已收集的 DO 通道。

### 1.2 核心理念

| 原则 | 规定 |
|------|------|
| domain 不碰外设 | `engine.c` 只调用 `engine_io_get_ops()`，不 include HAL/SDK |
| 方案内存所有权清晰 | `engine_load_program()` 接管 `engine_program_t`，`engine_destroy()` 释放 |
| 表达式预编译 | JSON 加载期将字符串编译为 `engine_expr_t`，tick 内只求值 |
| 阶段串行、通道并行 | phase 按数组顺序串行推进；同一 phase 内 lane/step 每 tick 全量扫描 |
| 联锁高优先级 | `engine_tick()` 中 markers → interlocks → phase，联锁可中断后续流程 |
| IO 名称校验可选 | 注册 `engine_io_catalog_t` 时加载期校验 signal/output/axis；未注册时跳过 IO 名校验 |

### 1.3 端到端数据流

```text
JSON 方案文件 / 字符串
        │
        ▼
engine_program_json
        ├─ cJSON 解析 program
        ├─ engine_expr_compile()
        ├─ engine_program_validate()
        └─ engine_program_t*
                │
                ▼
engine_load_program(engine, program)
                │
                ▼
engine_start()
                │
                ▼
周期调用 engine_tick(dt_ms)
        ├─ markers_tick()       ← engine_io.read_signal/read_axis
        ├─ interlocks_tick()    ← condition / reset_condition
        └─ phase_tick()
             ├─ entry_guard / on_enter
             ├─ lanes steps
             │    ├─ event step: trigger → actions → done
             │    └─ control step: active_while → value_expr → output
             └─ exit_guard / on_exit / advance phase
```

---

## 2. 分层归属与依赖方向

### 2.1 模块落位

| 层次 | 路径 | 职责 |
|------|------|------|
| **运行时引擎** | `domain/wash/engine/engine.{h,c}` | 创建/销毁、装载方案、启动、tick 推进、状态查询 |
| **表达式引擎** | `domain/wash/engine/engine_expr.{h,c}` | 表达式编译、求值、变量遍历、AST clone |
| **IO 端口** | `domain/wash/engine/engine_io.{h,c}` | 注册/获取 `engine_io_ops_t` 与 `engine_io_catalog_t` |
| **方案模型** | `domain/wash/model/engine_model.{h,c}` | program/phase/lane/step 数据结构、释放、clone、枚举解析 |
| **语义校验** | `domain/wash/model/engine_program_validate.{h,c}` | 校验 phase、after、IO 名、表达式变量、estop 联锁等 |
| **manifest 校验** | `domain/wash/model/engine_program_manifest.{h,c}` | JSON 文件 SHA256 + manifest 比对 |
| **加载端口** | `ports/outbound/storage/engine_program_loader_port.{h,c}` | `engine_program_load()` 抽象入口 |
| **JSON 适配器** | `adapters/outbound/storage/json/engine_program_json.{h,c}` | cJSON → `engine_program_t`，注册 loader port |
| **仿真 IO** | `adapters/outbound/hal/sim/engine_io_sim.{h,c}` | 内存 DI/DO/axis 后端，注册 engine IO ops/catalog |
| **测试** | `tests/domain/test_wash_engine.c` | 领域模型、表达式、runtime 单元测试 |
| **测试** | `tests/adapters/test_engine_program_json.c` | JSON loader + sim IO + engine 闭环测试 |

### 2.2 依赖方向

```text
application / orchestrator
        │ engine_program_loader_port / engine API
        ▼
   domain/wash/engine
        ├── domain/wash/model
        └── engine_io ops
                ▲
                │ engine_io_register()
   adapters/outbound/hal/sim/engine_io_sim

   adapters/outbound/storage/json/engine_program_json
        ├── domain/wash/model
        ├── domain/wash/engine/engine_expr
        ├── engine_program_validate
        └── third_party/cJSON
```

**依赖禁令**：

- `domain/wash/engine` 不得 include `adapters/`、文件系统或具体 HAL 头文件。
- `engine_program_loader_port` 不得依赖 JSON 适配器实现。
- `engine_program_json` 不得执行洗车流程，只构建模型并校验。
- `engine_io_sim` 仅用于仿真/测试，不应成为真机 wiring 的默认后端。

### 2.3 推荐启动顺序

当前 `wash_orchestrator` 已负责方案加载、engine 生命周期、worker tick、完成/中止事件发布。项目层 wiring 应按以下顺序装配：

```text
wiring()
    ├── engine_io_sim_register() / 真机 engine_io 后端 register
    ├── engine_program_json_register_loader()
    └── 其他 ports/adapters

wash_orchestrator_init()
    ├── engine_program_load(path, err, errsz)
    ├── engine_create()
    └── engine_load_program(engine, program)

启动洗车命令
    └── engine_start()

洗车 worker 周期
    └── engine_tick(engine, dt_ms)
```

---

## 3. 文件清单

| 文件 | 职责 |
|------|------|
| `domain/wash/engine/engine.h` | engine opaque API、运行状态、当前阶段查询 |
| `domain/wash/engine/engine.c` | tick 状态机、interlock、marker、phase/lane/step 推进 |
| `domain/wash/engine/engine_expr.h` | 表达式编译/求值/clone/变量遍历接口 |
| `domain/wash/engine/engine_expr.c` | tokenizer + 递归下降解析 + AST 求值 |
| `domain/wash/engine/engine_io.h` | IO ops/catalog 契约 |
| `domain/wash/engine/engine_io.c` | IO ops/catalog 注册单例 |
| `domain/wash/model/engine_model.h` | program/phase/lane/step/action/interlock 数据模型 |
| `domain/wash/model/engine_model.c` | 模型释放、深拷贝、字符串枚举解析 |
| `domain/wash/model/engine_program_validate.h` | 方案语义校验接口 |
| `domain/wash/model/engine_program_validate.c` | IO 名、变量、after、phase、estop 校验 |
| `domain/wash/model/engine_program_manifest.h` | manifest 校验接口 |
| `domain/wash/model/engine_program_manifest.c` | SHA256、manifest JSON 读取与比对 |
| `ports/outbound/storage/engine_program_loader_port.h` | 加载端口 ops、register/get/load |
| `ports/outbound/storage/engine_program_loader_port.c` | 加载端口单例实现 |
| `adapters/outbound/storage/json/engine_program_json.h` | JSON loader API 与注册入口 |
| `adapters/outbound/storage/json/engine_program_json.c` | JSON 解析、模型构建、loader port 注册 |
| `adapters/outbound/hal/sim/engine_io_sim.h` | 仿真 IO 注入/读取 API |
| `adapters/outbound/hal/sim/engine_io_sim.c` | 内存 DI/DO/axis 后端与 catalog |

---

## 4. 数据模型

### 4.1 顶层方案

```c
typedef struct {
    char schema_version[ENGINE_VERSION_MAX];
    char id[ENGINE_NAME_MAX];
    char name[ENGINE_DISPLAY_MAX];

    engine_param_t     *params;       unsigned param_count;
    engine_axis_t      *axes;         unsigned axis_count;
    engine_marker_t    *markers;      unsigned marker_count;
    engine_interlock_t *interlocks;   unsigned interlock_count;
    engine_phase_t     *phases;       unsigned phase_count;
} engine_program_t;
```

当前 JSON loader 支持 `schema_version == "1.0"`。

### 4.2 阶段、通道、步骤

```text
program
  └── phases[]                  串行
        ├── entry_guard
        ├── exit_guard
        ├── on_enter / on_exit
        └── lanes[]             phase 内并行扫描
              └── steps[]       lane 内按数组扫描
                    ├── event step
                    └── control step
```

**phase 字段**：

| 字段 | 含义 |
|------|------|
| `id` / `name` | 阶段标识与显示名 |
| `direction` | `none` / `forward` / `backward` |
| `entry_guard` | 阶段进入条件；未满足时停留等待 |
| `exit_guard` | 阶段退出条件；满足后执行 on_exit 并进入下一阶段 |
| `timeout_ms` | 阶段最大时长；校验要求非 0 |
| `on_timeout` | 超时策略；默认 `halt_phase` |
| `on_enter` / `on_exit` | 阶段进入/退出即时动作 |

### 4.3 event step

event step 是一次性动作步骤，状态流如下：

```text
WAIT_AFTER → ARMED → RUNNING → WAIT_DONE → DONE
                  │
                  └──────── actions_complete ───────→ DONE
```

| 字段 | 含义 |
|------|------|
| `trigger` | `condition` 表达式或 `signal` 边沿/电平 |
| `guard` | 可选触发后守卫，失败则保持 ARMED |
| `actions` | 顺序动作，支持 `io_set` 与 `wait_time` |
| `done` | 完成条件：`actions_complete` / `trigger_exit` / `signal` / `timeout` |
| `after` | 同 phase 内前置 step id，最多 `ENGINE_AFTER_MAX` 个 |
| `on_error` | done 超时或表达式失败时的策略 |

### 4.4 control step

control step 是持续控制输出，不使用 event step 的运行态。

| 字段 | 含义 |
|------|------|
| `active_while` | 为真时输出 `value_expr` |
| `value_expr` | 输出值表达式，转为 `int` 写 DO |
| `output` | DO 通道名 |
| `on_error` | `value_expr` 求值失败时的策略，默认 `halt_phase` |

每个 tick 中：

```text
active_while == true  → write_output(output, (int)value_expr)
active_while == false → write_output(output, 0)
```

### 4.5 interlock

```c
typedef struct {
    char id[ENGINE_NAME_MAX];
    engine_expr_t *condition;
    engine_interlock_action_t action;
    engine_action_t *actions;
    unsigned action_count;
    int priority;
    engine_expr_t *reset_condition;
    bool auto_reset;
} engine_interlock_t;
```

| action | 行为 |
|--------|------|
| `halt_all` | 清零已收集的 DO 通道，状态转 `ENGINE_STATE_HALTED` |
| `halt_phase` | 执行当前 phase on_exit、停 control 输出，状态转 `ENGINE_STATE_PHASE_HALTED` |
| `custom_action` | 立即执行 interlock actions，不改变运行状态 |

扫描顺序按 `priority` 升序。校验要求存在 id 为 `estop` 的 interlock。

### 4.6 marker 与 axis

marker 用于在 DI 边沿到达时锁存 axis 位置：

```text
marker.signal edge 命中
    └── engine_io.read_axis(marker.axis)
          ├── position → markers.<id>.position
          └── valid    → markers.<id>.valid
```

表达式可引用：

- `axes.<id>.position`
- `axes.<id>.speed`
- `axes.<id>.valid`
- `markers.<id>.position`
- `markers.<id>.valid`

---

## 5. 表达式系统

### 5.1 支持语法

| 类别 | 支持项 |
|------|--------|
| 算术 | `+`、`-`、`*`、`/`、一元负号 |
| 比较 | `>`、`<`、`>=`、`<=`、`==`、`!=` |
| 逻辑 | `AND`、`OR`、`NOT` |
| 三目 | `cond ? a : b` |
| 常量 | 数字、`true`、`false` |
| 变量 | `$param`、DI 名、axis、marker、phase 变量 |

除零返回 `0.0`，不会直接报错。未知变量会使本次求值 `ok=false`。

### 5.2 变量解析

运行时 `engine_resolve()` 解析变量：

| 变量形态 | 来源 |
|----------|------|
| `$name` | `engine_program_t.params` |
| `axes.<id>.position/speed/valid` | `engine_io_ops_t.read_axis` |
| `markers.<id>.position/valid` | engine 内部 marker runtime |
| `phase.elapsed_ms` | 当前 phase 累计时间 |
| `phase.direction` | `forward=1.0`、`backward=-1.0`、`none=0.0` |
| 其他名称 | 作为 DI 信号名，调用 `read_signal(name)` |

### 5.3 编译与 clone

- `engine_expr_compile(text)`：语法错误返回 `NULL`，可通过 `engine_expr_last_error()` 获取错误描述。
- `engine_expr_clone(expr)`：深拷贝 AST，供 `engine_program_clone()` 使用。
- `engine_expr_foreach_var()`：用于加载期语义校验，遍历变量名并去重。

---

## 6. 运行时行为契约

### 6.1 生命周期

| API | 行为 |
|-----|------|
| `engine_create()` | 分配实例，初始 `ENGINE_STATE_IDLE`，`cur_phase=-1` |
| `engine_load_program(e, prog)` | 接管 `prog`，构建 runtime tables 和 DO 通道集 |
| `engine_start(e)` | 复位 marker/interlock，进入第 0 个 phase |
| `engine_tick(e, dt_ms)` | 推进 marker、interlock、phase |
| `engine_recover(e)` | 从 `PHASE_HALTED` 恢复当前 phase |
| `engine_destroy(e)` | 释放 runtime tables 和接管的 program |

### 6.2 状态机

| 状态 | 含义 |
|------|------|
| `ENGINE_STATE_IDLE` | 未启动或仅装载方案 |
| `ENGINE_STATE_RUNNING` | 正在执行 |
| `ENGINE_STATE_PHASE_HALTED` | 当前阶段暂停，允许 `engine_recover()` |
| `ENGINE_STATE_HALTED` | 全局终止，不通过 `recover` 恢复 |
| `ENGINE_STATE_DONE` | 所有 phase 完成 |

### 6.3 tick 顺序

```text
engine_tick()
    ├── markers_tick()       // RUNNING / PHASE_HALTED 下执行
    ├── interlocks_tick()    // RUNNING / PHASE_HALTED 下执行
    ├── 若状态不是 RUNNING，返回
    └── phase_tick()
```

**注意**：`phase.elapsed_ms` 在 `phase_tick()` 末尾累加。因此某一拍内表达式看到的是进入本拍前的累计时间。

### 6.4 phase 推进

1. 若 phase 尚未 entered，先求 `entry_guard`。
2. `entry_guard` 为真后执行 `on_enter` 一次。
3. 扫描所有 lane/step。
4. 累加 `phase_elapsed += dt_ms`。
5. 若达到 `timeout_ms`，按 `on_timeout` 处理。
6. 若 `exit_guard` 为真，执行 `on_exit` 并进入下一 phase。
7. 最后一个 phase 退出后状态转 `ENGINE_STATE_DONE`。

### 6.5 step 推进

event step 在单拍内允许级联推进，例如 `WAIT_AFTER → ARMED → RUNNING → DONE` 可在同一次 tick 完成，减少固定一拍延迟。

`wait_time` 是跨拍动作：

- 遇到 `wait_time` 后设置 `waiting=true`。
- 后续 tick 按 `dt_ms` 扣减。
- 倒计时结束后继续执行后续 action。

### 6.6 halt 与 recover

`halt_all`：

- 遍历装载方案时收集到的所有 DO 通道。
- 对每个 DO 写 `0`。
- 状态置为 `ENGINE_STATE_HALTED`。

`halt_phase`：

- 停止当前 phase 内所有 control step 输出。
- 执行当前 phase `on_exit`。
- 状态置为 `ENGINE_STATE_PHASE_HALTED`。

`engine_recover()`：

- 仅 `ENGINE_STATE_PHASE_HALTED` 可调用。
- 清除 `halt_phase` 类联锁 active 标志。
- 将 `RUNNING` / `WAIT_DONE` 中途步骤回退到 `ARMED`。
- 重新执行 `on_enter`。
- 不重置 `phase_elapsed`，避免多次恢复扩展最大阶段时长。

---

## 7. JSON 加载器契约

### 7.1 入口

| API | 行为 |
|-----|------|
| `engine_program_load_json_string(json, err, errsz)` | 解析 JSON 字符串并返回 program |
| `engine_program_load_json_file(path, err, errsz)` | 读取文件后调用 string loader |
| `engine_program_json_register_loader()` | 注册到 `engine_program_loader_port` |
| `engine_program_load(path, err, errsz)` | 通过已注册 loader 加载；未注册返回 `NULL` 并填错 |

### 7.2 支持的 JSON 子集

当前 loader 支持以下结构：

- `program.schema_version == "1.0"`
- `params`：对象，值为 number；非 number 按 `0.0`。
- `axes`：对象，仅支持 `type == "physical"`。
- `markers`：对象，仅支持 `type == "latch"`。
- `interlocks`：数组，支持 `halt_all` / `halt_phase` / `custom_action`。
- `phases`：数组，必需且非空。
- event step：`condition` / `signal` trigger，`io_set` / `wait_time` action。
- control step：`active_while`、`value_expr`、`output`。

不支持字段不会自动报错；但关键必需字段缺失、枚举非法、表达式编译失败会返回 `NULL`。

### 7.3 加载期语义校验

`engine_program_validate()` 当前检查：

| 检查项 | 失败条件 |
|--------|----------|
| program 非空 | `prog == NULL` |
| phases | `phase_count == 0` 或 `phases == NULL` |
| phase id | 重复 |
| estop interlock | 缺少 id 为 `estop` 的 interlock |
| marker axis | 引用未知 axis |
| marker signal | catalog 存在且 signal 不在 catalog |
| phase timeout | `timeout_ms == 0` |
| expression vars | 未知 `$param`、axis、marker、DI |
| action output | catalog 存在且 DO 不在 catalog |
| trigger/done signal | catalog 存在且 DI 不在 catalog |
| after | 引用当前 phase 内不存在的 step |
| control output | catalog 存在且 DO 不在 catalog |

### 7.4 manifest 校验

`engine_program_manifest_verify(json_path, manifest_path, err, errsz)`：

- 读取 JSON 文件并计算 SHA256。
- 读取 manifest JSON。
- 比对 manifest 中记录的 hash 与 size。
- 成功返回 `SW_OK`，不一致返回 `SW_ERR_CRC`，文件/格式问题返回 `SW_ERR_PARAM`。

`engine_program_manifest_path_from_json()` 将 `xxx.json` 推导为 `xxx.manifest.json`。

---

## 8. 仿真 IO 后端

### 8.1 后端行为

`engine_io_sim` 是内存表实现：

| 类型 | API | 未设置时 |
|------|-----|----------|
| DI | `set_signal` / `get_signal` | 读 0 |
| DO | `set_output` / `get_output` | 读 0 |
| Axis | `set_axis` / `get_axis_pos` | 位置 0、速度 0、valid=false |

`engine_io_sim_register()` 同时注册：

- `engine_io_ops_t`
- `engine_io_catalog_t`

### 8.2 容量限制

| 宏 | 值 | 说明 |
|----|-----|------|
| `ENGINE_IO_SIM_NAME_MAX` | 48 | 单个名称最大长度 |
| `ENGINE_IO_SIM_SIGNAL_CAP` | 128 | DI 名条目上限 |
| `ENGINE_IO_SIM_OUTPUT_CAP` | 128 | DO 名条目上限 |
| `ENGINE_IO_SIM_AXIS_CAP` | 16 | 轴条目上限 |

容量满时新名称写入会被忽略；已有名称仍可读写。

---

## 9. 测试覆盖

当前仓库测试覆盖：

| 测试 | 覆盖点 |
|------|--------|
| `tests/domain/test_wash_engine.c` | 表达式求值、枚举解析、program clone、语义校验、tick 执行、after、phase done、halt_all |
| `tests/adapters/test_engine_program_json.c` | JSON 字符串加载、catalog 校验、sim IO 驱动 engine、loader port 文件加载 |

验证命令：

```bash
cmake --build build-native -j4
ctest --test-dir build-native --output-on-failure
```

截至 2026-07-12，`build-native` 下 42 个单元测试通过。

---

## 10. 当前边界与项目接入

### 10.1 已落地边界

- 已具备配置模型、表达式、运行时、JSON loader、loader port、仿真 IO。
- 已迁入 `wash_orchestrator`，将 START/STOP 洗车副作用连接到 engine 生命周期。
- 已迁入 `recovery_service`、`safety_deferred_stop`、`emergency_handler`，支持恢复请求、急停/安全锁定和延迟停机事件链。
- 可以在测试中从 JSON 方案加载并由 sim IO 驱动 orchestrator/engine 完成一次洗车会话。
- engine 运行时不创建线程，周期 tick 由 `wash_orchestrator` 注册的 worker 负责。

### 10.2 项目侧待接入

| 模块 | 说明 |
|------|------|
| 项目级 engine IO 真机后端 | 将 `engine_io_ops_t` 映射到真实 DI/DO/axis |
| 项目级方案文件 | M8 或其他项目的 JSON/YAML program 资产应留在项目层 |

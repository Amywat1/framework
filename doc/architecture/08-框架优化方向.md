# 框架优化方向

## 1. 文档定位

本文档用于指导 `wash-device-framework` 后续优化方向。它不描述具体代码实现细节，而是记录当前通用框架在架构治理、项目接入、运行可靠性和可维护性上的改进重点。

优化原则：

- 优先增强通用框架能力，不迁入项目专属资产。
- 优先把文档约束转化为代码检查、启动期校验或测试。
- 优先补齐可观测性和接入可靠性，再扩展新业务能力。
- 对洗车机这类嵌入式项目，不以追求纯 DDD / 纯六边形为目标；可靠、可诊断、可接入、可复用优先。
- 对会显著增加抽象层、调用链和排查成本的改造，应先评估真实收益。
- 每项优化应同步更新 `module-design/` 中对应模块设计，并补充重新生成的单元测试。

## 2. 总体评价

当前框架方向是合理的，已经具备通用洗车机框架的主要骨架：

- 框架与项目资产边界清楚。
- DDD 分层和 Ports & Adapters 方向正确。
- 命令、运行模式、报警、安全姿态、方案引擎、事件总线和状态投影已经形成核心闭环。
- profile、detector、水路和模板能力的沉淀比较克制，未直接迁入具体机型表格。
- 文档已拆分为 `architecture/` 和 `module-design/`，具备方案评审和代码基线两种视角。

后续优化重点不是继续搬迁项目表格，而是让框架更可验证、更可接入、更难被项目代码污染。

## 3. 嵌入式项目性价比判断

洗车机软件具备嵌入式项目的典型特点：硬件链路长、现场问题复现困难、安全状态重要、项目接入差异多。对这类系统，架构优化应优先服务于启动期发现问题、运行期定位问题和安全行为可验证。

因此，当前不建议为了架构纯度做大规模重构。只要通用框架不依赖具体 adapter、SDK、项目资产和机型表格，当前工程化六边形架构已经具备继续演进的基础。

高性价比方向：

- 项目接入契约检查。
- 事件总线统计和诊断。
- 安全状态矩阵和命令允许矩阵。
- 依赖边界自动检查。
- 全局注册生命周期规范。

中等性价比方向：

- 错误模型统一。
- 配置与资产版本化。
- 状态投影一致性策略。
- HAL provider 边界收紧。

低性价比或暂不优先方向：

- 为了纯 DDD，把所有 domain event 都抽象一层再发布。
- 把所有 domain 使用的 port 都迁入 domain 内部。
- 把 application 对 event bus 的依赖完全端口化。
- 把当前框架全面改造成教科书式 Clean Architecture。

这些低性价比方向不是错误方向，但只有在测试隔离、事件系统替换、多运行时支持或模块复用边界出现真实痛点时才建议推进。

## 4. 优先级建议

| 优先级 | 性价比 | 方向 | 实施策略 | 状态 |
|--------|--------|------|----------|------|
| P0 | 高 | 项目接入契约检查 | 启动期集中检查缺失 port、provider、binding 和资产 | 已完成 |
| P0 | 高 | 事件总线可观测性 | 增加统计、队列水位、失败计数和慢 handler 诊断 | 已完成 |
| P0 | 高 | 安全状态矩阵 | 明确报警、安全姿态、命令允许和恢复路径 | 已完成（切断失败的姿态升级规则未定） |
| P1 | 高 | 依赖边界自动检查 | 用脚本防止 domain、ports、common 被 adapter 或项目语义污染 | 已完成 |
| P1 | 高 | 全局注册生命周期治理 | 统一 register/reset/reinit 语义，降低测试和重启风险 | 已完成 |
| P2 | 中 | 错误模型统一 | 在错误码开始分散前建立错误语义和处理策略 | 已完成 |
| P2 | 中 | 配置与资产版本化 | 为方案、物模型、报警目录、binding 提供版本和 schema 校验 | 已完成 |
| P2 | 中 | HAL provider 边界收紧 | 先靠文档和检查约束，项目增多后再强化 | 未开始 |
| P3 | 中 | 状态投影一致性策略 | 云端、UI、诊断复杂后补齐重建和一致性规则 | 未开始 |
| P3 | 低到中 | 表达式与 profile 可控扩展 | 只在业务确有需要时扩展查询能力，避免脚本化 | 部分完成（白名单与加载期校验；无副作用约束未加机制）|

各项状态的具体落地范围见本文档对应章节的「落地情况」小节。除上表方向外，另有六轮不属于上表任何
方向的整治记录：并发缺陷与工程化护栏（第 17 节）、复用性与层归属（第 18 节）、
安全形式化与运行可靠性（第 19 节）、急停热路径（第 20 节）、
目录环与命名一致性（第 22 节）、序列化分层与云子域归位（第 23 节）。

## 5. P0：项目接入契约检查

### 问题

当前项目接入已拆分为 register/configure/bind/validate/init/start 阶段，但缺失 port、provider、binding 或资产时，仍可能在运行到某条业务链路时才暴露问题。

### 优化目标

在 `project_validate()` 及框架侧校验点提供统一的启动期接入检查，使项目错误尽早失败。

建议检查项：

- 必需 port 是否已注册。
- `alarm catalog` 是否已加载。
- `cloud model` 是否通过校验。
- `engine_program_loader` 是否注册。
- `engine_io` 和 IO catalog 是否符合项目选择。
- HAL provider 和设备控制 binding 是否完成。
- 配置资产和 manifest 是否存在且版本匹配。

### 验收标准

- bootstrap 阶段能输出明确的接入检查结果。
- 任一必需能力缺失时返回明确错误，不进入运行线程启动。
- Demo 和单元测试覆盖至少一个缺失 port、一个缺失资产和一个非法 binding 场景。

### 落地情况（已完成）

分两层：端口侧 `ports/port_contract.h`（14 个 `PORT_REQ_*` 位），资产侧
`application/asset_contract.h`（3 个 `ASSET_REQ_*` 位）。两者都由项目在
`project_validate()` 声明位掩码，框架一次报出全部缺失项而非首个即返回——接入调试
时一次看到完整清单，比反复启动逐个发现更省时间。

分工：**port_contract 答「函数表是否注册」，asset_contract 答「数据是否就位」。**
两者是同一问题的两面——端口注册了但资产没加载，业务链路同样在第一次真实调用才
失败，而启动日志一切正常。

资产侧补的是第 21.1 节核查出的实际缺口：

| 验收项 | 核查时状态 | 现状 |
|--------|-----------|------|
| `alarm catalog` 是否已加载 | **全仓零处检查** | `ASSET_REQ_ALARM_CATALOG`，两侧 wiring 均声明 |
| `cloud model` 是否通过校验 | 仅 target 调 `cloud_model_validate()` | `ASSET_REQ_CLOUD_POINT_TABLE`（target 声明），先查就位再查内容 |
| `engine_io` 和 IO catalog | 只有三个 smoke 判非空 | `ASSET_REQ_ENGINE_IO_CATALOG`，两侧 wiring 均声明 |
| 测试覆盖缺失资产场景 | **不存在** | `tests/application/test_asset_contract.c` 13 个用例 |

报警目录这条是三者中现场代价最高的：目录为空时 `alarm_registry` 把任何报警码都
解析不到定义，`trigger` 静默失败——设备表现为**"从不报警"而非启动失败**，只能靠
触发一次真实故障才发现。

两处判据值得记录：

- **`engine_io` 目录已注册但 signal 与 axis 皆空，按缺失处理。** 空目录下方案加载期
  无法校验任何名称，非法 signal / axis 会一路通过到求值期，即洗车中途失败而非
  加载期拒绝——与目录未注册的后果相同，故不区分。
- **只答「有没有」，不答「对不对」。** 点位表的字段级校验仍由 `cloud_point_validate`
  负责，两者互补。target 侧 `project_validate` 先调 `asset_contract_validate`
  再调 `cloud_model_validate`：物模型未注册时后者返回 `NOT_INIT`，缺失原因不如
  前者点名清楚。

为此补了两个探测入口：`alarm_registry_catalog_count()` 与 `cloud_model_point_count()`。
`cloud_model` 另补 `cloud_model_reset_for_test()`——`cloud_model_register(NULL)` 是
参数错误而非解除注册，本模块也无 init 入口可复用，故按 `device_snapshot` 的先例
单独提供。

`asset_contract` 独立成 CMake 目标而非并入 `wdf_application`：它要探测报警目录、
云物模型与方案引擎 IO 目录三处资产，并入会让最小接入被迫链接 `cloud` 与
`program_engine`，与 `wdf_application` 刻意保持最小的粒度划分冲突。理由同
`wdf_report_scheduler` 的拆分。

已按第 21.3 节的判据验证有生产调用方（M8 sim 与 target 两侧 `project_validate`），
并注入「探测函数恒返回 true」确认检查确有约束力——两个用例立即失败。测试正序与
倒序执行均通过。

## 6. P1：全局注册生命周期治理

### 问题

当前多个模块采用全局 singleton/register 模式。该模式适合嵌入式 C 框架，但需要统一生命周期语义，否则测试隔离、重复初始化和 bootstrap 失败恢复会变复杂。

### 优化目标

统一以下规则：

- 是否允许重复注册。
- 重复注册是覆盖、拒绝还是幂等成功。
- 是否提供 unregister 或 reset。
- 单元测试如何清理全局状态。
- bootstrap 失败后是否允许重新初始化。

### 建议方案

- 为测试提供 `*_reset_for_test()` 或统一 `framework_reset_for_test()`。
- 对运行期不允许重复注册的 port 返回明确错误。
- 对允许替换的 provider 在文档和 API 名称中表达清楚。
- 在 `module-design/` 中补充各模块注册生命周期表。

### 验收标准

- 单元测试不依赖测试执行顺序。
- 重复注册行为在所有 port/provider 中一致或有明确例外。
- bootstrap 失败路径不会留下不可预测的半初始化状态。

### 落地情况（已完成）

已完成：端口注册语义统一，见 `ports/port_registry.h`。

- 13 个 `*_register()` 全部返回 `sw_err_t`，不再是 `void`。
  原先 `engine_program_loader_register` 校验失败后静默忽略，调用方无从得知。
- `ops == NULL` 明确为「解除注册」，返回 `SW_OK`。这是受支持的显式操作，
  测试与运行期降级均可使用。
- `ops != NULL` 但缺必填函数指针时返回 `SW_ERR_PARAM`，且**保持原注册不变**，
  不会被半个 ops 覆盖。必填字段只限调用方会无条件解引用的入口：

  | 端口 | 必填字段 | 依据 |
  |------|----------|------|
  | `param_store` | `load` / `save` / `get` / `set` | `svc_param` 只判 ops 非空即调用四者 |
  | `deploy_store` | `load` | bootstrap 只判 ops 非空即调 `load()` |
  | `device_command_port` | `submit` | 本端口唯一入口 |
  | `alarm_binding` | `trigger` / `clear` / `load_catalog` | 适配器无条件调用三者 |
  | `engine_program_loader` | `load` | 本端口唯一入口 |
  | HAL / `machine_ops` / cloud 各端口 | 无 | 调用方逐个判空，允许注册能力子集 |

- 重复注册合法函数表以最后一次为准（替换），返回 `SW_OK`。
- 按注册表分层提供复位：`port_registry_hal_reset()`、`port_registry_infra_reset()`、
  `port_registry_cloud_reset()`。分层而非单一入口，是为避免测试为了一个复位函数
  被迫链接全部三个注册表源文件。

单元测试：`tests/ports/test_cloud_ports.c` 覆盖合法注册、NULL 解除、缺必填字段被拒
且不覆盖原注册、重复注册替换、复位清空，并在 `setUp`/`tearDown` 调用复位消除用例
间顺序耦合。

安全端口后续由弱符号改为注册表绑定（`ports/port_registry_safety.c`），遵循同一套
语义并提供 `port_registry_safety_reset()`，四个注册表的复位入口统一在
`ports/port_registry.h` 声明。

线程登记表也已补 `thread_registry_reset_for_test()`。此前
`tests/runtime/test_scheduler.c` 多个用例硬编码「累加后」的期望值——
`test_register_arg_passes_context` 期望 count 为 2、
`test_periodic_task_register_and_scheduler_start` 依赖前序用例遗留的 one_shot
线程才能观察到执行——顺序耦合比原先记录的一处更普遍。现在 `setUp` 统一复位，
各用例自行注册所需线程；倒序执行全部用例同样通过。

该复位接口只允许测试调用：线程一经 `scheduler_start_all` 创建即 detach 且无法
回收，清空登记表不会停止已启动的线程，生产路径调用会造成登记与实际线程不一致。

bootstrap 失败后的进程约束已写入 `runtime/bootstrap/bootstrap.h` 与
`architecture/04` 第 4.1 节：失败即必须终止进程，不得重试。不提供 teardown 是
有意取舍——为覆盖任意阶段失败而维护对称的反初始化路径，其自身正确性比
"失败即退出" 更难保证，而嵌入式设备由进程管理器重启即可回到确定状态。
文档同时写明了重复调用的具体后果（队列被重置、线程表累积重复条目）。

domain 层全局态也已补齐，但**没有采用**原方案里的统一 `framework_reset_for_test()`：
那样会让每个测试目标为了一个复位函数被迫链接全部 domain 模块，与端口注册表按层
拆分复位入口是同一个理由。改为按模块提供，并优先复用已有入口：

| 模块 | 复位方式 |
|------|----------|
| `operational_mode` | `operational_mode_init()` 本已复位三个字段，无需新增 |
| `alarm_registry` | `alarm_registry_init()` 补为清空目录、活动表、会话日志与待发事件 |
| `device_snapshot` | 新增 `device_snapshot_reset_for_test()`（本模块无 init 入口） |

`alarm_registry_init()` 选择改语义而非新增 `*_reset_for_test()`：13 个测试文件
本就在 `setUp` 调用它并当它是复位，而它此前只注册 binding、不碰任何状态——
把实现对齐到既有调用方的预期，比再加一个入口更贴合现状。bootstrap 在 bind 阶段
于 `project_bind_alarm_catalog` 之前调用，故清空目录不会丢已加载资产。

该项立即暴露了一处真实的顺序耦合：`test_telemetry_projection` 倒序执行时
`test_operational_projection_syncs_current_context` 失败（期望 `OP_MODE_IDLE`
得到 `OP_MODE_EXCEPTION`）——前序用例遗留的活动报警使 `op_mode` 推导出故障停机。
该用例此前只在"报警用例不先执行"的顺序下成立。现 `setUp` 统一复位，正序与倒序
均通过。

## 7. P0：事件总线可观测性

### 问题

事件总线是框架核心链路。真机问题常见于事件队列满、handler 执行慢、事件处理顺序不符合预期或关键事件被延迟。

### 优化目标

增强事件总线运行统计和诊断能力。

建议能力：

- 每类事件发布计数。
- 每类事件消费计数。
- 队列当前深度和高水位。
- 发布失败和队列满计数。
- handler 最大执行耗时。
- dispatch 线程 fatal 原因记录。

### 验收标准

- 测试可读取 event bus stats。
- 队列满场景有明确返回值和统计。
- 慢 handler 能被统计或告警。
- 安全关键事件的处理策略在文档中明确。

### 落地情况（已完成）

`event_bus_stats_t` 现含下列字段，测试可通过 `event_bus_get_stats()` 读取：

| 能力 | 字段 |
|------|------|
| 总量计数 | `published_count` / `dispatched_count` / `dropped_count` |
| 分类别计数 | `published_by_cat[]` / `dispatched_by_cat[]` / `dropped_by_cat[]`，下标为 `event_category_t` |
| 队列水位 | `queue_depth` / `queue_peak_depth` / `hi_queue_depth` / `hi_queue_peak_depth` |
| 失败计数 | `sem_post_fail_count` / `sem_wait_fail_count` |
| handler 耗时 | `handler_max_ms` / `handler_max_type` / `slow_handler_count` / `dispatch_max_ms` |

慢 handler 门限为 `EVENT_BUS_SLOW_HANDLER_MS`（50ms），超限同时计数并输出告警日志。

事件总线内部诊断已从 `printf` 改为 `sw_log`，队列满、丢事件、`sem_post` 失败等
关键记录会进入已注册的 sink 链（黑匣子、可观测服务），不再只打到 stdout。

dispatch 线程 fatal 原因通过 `event_bus_set_fatal_cb` 回调上报，bootstrap 注册
`system_panic_safe_stop` 置安全输出后 `abort()`。

单元测试：`tests/runtime/test_event_bus.c` 覆盖分类别计数、丢弃归集、慢 handler
识别与快 handler 不误判。

## 8. P0：安全状态矩阵

### 问题

报警、安全姿态、运行模式和命令网关已经形成链路，但安全行为需要更形式化，方便评审、测试和项目接入。

### 优化目标

形成统一安全矩阵：

- 报警响应策略到安全姿态的映射。
- `LOCKOUT` 下允许和拒绝的命令。
- 急停、暂停、停止、恢复、复位的状态转换。
- 安全输出失败时的兜底策略。
- ON_MOTION 重评估触发后的清除边界。

### 验收标准

- `architecture/` 有方案级安全矩阵说明。
- `module-design/` 有代码级状态表和测试映射。
- 单元测试覆盖关键命令在不同安全姿态下的裁决结果。

### 落地情况（已完成）

等级与清除策略的语义原先散在判定点上：`level >= ALARM_LEVEL_MAJOR` 出现 3 处、
`level == CRITICAL` 出现 2 处、清除策略判定 3 处。每处都在重新表达"MAJOR 意味着
什么"，调整语义要逐个找齐，漏改无编译期提示。

现收口到 `domain/safety/model/safety_matrix.h`，以**行为属性**而非等级序号表达语义：

| 矩阵 | 维度 | 收口的判定点 |
|------|------|--------------|
| 等级行为 | 阻塞开洗 / 进入 LOCKOUT / 记会话日志 | `alarm_registry` 会话日志、blocking 判定、姿态推导（共 5 处） |
| 清除策略 | 条件消失即清 / 需运动重评估 / 允许手动复位 | `alarm_registry` 自动清除、重评估分组、手动复位（共 3 处） |

改为行为属性而非"序号比较"的原因：`level >= MAJOR` 隐含了「等级按严重度排序且
新等级只会追加在末尾」这一假设。若将来在 MINOR 与 MAJOR 之间插入一级，所有
`>=` 判定都会静默改变含义。查表则要求新等级显式声明自己的三个属性。

命令允许矩阵此前已是表驱动（`operational_mode.c` 的 `k_cmd_matrix`，8×10 显式列出），
本轮未改动，只在文档中说明它与上面两张表的分工。

测试：`tests/domain/test_safety_matrix.c` 按格点逐一断言（3×3 两组共 18 格），
并校验「更高等级不得比更低等级宽松」的单调性不变量。已验证矩阵是行为的单一
来源——把 MAJOR 的 `blocks_wash_start` 改为 false 后，`test_safety_matrix`、
`test_alarm_registry`、`test_operational_mode` 三个测试同时失败。

安全输出失败的处置已补第一层——「可观测」，见第 20.2 节。原先 `cutout` 返回
`void`，M8 实现把每路 `safe_do_set` 的返回值全部 `(void)` 丢弃，切断失败时框架侧
完全无从知晓。现在失败会被逐层汇总、记录并计数。

仍未完成：失败后的姿态升级规则。当前语义是「失败可见但不改变状态机走向」，
没有"切断失败即升级到何种姿态"的统一规则。这取决于项目侧能提供哪些冗余切断
手段（第二路继电器、独立看门狗、动力回路硬连锁），框架无法单方面规定，暂不推进。

## 9. P1：依赖边界自动检查

### 问题

当前分层边界主要依靠文档和人工 review。随着项目接入增加，容易出现 domain include adapter、ports 暴露 SDK 类型、通用目录混入项目名等架构腐化。

### 优化目标

用脚本或 CI 检查约束依赖方向。

建议检查项：

- `domain/` 不 include `adapters/`。
- `ports/` 不 include provider 或 SDK 头。
- `common/` 不依赖上层目录。
- 通用框架目录不出现具体项目名、机型名、项目报警码表或 IO 点位表。
- `adapters/providers` 不出现项目工艺动作或报警码语义。

### 验收标准

- 新增 `scripts/check_arch_boundary.*` 或等价检查。
- CI 或本地验证能执行边界检查。
- 检查失败时输出具体文件和违规 include/关键词。

### 落地情况（已完成）

`scripts/check_arch_boundary.sh` 共 20 条规则，当前全部通过：

| 规则 | 约束 |
|------|------|
| R1 | `common/` 不依赖任何上层目录 |
| R2 | `domain/` 不依赖 `adapters/`、`application/` |
| R3 | `ports/` 不依赖 `adapters/`、`application/`、`runtime/` |
| R4 | `runtime/event_bus/` 不依赖业务层 |
| R5 | `runtime/scheduler/` 不依赖业务层 |
| R6 | 各框架层不引用项目专属头（`m8/` 前缀），按 8 个层分别检查 |
| R7 | 框架核心目录无项目机型前缀文件（`m8_*.c` / `m8_*.h`） |
| R8 | 全框架不使用 `../` 相对 include |
| R9a~R9d | `domain/` 不做文件 IO、序列化解析、线程注册、动态内存，详见第 18.5 节 |
| R10 | `adapters/` 不含 `*_bridge` / `*_projection` / `*_coordinator`，详见第 18.5 节 |

规则条数（20）多于表格行数：R6 按 8 个层各计一条，R9 拆为 4 条子规则。脚本
结尾输出的条数以实际执行的检查为准。

R8 是 R1~R6 的兜底：前六条依赖 `#include "<层>/` 的前缀匹配，若改用 `../` 形式
引用上层，依赖违规会绕过全部前缀规则。

`scripts/check_all.sh` 串联边界检查、格式检查（`format.sh --check`）与单元测试，
任一失败即整体失败，作为提交前门禁；无 cmake 环境时可用 `--skip-tests` 只做静态检查。

## 10. P2：错误模型统一

### 问题

框架已使用 `sw_err_t`，但不同模块对错误码语义的使用可能逐渐分散。

### 优化目标

统一错误分类和处理策略：

- 参数错误。
- 状态错误。
- 未注册或未初始化。
- 外设暂不可用。
- 存储损坏。
- 安全拒绝。
- 资源不足。
- 启动期致命错误。

### 验收标准

- 有统一错误语义说明。
- 关键模块错误返回与说明一致。
- 可重试和不可重试错误有明确边界。

### 落地情况（已完成）

语义与边界定义见 `common/sw_error.h`：三个判定 helper（`sw_err_is_transient` /
`_caller_fault` / `_missing_binding`）划分瞬时可重试、调用方缺陷、接入缺失三类，
`tests/common/test_sw_error.c` 断言三类互斥且覆盖全部错误码。

第 21 节核查时这三个 helper 尚无生产调用方——语义已定义但对运行时行为没有任何
约束力。现已接入 `report_scheduler` 的两处上报路径：

原先写法是 `(ret != SW_OK) && (ret != SW_ERR_COMM)`，手工挑出 COMM 一个码当作
"可以不告警"。但上报按周期重来，`SW_ERR_BUSY` 与 `SW_ERR_TIMEOUT` 同样是等下一拍
即可的情形，为它们打 WARN 只会淹没真正需要看的错误。改用 `sw_err_is_transient()`
后三个瞬时码一致处理，日志同时改用 `sw_err_name()` 输出可读名而非裸数字。

**刻意没有全局替换**：`drv_voice` 与 `hal_vfd_manager` 里的 `ret == SW_ERR_COMM`
判断保持原样。那两处用 COMM 驱动通信丢失/恢复状态机，语义就是"通信断了"，
把 `SW_ERR_BUSY` 也算作通信丢失会误报 COMM_LOST 事件。helper 表达的是"该不该重试"，
不能替代"具体哪种故障"——凡是错误码本身参与状态判定的地方，都应保留具体码。

这条区分也是 helper 的使用边界：适用于「按类别决定处置」（是否重试、是否告警、
是否致命），不适用于「按具体故障决定行为」。

## 11. P2：配置与资产版本化

### 问题

框架已有方案 manifest 校验，但项目接入涉及更多资产：云物模型、报警目录、HAL binding、水路拓扑和运行参数。这些资产需要版本和 schema 边界。

### 优化目标

为项目资产提供通用版本校验机制。

建议资产：

- engine program schema version。
- cloud model version。
- alarm catalog version。
- HAL binding version。
- fluid topology version。
- deploy config version。

### 验收标准

- 通用框架提供版本字段约定或校验 helper。
- 版本不兼容时启动期失败。
- 项目资产仍保留在项目侧，不迁入通用框架。

### 落地情况（已完成）

上面「建议资产」六项里，只有两项是运行期从文件读入的，也只有这两项需要版本校验：

| 资产 | 载入形态 | 版本校验 |
|------|----------|----------|
| engine program | 运行期读 JSON | 已接入（`ENGINE_PROGRAM_SCHEMA_SUPPORTED`）|
| deploy config | 运行期读 JSON | 已接入（`DEPLOY_CONFIG_SCHEMA_SUPPORTED`）|
| cloud model | 编译期 `cloud_model_bundle_t` 结构体 | **不适用** |
| alarm catalog | 编译期 `alarm_def_t` 数组，`m8_alarm_init` 合并三张表后 `load_catalog` | **不适用** |
| HAL binding | 编译期 X-macro 展开自 `m8_io_table.h` | **不适用** |
| fluid topology | 编译期 `m8_water_path_table[]` | **不适用** |

后四项标「不适用」而非「待接入」：它们与框架编译进同一个二进制，不存在版本漂移的
可能——结构体定义变了，编译期就失败，这比运行期比对版本串更强。给一个编译期数组
配版本串只是仪式，读它的代码和写它的代码本来就是同一次编译的产物。

原「建议资产」清单把这六项并列，隐含了「都需要版本校验」的判断，是不成立的。
判据应当是**资产能否独立于框架二进制变化**：能，才需要版本；不能，编译器已经保证。

deploy config 的接入要点：

- JSON 必须声明 `schemaVersion`，缺失即拒绝加载。不按"缺失视为最早版本"处理，
  是因为该文件承载设备身份与云端主题，静默接受一份不声明版本的配置，等于让
  字段含义的任何变更都无从被发现。
- 两种失败分开返回：版本串缺失或无法解析返回 `SW_ERR_PARAM`（资产本身不合契约），
  可解析但不兼容返回 `SW_ERR_STATE`（资产合法、只是与本框架不配）。
- 版本不合时整份丢弃新配置，但**保留上一次加载成功的内容**。`deploy_load` 也用于
  重新加载，运行期推来一份版本不对的配置时，清空会连设备身份与云端主题一起丢掉，
  故障面反而更大；首次加载失败则由 bootstrap 的 `BOOT_CHECK` 终止启动，不存在
  "带着空配置运行"。

测试：`tests/adapters/test_json_deploy_store.c` 覆盖缺字段、主版本不符、次版本更高、
带修订号后缀被接受、拒绝后旧配置仍可读五个场景。已用真机路径验证端到端行为——
`m8_sim` 在版本 `2.0` 时退出码 1 并输出「版本不兼容，资产 2.0.0 框架支持 1.0.x」，
删除字段时退出码 1 并输出「缺少 schemaVersion 字段」，恢复后正常启动。

## 12. P2：HAL provider 边界收紧

### 问题

vendor provider 放在通用仓库中有实际价值，但长期维护中容易混入项目语义。

### 优化目标

明确 provider 只做协议和 SDK 适配。

禁止内容：

- 项目动作名。
- 报警码和报警等级。
- 工艺流程判断。
- 具体机型点位表。
- 云端产品字段。

### 验收标准

- provider 文档明确输入输出边界。
- provider API 只接收通用配置或项目传入的 binding。
- 架构边界检查覆盖 provider 目录。

## 13. P3：表达式与 profile 可控扩展

### 问题

方案引擎表达式和 profile 查询接口已经具备扩展基础。后续若不加约束，表达式可能演化成难以验证的脚本语言。

### 优化目标

表达式只用于条件和简单计算，不承担副作用。

建议规则：

- 表达式不得写 IO、触发报警或修改状态。
- 内建函数必须白名单注册。
- 函数签名在加载期校验。
- profile 扩展优先增加查询接口，不迁入具体扫描算法。

### 验收标准

- 表达式加载期能发现未知函数、参数数量错误和类型错误。
- profile provider 缺失或查询失败时行为明确。
- 单元测试覆盖 profile 查询失败和默认值路径。

### 落地情况（部分完成：白名单与加载期校验）

四条建议规则里第二、三条已落地，第一、四条本轮未动（见下文「未做」）。

`engine_expr.c` 内新增 `k_builtins[]` 白名单表，`engine_expr_compile` 在语法解析
成功后递归全树校验每个 `NODE_FUNC`，三类错误在加载期拒绝并点名函数：

| 错误 | 原先 | 现在 |
|------|------|------|
| 未知函数名 | 求值期 `*ok = false`，无名称信息 | `未知函数 'body_containz'` |
| 参数个数不符 | 同上 | `函数 'body_contains' 需要 3 个参数，实际 2 个` |
| `profile.in_zone` 首参非字符串字面量 | 同上 | `函数 'profile.in_zone' 首参必须是字符串字面量` |

**为何值得做**：方案是部署时下发的资产。写错一个函数名，原先要洗到用到该表达式
的那一步才失败——车在机器里中途停机；现在部署即拒绝，方案不生效。这与本轮资产
契约是同一条思路：把「运行到某一步才失败」提前到「加载期拒绝」。

首参类型这条只能在加载期查：字符串在求值期一律置 `*ok = false`，`profile.in_zone`
的区域名是唯一合法的字符串位置，求值期无从区分「该位置本应是字符串」与「表达式
里误用了字符串」。

**求值侧的 arity 检查保留未删。** `engine_expr_clone` 可以绕过 `compile` 直接得到
AST，删掉会让克隆路径失去保护。两处检查读同一张白名单表，语义不会分叉。

**防漂移**：`test_engine_expr_whitelist_all_evaluable` 逐个实际求值白名单里的每个
函数，而不只是判「能编译」——表里加了名字而 `eval_node` 漏加分支时，加载期会放过
而求值期落到兜底的 `*ok = false`，只判能编译是抓不到这种漏分支的。已注入验证：
白名单加一个无求值分支的 `ghost_fn` 后该用例立即失败。

同样注入验证了校验本身有约束力——跳过 `check_functions` 调用后 4 个用例失败。

**本轮未做**：
- 「表达式不得写 IO、触发报警或修改状态」：当前 4 个内建函数全是纯查询，无副作用
  入口，该约束**事实成立但无机制保证**。要真正约束需在白名单表加「是否纯函数」
  标记并在注册期拒绝副作用函数——等真有扩展需求时再加，现在加是为假想未来预支。
- 「profile 扩展优先增加查询接口」：属评审判据而非可自动检查项，维持文档约束。
- 类型校验只覆盖了字符串字面量位置。数值 / 布尔位置的类型在本表达式语言里都是
  double，没有可校验的差异。

M8 现有方案（`programs/gen/` 下三个）未用到任何内建函数，故本项在项目侧无行为
变化，价值全在框架侧——下一个用到 `profile.*` 的方案会在部署期就得到反馈。

## 14. P3：状态投影一致性策略

### 问题

状态投影为云端、CLI、UI 和诊断提供只读快照。需要明确快照与事实源之间的一致性关系。

### 优化目标

明确以下策略：

- 快照是否允许短暂滞后。
- 云端上报读取事实源还是投影。
- 投影初始化和重建方式。
- 事件丢失后如何重新同步。
- 多个投影之间是否要求同一时刻一致。

### 验收标准

- `module-design/domain/状态投影与设备上下文模块设计.md` 补充一致性等级。
- 测试覆盖投影初始化、事件更新和重建路径。
- 云端上报和诊断读取路径有明确数据来源。

## 15. 不建议优先推进的纯架构化改造

以下方向可提升架构纯度，但对当前洗车机嵌入式框架的短期收益有限，暂不建议优先投入：

| 方向 | 暂不优先原因 | 触发条件 |
|------|--------------|----------|
| domain event 完全解耦 event bus | 会增加事件中转层和排查链路 | 需要替换 event bus，或 domain 需要脱离 runtime 独立复用 |
| 所有 domain port 迁入 domain 内部 | 目录重构成本高，收益主要是架构纯度 | port 出现 SDK 泄漏，或多个运行时需要不同端口定义 |
| application event bus 端口化 | 当前 event bus 是框架基础设施，替换需求不强 | 需要多种事件实现，或测试隔离成本明显上升 |
| 全面 Clean Architecture 化 | 会增加抽象层、初始化复杂度和调试成本 | 框架需要作为跨产品 SDK 长期独立发布 |

这些方向可以作为长期演进选项，但不应阻塞当前更有价值的接入检查、运行诊断和安全矩阵建设。

## 16. 推荐实施顺序

建议按以下顺序推进：

1. 项目接入契约检查。
2. 事件总线可观测性。（已完成）
3. 安全状态矩阵。
4. 依赖边界自动检查。（已完成）
5. 全局注册生命周期治理。（已完成）
6. 错误模型统一。
7. 配置与资产版本化。
8. HAL provider 边界收紧。
9. 状态投影一致性策略。
10. 表达式与 profile 可控扩展。

其中 1 到 5 是框架可靠性和架构治理基础，应优先于新增业务能力。
下一步建议从第 1 项（项目接入契约检查）和第 3 项（安全状态矩阵）继续。

## 17. 并发缺陷与工程化护栏修复记录

本节记录一轮代码审查后的修复，不属于上表任何方向，但影响运行可靠性。

### 17.1 已修复的并发缺陷

**事件总线互斥量优先级反转。** 事件总线允许被实时线程调用——框架自带的急停轮询
适配器以 `SCHED_FIFO` 优先级 90 发布事件，项目也可能从自己的实时线程发布——而队列锁
同时被多个 `SCHED_OTHER` 周期任务竞争。原先两把锁是裸 `PTHREAD_MUTEX_INITIALIZER`，
普通优先级线程持锁期间被抢占会让实时线程阻塞且时长不受优先级保护，构成无界优先级反转。现改为运行期 `pthread_mutexattr_init` +
`PTHREAD_PRIO_INHERIT`，用 `pthread_once` 保证恰好初始化一次（`event_bus_init()`
允许被重复调用，对已初始化互斥量再次 `pthread_mutex_init` 是 POSIX 未定义行为）。
平台不支持优先级继承时退化为默认互斥量，功能不受影响。

**命令网关信号量残留计数。** `command_gateway_drain()` 原先在解锁后才 `sem_post`。
若 `gateway_submit` 正好在该窗口内超时并释放槽位，信号量计数会残留给下一任槽主，
使其 `sem_timedwait` 立即返回并读到占位 receipt——命令被静默丢弃、调用方却收到
`SW_ERR_BUSY`。现将 `sem_post` 移入持锁范围（`sem_post` 不阻塞，持锁开销可忽略），
并在抢到槽位时用 `sem_trywait` 排空残留计数作为双重保险。

同一函数另修两处：`sem_timedwait` 补 `EINTR` 重试（截止时间是绝对值，重试不延长
总等待时长），避免一个信号就把正常命令报成超时；新增 dispatch 线程重入防护——从
dispatch 线程自身调用 `submit` 必然自锁，原先要白等满超时才返回，现直接返回
`SW_ERR_STATE` 并记录错误。调用上下文约束已写入 `command_port.h`。

**告警注册表锁范围。** `find_def_index()` 原在锁外调用，而 `load_catalog` 持锁改写
`s_catalog` 与 `s_catalog_count`，读写保护不对称。现将查表纳入同一把锁。

**安全投影快照不一致。** `telemetry_projection` 原先分两次取锁（`copy_active_projection`
+ `safety_posture`），拿到的活动表与安全姿态可能来自不同时刻。新增
`alarm_registry_copy_safety_view()` 一次持锁读出四项；同时补订阅
`EVT_SAFETY_LOCKOUT` / `EVT_SAFETY_NOMINAL`，避免姿态切换后快照滞后。

### 17.2 已修复的容量与时序问题

**周期任务槽位耗尽。** `PERIODIC_TASK_MAX` 原为 9，而 M8 实测 sim 与 target 均已
注册满 9 个周期任务，零余量——下一个周期任务注册就会让 bootstrap 失败。现改为与
`THREAD_REGISTRY_MAX` 对齐（后者由 16 提升到 24），周期任务不再构成独立的第二道
容量上限，超限统一由 `thread_registry` 报错。并加编译期断言防止两者失配。
`thread_registry.h` 中记录了槽位构成的核算口径。

**周期任务累积漂移。** 原实现是「执行回调 → `usleep(period)`」，睡的是固定相对
时长，回调耗时会累加进周期（10ms 任务若回调耗 3ms，实际周期变 13ms）。现改用
`clock_nanosleep(CLOCK_MONOTONIC, TIMER_ABSTIME)` 维护绝对下一拍；单次回调超过一个
周期时跳过已错过的拍而不追赶。9 个周期任务中有 5 个周期 ≤20ms，漂移会直接影响
传感器采样与水路判定的时间基准。

### 17.3 日志与诊断

- `sw_log` 增加运行期级别门限（`sw_log_set_level` / `sw_log_level_enabled`），
  过滤在格式化之前完成，被丢弃的日志不付出 `vsnprintf` 开销。
- 默认 sink 改为先整条组装进栈缓冲再单次 `fwrite`。原先用 `fprintf` + `vfprintf`
  + `fputc` 三次独立 stdio 调用，多线程并发时同一行会被其他线程输出插入。
- sink 表加锁保护，并在写日志时取快照后即释放锁，避免 sink 内部再写日志造成自锁。
- `level` 越界不再用于数组下标。

### 17.4 其他

- `util_fifo` 全部入口补判空；头文件明确「不含内部锁，多线程需调用方加锁」，
  并说明当前不可当作无锁 SPSC 队列使用的原因。
- `observation_context_t` 改为 `trace_context_t` 的别名。两者原本是同名同序的四个
  `uint64_t` 字段，重复定义会在各自演进后失同步。
- 云适配器的 `strcpy` 统一为 `snprintf`（原有长度校验本已到位，此处只统一风格）。
- `event_bus_dispatch_loop` 的 handler 快照数组补零：非法事件类型在 publish 侧已被
  拦截、理论不可达，但若不清零会保留上一轮 handler 并被重复调用。
- 补齐 `tests/stubs/mcc/motor_control_core_fake.c` 缺失的 `motor_pop_event` 桩。
  该缺失导致 `test_hal_motor_exec_mcc` 长期链接失败，而父工程从不构建 framework
  自带测试，因此一直未被发现。framework 独立测试现为 46/46 全通过。

## 18. 复用性与层归属整治记录

本节记录一轮架构评估后的结构性改动。这些改动不新增业务能力，目标是让框架能被
第二个项目干净接入，并让层边界从"文档约定"变为"脚本可判定"。

### 18.1 框架导出构建目标（原缺口：复用单位是源文件清单）

框架原先只为 5 个可选 vendor provider 建了 `add_library`，核心层一个目标都没有，
项目必须手抄 74 条 `framework/**/*.c` 路径。框架增删文件时项目要同步改列表，
第二个项目接入要把清单重抄一遍，编译选项与包含目录也无法传递。

现由 `cmake/wdf_targets.cmake` 导出分层目标，项目侧 `target_link_libraries` 即可。

选择 **INTERFACE 库而非 STATIC**：同一份框架源在不同目标下需要不同编译定义
（例如项目 smoke 目标为 `fluid_path.c` 定义 `FLUID_PATH_UNIT_TEST` 以剥离周期任务，
sim/真机不定义）。STATIC 只编译一次，无法同时满足；INTERFACE 传递源与包含目录，
由消费方按自己的宏编译，行为与手写源列表完全一致。

目标粒度按"可独立取用"划分而非按目录：`wdf_domain` 只含命令裁决、报警、遥测读模型，
设备控制模式与方案引擎各自独立（它们分别要求项目提供 motor provider / 方案资产，
最小接入并不需要）。`engine_session`、`report_scheduler`、方案 JSON 加载同理拆出。
验证方式是 demo 只链接最小集合仍能构建通过。

### 18.2 安全端口从弱符号改注册表（原缺口：最高风险处用了最松的绑定）

四个安全入口原为 `__attribute__((weak))` 空实现。弱符号是链接期绑定，项目漏实现
不会有任何提示——而空实现的 `safety_cutout_execute` 意味着急停不切断动力输出，
启动日志里也看不出异常。风险最高的四条路径用了整套端口机制里最松的方式。

现统一为 `safety_ops_t` 注册表：四字段全必填（缺任一都会让某条安全链路静默失效），
未注册时各入口首次被调用时报 ERROR（急停期间高频调用，故按路径各报一次而非每次），
并可由 `port_contract_validate(PORT_REQ_SAFETY)` 在启动期拦住漏注册。

改造过程中该机制立即暴露了两处此前被掩盖的真实缺陷：M8 smoke 在完成 wiring 前就
调用安全路径（弱符号时代是空操作，故一直"通过"）；急停 `cutout` 真正生效后会置位
水路 emergency 标志，影响后续水路时序断言。两处均已修正。

### 18.3 必需端口启动期校验（原缺口：漏注册与"不需要"无法区分）

`bootstrap` 原先对未注册端口一律 `return SW_OK` 跳过，于是漏注册能一路启动成功，
直到第一次业务调用才失败——现场表现为随机时刻功能不可用，而非启动失败。

现由项目在 `project_validate()` 声明必需端口位掩码，框架一次报出全部缺失项。
框架不内置"哪些端口必需"的判断：那取决于项目用到哪些能力，无云连接的项目本就
不该被要求注册 `cloud_link`。M8 sim 声明 10 项、target 13 项，demo 7 项。

### 18.4 层归属修正

| 原位置 | 现位置 | 理由 |
|--------|--------|------|
| `domain/program_engine/model/engine_program_manifest.*` | `adapters/outbound/storage/json/` | 做的全是文件读取 + SHA256 + JSON 解析，无领域规则；完整性校验改为 loader 端口的可选 ops，`application` 只依赖端口 |
| `domain` 内 `fluid_path` 自建周期任务 | 调用方注册 | 领域层不创建线程；`fluid_path_poll` 由条件编译的测试专用接口转为常规接口 |
| `runtime/platform/safety_thread.*` | `adapters/inbound/safety/estop_poll_thread.*` | 这是"如何采集急停"的一种实现，属入站适配器而非运行时核心。M8 有自己的 DI detector 采集通路，从未链接它 |
| `adapters/inbound/event/*_bridge.*` | `application/bridges/` | 四个 bridge 只依赖 domain + runtime，不适配任何外部系统；与已在 `application/orchestrators/` 的同性质协调器分处两层会让"adapters 是外部适配"失去可判定性 |
| `services/dev_ctx` | 删除，并入 `device_snapshot` | 该层只是把快照摊平再补一个 `cloud_connected`，无其他职责。`cloud_connected` 并入快照后读侧还获得了跨子域一致性 |

### 18.5 边界检查扩展（R9、R10）

原有 R1~R8 只看 include 路径，管不住"用标准库直接做本不属于该层的事"。新增：

- R9a~R9d：`domain/` 禁止文件 IO、序列化解析、线程注册、动态内存。
  方案引擎三文件按加载期一次性分配 + 显式 free 登记豁免，新增文件需显式登记。
- R10：`adapters/` 不得出现 `*_bridge` / `*_projection` / `*_coordinator`。

规则均以注入违规的方式验证过确实能捕获（而非写出永远通过的假规则）。

### 18.6 事件 param 编解码收口

三种不同位布局共用 `uint32_t param`，发布方与订阅方各自手写移位，改枚举顺序会
静默错位且编译器无法察觉。现统一为 `encode/decode` inline 函数，并补往返测试
锁定布局契约——两侧不同步时在 CI 阶段就失败。

### 18.7 跨线程读修正

`op_mode_*` 直读接口不加锁（其状态只在 event_dispatch 线程内修改），但
`m8_park_supervisor` 在自己的周期任务线程调用 `op_mode_get_current()`，构成无保护
跨线程读。已改用加锁的 `device_snapshot_get()`，并在头文件写明线程约束。

## 19. 安全形式化与运行可靠性记录

本节记录第二轮评估后的改动。上一轮（第 18 节）解决的是复用性与层归属，本轮
聚焦安全行为形式化、容量依据与错误语义。

### 19.1 安全矩阵（P0 收尾）

三个 P0 方向中最后一个未动的项。详见第 8 节「落地情况」。要点：等级与清除策略
的语义从散落在 8 处判定点收口到 `domain/safety/model/safety_matrix.h`，以行为属性
（阻塞开洗 / 进入 LOCKOUT / 记会话日志）而非等级序号表达。

选择行为属性而非序号比较的原因：`level >= MAJOR` 隐含「等级按严重度排序且新等级
只追加在末尾」的假设，若将来在 MINOR 与 MAJOR 之间插入一级，所有 `>=` 判定都会
静默改变含义。

### 19.2 零覆盖模块补测试

核对了 12 个无独立测试文件的模块，其中 11 个被其他测试间接链接覆盖，只有
`alarm_lifecycle_bridge` 完全零覆盖。它虽只有 30 行，但决定 session journal 何时
开关，而 journal 决定洗完后哪些 MAJOR 报警把设备推入 EXCEPTION——错了的现场表现
是"洗完车该停机却没停"。

`tests/application/test_alarm_lifecycle_bridge.c` 覆盖 9 个场景，含会话未开始不记
日志、新会话清空上轮、MINOR 不记录、重复触发去重、清除后仍保留。已验证漏订阅
`EVT_WASH_DONE` 会导致测试失败。

### 19.3 容量常量补实测依据

原先 `EVENT_BUS_QUEUE_SIZE` 等是拍定的数。`tests/runtime/test_event_bus_capacity.c`
按 M8 最密的 10ms 周期档压测，实测：

| 项目 | 实测峰值 | 容量 | 余量 |
|------|----------|------|------|
| 普通队列稳态 | 9 | 64 | 86% |
| 高优先级稳态 | 0 | 16 | 100% |
| 单事件订阅者 | 4 | 8 | 50% |

另外验证了两个设计前提确实成立：慢 handler 阻塞期间普通队列能完整填到 64 才开始
拒绝（有效容量等于标称容量，不是提前失效），且此时高优先级队列仍能完整接受 16 个
安全事件（双队列隔离有效）。

依据已写入 `event_bus_config.h` 与 `alarm_types.h` 的头部注释，调整容量时需同步
更新表格与测试断言门限。

### 19.4 错误语义分类

`SW_ERR_PARAM` 占全部返回的约一半（192 处），被当作通用失败码，调用方无法据此
判断该重试、降级还是终止。现在 `common/sw_error.h` 提供三类判定 helper
（瞬时可重试 / 调用方缺陷 / 接入缺失），并说明 `SW_ERR_PARAM` 应收窄到"入参本身
不合契约"。

两个从未使用的枚举（`SW_ERR_UPGRADE` = -11、`SW_ERR_NOT_SUPPORT` = -13）**未删除**：
错误码数值经 protobuf 跨进程传给观测进程（`observation.proto` 的 `effect_error`），
删除中间值会让后续值前移，观测侧解读出的错误原因全部错位。现改为保留数值并标注
废弃，`tests/common/test_sw_error.c` 断言全部数值不变以拦住这类改动。

### 19.5 资产版本校验

原先只有方案与观测记录有 schema version，且方案用 `strcmp("1.0")` 精确匹配——
`"1.0.1"` 这类合法资产会被拒，也无法表达"接受同主版本的较低次版本"。

`common/asset_version.h` 提供通用语义化版本解析与兼容判定（主版本不同不兼容、
资产次版本高于框架不兼容、修订号不参与判定），方案加载器已改用它。云物模型、
报警目录、HAL binding、水路拓扑可按同一 helper 逐步接入，资产本身仍留项目侧。

刻意不做"未知字段自动忽略"：对报警目录、水路拓扑这类安全相关资产，静默忽略
未识别字段等于让缺失的约束通过校验。

## 20. 急停热路径整治记录

本节记录第三轮评估后的改动。上两轮分别解决复用性/层归属（第 18 节）与安全形式化
（第 19 节），本轮只针对一条链路：硬件急停触发后的切断路径。

选这条链路的原因是它同时命中两类此前未查透的缺陷，而且都在同一段代码里：
第 17 节修优先级反转时只追到 `event_bus` 与 `log`，没追到 cutout 往下的 HAL 层；
第 19 节做安全矩阵时形式化了"报警等级到姿态"的映射，但没管"切断本身失败了怎么办"。

### 20.1 切断路径上残余的优先级反转

`estop_poll_thread` 以 `SCHED_FIFO` / `THD_SAFETY_THREAD_PRIO` 运行，
`safety_cutout_execute()` 往下会取三处**裸 `PTHREAD_MUTEX_INITIALIZER`**：

| 锁 | 位置 | 竞争方 |
|----|------|--------|
| `s_output_mutex` / `s_input_mutex` | `io_exp_driver.c`（真机 DO 写） | CAN 刷新、输入采样周期任务 |
| `s_do_mutex` / `s_di_mutex` | `hal_io_sim.c`（sim DO 写） | 同上 |
| `s_vfd_lock` / `s_control_lock` | `hal_vfd_manager.c`（电机 cutoff） | 变频器轮询、通信保活 |

六把锁全部改为优先级继承。`fluid_path_emergency_off` 已核查为纯原子写，不涉及锁，
不在此列。

同时把第 17 节各自手写的初始化收口为 `common/sw_mutex.h` 的
`sw_mutex_init_prio_inherit()`。原先 `event_bus`（两把）与 `log`（一把）各有一份
约 20 行的相同代码，本轮再加六把会变成五份副本，任何一份漏掉
`setprotocol` 失败分支都不会有编译期提示。

不提供"带静态初始化的包装类型"：静态初始化出来的互斥量已经可用，再对它调用
`pthread_mutex_init` 是 POSIX 未定义行为，因此无法"先静态初始化、事后升级为
优先级继承"。三个 HAL 模块都没有可靠的集中初始化时机（`hal_io_sim` 与
`hal_vfd_manager` 根本没有 init 入口，`io_exp_driver` 的 getter 允许在
`drv_io_init` 之前被调用），故沿用 `pthread_once` 在首次上锁前初始化。

已用独立探针确认本平台确实启用了继承而非走退化分支（`helper_applied=1`）。

### 20.2 切断失败不可见（第 8 节遗留验收项）

原先 `safety_ops_t.cutout` 返回 `void`，M8 实现 `m8_safety_cutout` 把每一路
`safe_do_set` 的返回值都 `(void)` 丢弃，`m8_assert_safe_outputs` 同样丢弃。
后果是急停切断失败时：无日志、无计数、无报警，框架侧完全无从知晓——而这正是
最需要留下现场证据的时刻。

现改为逐层汇总：

| 层 | 变化 |
|----|------|
| `safety_ops_t.cutout` | 返回 `sw_err_t`；契约要求「尽力做完全部切断动作再返回」，不得在首个失败处提前返回 |
| `safety_cutout_execute` | 返回错误码，失败即记 ERROR 并累加 `safety_cutout_failure_count()` |
| `m8_safety_cutout` | 汇总各路 DO 写失败，返回首个错误码并记录失败路数 |
| `m8_assert_safe_outputs` | 同上，且纳入 `flush_outputs_now` 的失败 |
| `m8_boot_profile_init` | 上电置安全态失败改为**拒绝启动**（原先无条件返回 `SW_OK`） |

三条设计取舍：

- **不在急停热路径重试。** 重试会延长动力输出未确认切断的窗口，且失败原因通常在
  硬件链路本身（总线离线、板卡无响应），重试无从改变。
- **切断失败不阻断后续事件发布。** `EVT_HW_ESTOP_ON` 必须照常送出，否则领域层不会
  进入急停态，故障会同时丢掉切断与状态收敛两条路径。这条性质由
  `test_cutout_failure_still_publishes_event` 锁定，已注入「失败即 return」的
  回归验证过该用例确实会失败。
- **失败计数不做首次节流。** 与"未注册"的 `warn_once` 不同：未注册是接入错误，
  一次就够；切断失败是运行期硬件故障，每一条都是判因证据。未注册导致的"未执行"
  返回 `SW_ERR_NOT_INIT` 且**不计入**失败计数，两者语义分开。

`m8_boot_profile_init` 改为可拒绝启动是行为变更：上电无法把输出置安全态说明 IO
链路本身不可信，继续启动会让设备带着未知输出状态进入运行，且后续所有安全动作都
建立在同一条不可信链路上。bootstrap 的 `BOOT_CHECK` 会据此终止进程。

## 21. 验收项与代码一致性核查

前几轮连续出现"文档声称已处理、代码实际未做"（第 20 节的切断失败上报、第 9 节的
规则条数、第 16 节的 thread_registry 状态），共同点是都靠临时发现而非系统核对。
本节把第 5 到第 14 节的验收项逐条拿到代码里验了一遍。

核查方法：每条验收项对应一次代码检索，只认「能指到具体实现或调用点」为达成。

### 21.1 表格声称已完成、但缺少落地情况小节的三节

核查时，第 6、7、8、9 节都有「落地情况」小节写明范围，第 5、10、11 节没有——
正文仍是未实施的提案口吻，读者只能从第 4 节表格的一句状态推断实际范围。这三节
是本次核查的重点，也是最容易再次失真的地方。第 10、11 节已在核查当轮补上该小节，
第 5 节的落地情况小节与其资产侧缺口已在后续一轮补齐（见第 5 节「落地情况」），
下表保留核查当时的实况作为对照。

**第 5 节 项目接入契约检查**（核查时表格状态：已完成（端口部分））

| 验收项 | 实况 |
|--------|------|
| 必需 port 是否已注册 | 达成，`port_contract_validate` 由两个 wiring 的 `project_validate` 调用 |
| `alarm catalog` 是否已加载 | **未做**，全仓零处检查 |
| `cloud model` 是否通过校验 | 部分，仅 target 调 `cloud_model_validate()`，sim 不调 |
| `engine_program_loader` 是否注册 | 达成，`PORT_REQ_PROGRAM_LOADER` |
| `engine_io` 和 IO catalog 是否符合项目选择 | 部分，只有三个 smoke 判非空，`project_validate` 不查 |
| HAL provider 与设备控制 binding | 达成，`PORT_REQ_HAL_*` / `PORT_REQ_MACHINE_OPS` |
| 配置资产和 manifest 版本匹配 | 核查时仅方案资产接入；**已补 deploy config**，且已查明其余四类属编译期资产、不适用，见第 11 节 |
| 测试覆盖缺失 port / 缺失资产 / 非法 binding 三场景 | 缺失 port 与非法 binding 达成，**缺失资产场景不存在** |

结论：表格的「端口部分」措辞准确，但正文缺少落地情况小节，且资产侧那半始终
没有单独立项。资产校验是这一节剩余工作的实际内容。

**已闭环**：后续一轮补 `application/asset_contract.h`（3 个 `ASSET_REQ_*` 位）与
`tests/application/test_asset_contract.c`，上表标「未做 / 部分 / 不存在」的四行
全部达成，第 4 节表格状态改为「已完成」。

**第 10 节 错误模型统一**（表格：已完成）

| 验收项 | 实况 |
|--------|------|
| 有统一错误语义说明 | 达成，`common/sw_error.h` |
| 关键模块错误返回与说明一致 | 核查时**零生产调用方**，只有测试在用；**已修复**，见第 10 节落地情况 |
| 可重试与不可重试边界明确 | 达成，helper 已定义边界 |

结论：核查时这一节是「语义已定义、尚未被消费」。已接入 `report_scheduler` 两处
上报路径，同时确认 `drv_voice` 与 `hal_vfd_manager` 的具体码判断不应替换——
详见第 10 节。表格状态已从「已完成（语义定义，未接入）」回到「已完成」。

**第 11 节 配置与资产版本化**（表格：已完成（通用 helper））

| 验收项 | 实况 |
|--------|------|
| 通用框架提供版本校验 helper | 达成，`common/asset_version.h` |
| 版本不兼容时启动期失败 | 核查时仅 engine program 接入；**已补 deploy config**，见第 11 节 |
| 项目资产仍保留项目侧 | 达成 |

结论：表格加了「通用 helper」限定，措辞诚实。

**本节初版的一处判断有误，在此更正**：初版写「剩余是把云物模型、报警目录、
HAL binding、水路拓扑四类接上同一 helper」，隐含了这四类都需要版本校验。接入时
查证发现它们全部是编译期结构体或数组，与框架编译进同一个二进制，不存在版本漂移
的可能——结构体定义变了编译期就失败，比运行期比对版本串更强。真正需要版本的是
「能独立于框架二进制变化」的资产，六项里只有 engine program 与 deploy config 两项
符合，后者已在本轮接入。判据与逐项结论见第 11 节落地情况。

这也说明核查本身会犯同一类错误：把文档的清单当成需求来核，而没有先问清单是否
成立。核查的第一步应当是验证验收项自身的前提。

### 21.2 标「未开始」的三节，与代码一致

| 节 | 抽查项 | 实况 |
|----|--------|------|
| 12 HAL provider 边界收紧 | 边界检查是否覆盖 provider 目录 | 未覆盖（脚本零处提及 `providers`），与「未开始」一致 |
| 13 表达式与 profile 可控扩展 | 未知函数是否在加载期发现 | 核查时在**求值期**才发现：`eval_node` 用 `strcmp` 逐个比函数名，未知名与参数个数不符都只在 `NODE_FUNC` 求值时置 `*ok = false`，与「未开始」一致。**已改**：后续一轮补白名单表与 `check_functions`，三类错误在 `engine_expr_compile` 拒绝并点名函数，见第 13 节落地情况 |
| 14 状态投影一致性策略 | 模块设计是否有一致性等级 | 文档存在但仅两处提及「一致性」，未形成等级定义。与「未开始」一致 |

这三节没有失实。

### 21.3 结论

核查当时：七节里三节的表格状态与代码基本吻合（第 12、13、14 节），三节需要补落地
情况小节（第 5、10、11 节），其中第 10 节的「已完成」应改为「已完成（语义定义，
未接入）」以反映零调用方的事实。

此后三节的缺口均已补齐：第 10 节接入 `report_scheduler`，第 11 节接入 deploy config，
第 5 节补齐资产侧校验（`application/asset_contract.h`）。第 10 节的表格状态在接入
后回到「已完成」。至此第 4 节表格中不再有「已完成但无落地情况小节」的条目。

一个可复用的判据：凡是「提供了 helper / 定义了语义」类的工作，达成标准应当是
**至少一个生产调用方**，否则它对运行时行为没有任何约束力，与未做的区别只在于
将来接入更省事。第 10 节与第 11 节都属于这一类。

## 22. 目录环与命名一致性整治记录

本节记录第四轮评估后的改动。前几轮聚焦运行可靠性与安全形式化，本轮针对的是
**分层与命名的可判定性**：让"目录能否单独提取"和"保护宏是否跟得上目录"这两件事
从人工 review 转为脚本判定。

### 22.1 安全端口五头合一

`ports/outbound/safety/` 下曾有 5 个头文件表达同一组能力：`safety_port.h` 是注册
入口（`safety_ops_t` 四字段全必填），另 4 个（`safety_cutout_port.h`、
`safety_deferred_stop.h`、`hw_estop_port.h`、`op_mode_alarm_port.h`）各只声明一个
包装函数，且全部由 `port_registry_safety.c` 实现。

这是弱符号时代的残留：那时它们确实是 4 个独立的链接期可替换符号，第 18.2 节改为
注册表后就只剩 4 个转发。消费者也印证了这一点——除注册表与测试外，每个头各只有
1 个真实调用点。

现合并进 `safety_port.h`（含 `safety_cutout_failure_count`），5 个头减为 1 个。

合并同时修掉两处**已经失实**的注释：`hw_estop_port.h` 与 `op_mode_alarm_port.h`
都还写着「项目层提供强符号实现；框架默认弱符号返回 false」，而弱符号早已不存在。
这类注释比缺注释更危险——它会让读者按已废弃的机制去接入。同样过时的表述在
`module-design/runtime/Runtime模块设计.md` 与 `module-design/domain/报警系统模块设计.md`
中各有一处，一并改正。

代价是 `op_mode_bridge.c` 只需 `alarm_is_estop` 却会看到整个 `safety_ops_t`。
可接受：该头只依赖 `sw_error.h` + `stdbool` + `stdint`，无传递性负担。

### 22.2 cloud 与 application 的目录环

**问题**：`cloud/` 与 `application/` 互相依赖——

```
cloud/cloud_model.h            → application/orchestrators/report_scheduler.h
application/orchestrators/report_scheduler.c → cloud/cloud_point_watcher.h
```

不是编译单元环（两个 `.o` 不互相依赖），但是**目录级双向依赖**：两个目录谁都无法
单独提取。而当时 R1~R10 全部通过，原因见 22.3。

**方向判定**：`cloud/` 位于 `application/` 之下。依据是 `cloud/` 只提供点位模型与
编解码能力，不订阅事件、不注册周期任务；而 `report_scheduler` 两者都做，属编排。
故保留 `application → cloud`，砍掉反向。

**做法**：把上报策略从物模型注册包摘出，由项目直接注册到调度器。

| 改动 | 说明 |
|------|------|
| `cloud_model_bundle_t` 删 `report_policies` / `policy_count` | 策略是调度器的输入格式，不属物模型 |
| 删 `cloud_model_register_scheduler()` / `cloud_model_request_resync()` | 两者合计仅三行转发 |
| 内部 `model_point_id_by_index` 提为公开 `cloud_model_point_id_by_index()` | 项目注册 resolver 时需要 |

`report_policy_entry_t` **未搬家**：它是调度器的输入格式，留在
`application/orchestrators/report_scheduler.h` 是正确归属。为三行转发让整个 `cloud/`
反向依赖 `application/`，代价与收益不成比例。

**未采用的替代方案**：把 `report_scheduler.c` 整体搬进 `cloud/`。同样能断环且更
"云内聚"，但会让 `cloud/` 变成含编排的层，与 R10 的判据（bridge / projection /
coordinator 属 application）自相矛盾，且 `cloud/` 就不再位于 application 之下。

CMake 侧同一处失真也已修正：`wdf_cloud` 原声明 `DEPENDS wdf_common wdf_ports`，
但 `cloud_point.h` 需要 `domain/`、`cloud_point_watcher.c` 需要 `runtime/`。因为
include 根是仓库根，**缺失的依赖声明不会报错**——目标声明此前不是可信的依赖描述。

### 22.3 上层出向依赖规则（R11~R14）

上一节那个环能长期存在的根因：R1~R10 **只约束 `common`/`domain`/`ports`/`runtime`
的出向依赖**。`application`、`cloud`、`services`、`observability` 的出向没有任何
规则，它们只作为"禁止被引用的上层"出现在别人的规则里。

补四条，允许集按实际需要确定，不做超出现状的收紧：

| 规则 | 约束 |
|------|------|
| R11 | `application/` 不依赖 `adapters/`、`services/` |
| R12 | `cloud/` 不依赖 `adapters/`、`application/`、`services/` |
| R13 | `services/` 不依赖 `adapters/`、`application/`、`cloud/` |
| R14 | `observability/` 只依赖 `common/` |

R14 最严是有依据的：`architecture/01` 称 `observability` 为旁路设施——任何层都可
向它发布记录、它不回调任何层、因此不构成环。这条性质**只有在它除 `common` 之外
什么都不依赖时才成立**，所以规则逐一排除其余各层。

四条规则均以注入违规的方式验证过确实能捕获。R12 尤其做了针对性验证：把
`cloud_model.h` 对 `report_scheduler.h` 的 include 加回去，R12 立即失败并指到
`cloud/cloud_model.h:15`。

### 22.4 保护宏与路径一致（R15）

核对发现 **67 个头文件**的保护宏与路径不符，分三类：

| 类型 | 例子 | 后果 |
|------|------|------|
| 撞名风险 | `DRV_VFD_H`、`TIME_UTIL_H`、`SW_ERROR_H`、`UTIL_FIFO_H` | vendor SDK 出现同名宏时，后包含的头被静默跳过，报"类型未定义"而非重复定义，排查成本远高于改名成本 |
| 路径失同步 | `CORE_BOOTSTRAP_BOOTSTRAP_H`（`core/` 是旧目录名）、`CONFIG_THREADING_THREAD_CONFIG_H`（目录从未叫 `threading`） | 读者据宏名找不到对应目录 |
| 层级缺失 | `ADAPTERS_HAL_SIM_HW_HAL_IO_SIM_H` 缺 `OUTBOUND`、多余 `HW_`；`DOMAIN_MODEL_ALARM_TYPES_H` 缺 `SAFETY` | 同名文件在不同层时可能撞名 |

`DRV_VFD_H` 这类尤其值得注意——变频器 SDK 里出现同名宏完全不意外。

改写前先确认了两件事：全部保护宏只被自身 `#ifndef` 引用，无 `#ifdef`/`defined()`
形式的条件编译依赖，故重命名安全；绝大多数头文件是 CRLF 行尾，改写须保留行尾，
否则会产生全文件 diff 掩盖真实改动。

**这项的主要产出是 R15 而非改名本身。** 67 处不一致全是"目录搬过、宏没跟"的遗留；
只改一次而不加规则，下次搬目录同样不会跟，等于把同一笔债重新记一遍。

R15 的判据设计：只取文件中第一个 `#ifndef`，且要求其后紧跟同名 `#define`。这样
`#ifndef TRUE`（`sw_types.h`）与 `#ifndef SW_LOG_COMPONENT`（`log.h`）这类条件定义
不会被误判为保护宏——已验证这两个文件不出现在违规列表里。

同时修正 `runtime/config/thread_config.h` 注释里同样过时的 `core/scheduler` 表述。

### 22.5 本轮未做及原因

| 项 | 原因 |
|----|------|
| 拆散 `cloud/`（828 行，混了编解码/校验/注册三种职责） | 打散成 domain + application + adapters 三层只为架构纯度，且要动项目侧 include 与 CMake。框架文档已明确不以纯 DDD 为目标 |
| `deploy_store.h` / `param_store.h` 补 `_port` 后缀 | 文件改名波及约 8 处 include，收益仅是命名整齐 |
| `services/` 归并（193 行、1 个生产消费者成一层） | 攒到下次动该模块时一起做，单独做纯属制造 diff |
| `application/` 扁平与子目录混排整平 | 同上。`recovery_service` 与 `orchestrators/` 下两个协调器职责同族却分处两级 |
| `adapters/runtime/snack/` 改名（与顶层 `runtime/` 撞概念） | 同上 |
| `op_mode` / `operational_mode` 命名统一、`svc_` 前缀 | 同上 |
| 框架统一 `wdf_` 符号前缀（当前 `wdf_` 只在 CMake 目标名上，C 符号是裸名） | 波及全部公开符号，收益只在接第二个项目时兑现 |
| 线程模型（每周期任务一线程，约 13 个） | 仍无实测抖动或 CPU 痛点，与前三轮判断一致 |

### 22.6 遗留观察

两处定位问题本轮只记录、未处理，它们需要的是决策而非改代码：

- **`cloud/` 顶层定位可疑。** 它不是一个层，是按"云"这个话题横切出的包。
  要么拆开，要么在文档里明确它是话题包而非层。

  > **本条的职责划分判断有误，且已在第 23 节处理。** 原文称
  > 「`cloud_point_dispatch.c` 做 JSON 编解码、`cloud_point_validate.c` 是纯逻辑、
  > `cloud_model.c` 是注册入口」——三层职责。实读代码后：编解码在
  > `common/point_table/`，`cloud_point_dispatch.c` 只是转调，自己用 cJSON 仅为
  > 遍历顶层 key；它的真实职责是**语义分派**（按 semantic 路由到设备命令 /
  > 云服务 / set，并拒绝只读点位），属领域逻辑。故实际是两层而非三层，且两层
  > 都归 domain。「顶层定位可疑」的结论成立，但依据不是原文写的那个。
- **`observability/` 框架生产路径零接入。** `runtime/`、`application/`、`demo/` 无任何
  引用，只有自身与测试在用；而项目侧有 10+ 文件在用。当前它是"框架提供、框架不用、
  项目在用"的库，其正确性只由单测保证，框架自身链路从未验证过它（`blackbox_recorder`
  尤其如此）。定位要么明确写成旁路库，要么框架自己接入一条链路。

## 23. 序列化分层与云子域归位

本节记录第五轮改动。前几轮解决可靠性、安全形式化与命名一致性，本轮针对一条
贯穿三层的耦合：**最底层绑定了一种序列化格式**。

### 23.1 问题的实际形状

第 22.6 节把 `cloud/` 记为"混了三层职责"，实读后不成立（该条已就地更正）。真正
的问题在更下面一层：

`common/point_table/point_table.h` 自己 include cJSON，而 R1 特批 `common/` 依赖
`third_party/`。这意味着最底层绑定了 JSON，上面每一层都能传递性地碰到它——于是
「domain 不解析序列化格式」（R9b）变成一条**孤立的禁令**：禁 domain，却不禁
domain 所依赖的 common。

`cloud/` 之所以无法干净归位，也是这条耦合的下游表现：它要用点位表，就必然连上
cJSON；而 R9b 禁止 domain 用 cJSON，所以它只能留在顶层。

### 23.2 三步拆解

**第一步：point_table 拆模型与编解码。**

| 位置 | 内容 |
|------|------|
| `common/point_table/point_table.h` + `.c` | 点位表模型、结果汇总、查表。零外部依赖 |
| `adapters/outbound/serialization/json/point_table_json.*` | JSON 编解码 |

`point_apply_result_init` / `record_error` / `find_entry_at` / `find_entry` 四个
函数与 JSON 无关，原先却在 `point_table_from_json.c` 里——若随编解码一起搬走，
`common/` 就会声明一批只有 `adapters/` 才提供的符号。故新建 `point_table.c` 承载。

CMake 侧 `wdf_common` 不再传递 `wdf_cjson`，编解码独立为 `wdf_point_table_json`。
副作用是**不接云、不用 JSON 存储的项目现在可以完全不链接 cJSON**。

**第二步：物模型的 JSON 门面移出 domain。**

`cloud_point_dispatch.c` 原先一身两职：JSON 出入口 + 语义分派。现拆为

| 文件 | 层 | 职责 |
|------|----|------|
| `domain/cloud/cloud_point_dispatch.c` | domain | `cloud_point_apply_value()`：按 semantic 分派已解析的值，拒绝只读点位 |
| `adapters/outbound/cloud/cloud_point_json.c` | adapters | 属性 JSON 构建 / 解析，解析后逐个交给上面那个函数 |
| `adapters/outbound/cloud/cloud_model_json.c` | adapters | `property_port` 安装 + 全量/增量属性构建 |

**`property_port` 的安装从 `cloud_model_register()` 移到 `cloud_model_json_install()`** ——
该端口的契约参数是 JSON 载荷字符串，实现它必须解析 JSON，因此安装动作属适配层。
这是一处行为变更：注册物模型后须另调 install 才有属性下行，单元测试与 M8 wiring
均已同步。`cloud_model_bundle_t` 也因此去掉 `property_reply` 字段。

`cloud_model_json.c` 不持有点位表，运行期从 `cloud_model_entries()` 取——避免与
`cloud_model.c` 出现两份可能不一致的引用。

**未改端口签名。** 原计划把 `on_property_set` 从收 JSON 字符串改为收已解析值。
放弃的理由：改完后每个 cloud provider 都要各自解析属性载荷，JSON 结构会在多个
provider 间分叉，而现在两端（snack inbound 调用、`cloud_model_json` 实现）都在
adapters，domain 已完全不参与序列化，目标已经达成。

**第三步：`cloud/` 迁入 `domain/cloud/`。**

前两步做完后 `cloud/` 零 cJSON、出向只剩 `common` / `domain` / `runtime`，完全在
domain 的许可集内，搬迁没有阻碍。顶层目录从 9 个减为 8 个。

### 23.3 收益：约束从目录名巧合变为层归属决定

搬迁的实质价值不是目录整齐，而是 **R9b 现在自动覆盖云子域**。已注入验证：往
`domain/cloud/cloud_point_dispatch.c` 加一行 `#include cJSON`，R9b 立即失败并指到
该文件。此前同样的代码放在顶层 `cloud/`，任何规则都不会拦。

R12（"cloud/ 不依赖 adapters/application/services"）随之退役——它约束的目录已并入
`domain/`，由 R2 覆盖。R13 去掉 `cloud/` 前缀。**编号不重排**：R12 作为已废止编号
留空，避免历史记录里的规则编号与当前脚本对不上。本项使规则数减一（当前总数以
脚本结尾输出为准，其间另有他轮新增规则）。

### 23.4 一处判断修正

第 22.5 节曾把"拆散 `cloud/`"列为不做，理由是「打散成三层只为架构纯度，且要动
项目侧 include 与 CMake」。这个判断在当时的信息下是合理的，但它假设了拆分只有
纯度收益。实际做完后发现有两个实质收益：R9b 覆盖范围扩大（见 23.3），以及
`common/` 摆脱 cJSON 使最小接入不必链接 JSON 库。

代价也确实如当时估计：M8 侧改了 4 个文件的 include 与 CMake 目标清单。判断失误
不在代价估算，而在收益识别——**当时没有意识到 `common/` 的 cJSON 依赖是同一个
问题的根因**，只看到了 `cloud/` 这个下游表现。

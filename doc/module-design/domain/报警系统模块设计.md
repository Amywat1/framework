# 报警系统模块设计

**版本**：v1.1  
**状态**：已落地（领域核心 + 事件桥 + ON_MOTION 重评估桥 + 姿态边沿 + 投影；项目 detector 由项目侧实现）  
**最后同步代码**：2026-07-17（删除 `response_strategy` / `safety_posture` 独立模块 / overflow 元告警 / `estop_skip` / `detector_helper` / `BATCH_CLEARED`；MAJOR 洗后评估；保留 session journal）  
**适用范围**：`domain/safety/`、`application/bridges/alarm_*`、`ports/inbound/safety/`、`ports/outbound/safety/`；项目侧 detector 与报警目录  
**架构基线**：`doc/module-design/domain/命令网关模块设计.md`、`doc/module-design/runtime/EventBus模块设计.md`  
**关键词**：AlarmRegistry、alarm_binding_port、alarm_event_bridge、`EVT_ALARM_*`、`EVT_SAFETY_*`

---

## 1. 设计目标与核心理念

报警子系统把硬件/流程/通讯异常转化为**可分级、可清除、可查询**的活跃报警实例，并通过事件总线驱动安全姿态与运行模式收敛。

### 1.1 设计目标

- **单一事实源**：活跃报警列表、会话 journal、pending 域事件均由 `alarm_registry` 维护。
- **端口隔离**：项目 detector 经 `alarm_binding_port` 触发/清除，不直接 `#include` registry 实现。
- **异步广播**：registry pending 经 `alarm_event_bridge`（50ms 周期 drain）转为 `EVT_ALARM_*`，并在边沿发布 `EVT_SAFETY_*`。
- **重评估桥接**：ON_MOTION 清除经生命周期事件与项目 binding 表映射到 `reeval_group`。
- **与运行模式解耦**：等级语义在报警域；模式转移由 `op_mode_bridge` / `operational_mode` 消费。

### 1.2 三条独立轴（等级驱动行为）

| 轴 | 回答问题 | 权威模块 |
|----|----------|----------|
| 报警等级 `alarm_level_t` | 严重度 + 运行时处置 | `safety_matrix.h` 行为属性表 |
| 安全姿态 `safety_posture_t` | 是否 LOCKOUT | `alarm_registry_safety_posture()`（CRITICAL 推导） |
| 运行模式 `operational_mode_t` | 允许哪些命令 | `operational_mode` |

等级语义：

| 等级 | 行为 |
|------|------|
| `MINOR` | 仅记录，不挡开洗 |
| `MAJOR` | blocking：禁开洗；洗中不中断；洗完仍活跃 → `EXCEPTION` |
| `CRITICAL` | LOCKOUT：立刻停机（经 `EVT_SAFETY_LOCKOUT`） |

清除策略仍独立：`AUTO_STATIC` / `ON_MOTION` / `MANUAL_RESET`。

上述两组语义都以**行为属性**表达，单一事实源是 `domain/safety/model/safety_matrix.h`；`alarm_registry` 的全部等级与策略判定都查这张表，不再各处写 `level >= MAJOR` 这类内联判断。未知等级取最严格值、未知策略不自动清除——漏配置若默认放行，现场表现是"有报警却照常开洗"或"安全状态被未定义行为解除"，比误停机危险得多。完整格点见 `doc/architecture/05-安全与报警架构.md` §3。

### 1.3 端到端事件链

```text
  项目 detector / HAL / CommWatchdog
        │ alarm_binding_port.trigger/clear
        ▼
  alarm_registry（活跃列表 + session journal + pending）
        │ alarm_event_bridge_drain()（50ms 周期）
        ▼
  EVT_ALARM_TRIGGERED / CLEARED
        ├─► 姿态边沿 → EVT_SAFETY_LOCKOUT / NOMINAL（同桥内）
        ├─► telemetry_projection
        └─► op_mode_bridge → op_mode_on_*

  命令 / 会话路径（正交）：
  START_WASH           → 存在 blocking 报警则拒绝
  WASH 正常结束        → 仍 blocking 则 EXCEPTION，否则 WASH_DONE
  DEV_CMD_RECOVER      → 复位已恢复的锁存告警 + home + blocking 验证
```

---

## 2. 分层归属与依赖方向

### 2.1 模块落位

| 层次 | 路径 | 职责 |
|------|------|------|
| **领域聚合** | `domain/safety/alarm_registry/` | catalog、active 池、journal、pending |
| **类型契约** | `domain/safety/model/alarm_types.h` | 码值、等级、清除、实例、域事件、`safety_posture_t` |
| **事件桥** | `application/bridges/alarm_event_bridge.*` | pending → `EVT_ALARM_*` + 姿态边沿 → `EVT_SAFETY_*` |
| **重评估桥** | `application/bridges/alarm_reeval_bridge.*` | lifecycle → `reevaluate_group` |
| **会话生命周期** | `application/bridges/alarm_lifecycle_bridge.*` | 洗车会话起止开关 journal |
| **入站端口** | `ports/inbound/safety/alarm_binding_port.h` | trigger/clear/load_catalog |
| **出站端口** | `ports/outbound/safety/safety_port.h` | 急停码识别（项目注册 `safety_ops_t`） |

### 2.2 依赖方向

```text
项目 detector / watchdog（项目仓库）
        │ alarm_binding_port
        ▼
   domain/safety/alarm_registry
        │ pull_events
        ▼
   application/bridges/alarm_event_bridge → event_bus
        │
        ├─► EVT_SAFETY_*（桥内边沿）
        └─► op_mode_bridge / telemetry_projection
```

**依赖禁令**：

- `alarm_registry` 不得依赖 MQTT、云端或 `operational_mode`。
- `alarm_event_bridge` 不得解析机型业务，只做 pending 映射与姿态边沿。
- detector 不得绕过 port 直接写 registry 静态状态。

### 2.3 启动顺序契约

```text
bootstrap_bind()
    alarm_registry_init()           → 注册 alarm_binding_port
    project_bind_alarm_catalog()    → load_catalog + reeval_bridge_init
    ↓
bootstrap_init_services()
    alarm_event_bridge_init()       → 登记 50ms drain 周期任务（含姿态边沿）
    operational_mode_init()
    …
    alarm_lifecycle_bridge_init()
    op_mode_bridge_init()
    telemetry_projection_init()
    project_init_adapters()         → DI 防抖 / CommWatchdog
    ↓
bootstrap_start()
    project_start_runtime()         → alarm poll 线程等
    scheduler_start_all()
```

---

## 3. 文件清单

| 文件 | 职责 |
|------|------|
| `domain/safety/alarm_registry/alarm_registry.{h,c}` | 聚合根 |
| `domain/safety/model/alarm_types.h` | 类型、容量常量与码 helper |
| `domain/safety/model/safety_matrix.h` | 等级与清除策略的行为属性单一事实源 |
| `application/bridges/alarm_event_bridge.*` | pending → 总线 + 姿态边沿 |
| `application/bridges/alarm_reeval_bridge.*` | ON_MOTION 重评估 |
| `application/bridges/alarm_lifecycle_bridge.*` | 会话 journal 窗口 |
| `ports/inbound/safety/alarm_binding_port.h` | 入站 port |
| `ports/outbound/safety/safety_port.h` | 急停码判定 |
| `tests/domain/test_alarm_registry.c` | registry 单测 |
| `tests/domain/test_safety_matrix.c` | 等级/策略行为属性逐格断言与单调性不变量 |
| `tests/domain/test_safety_posture.c` | 姿态边沿（经 event_bridge） |
| `tests/application/test_alarm_event_bridge.c` | bridge 单测 |
| `tests/application/test_alarm_lifecycle_bridge.c` | 会话 journal 窗口 |
| `tests/application/test_alarm_reeval_bridge.c` | reeval 单测 |
| `tests/ports/test_op_mode_alarm_port.c` | 急停码判定端口 |

项目侧需自备：detector 实现、通讯 watchdog、报警码目录与 ON_MOTION binding 表。这些都是项目资产，不在本仓。

---

## 4. 数据模型

### 4.1 报警定义（alarm_def_t）

| 字段 | 说明 |
|------|------|
| `code` | 6 位报警码（`ALARM_CODE_MAKE`） |
| `level` | MINOR / MAJOR / CRITICAL |
| `clear` | AUTO_STATIC / ON_MOTION / MANUAL_RESET |
| `reeval_group` | ON_MOTION 重评估分组；非 ON_MOTION 填 `ALARM_REEVAL_GROUP_NONE` |
| `desc` | 描述 |

### 4.2 报警码编码

```text
报警码 = 大类 category(1) + 编号 index(3) + 性质 nature(2)
```

范围：category `1..9`，index `0..999`，nature `0..99`。  
通用大类/性质常量见 `alarm_types.h`；具体码表由项目目录提供。

### 4.3 域事件

| kind | 桥接为 |
|------|--------|
| `ALARM_DOMAIN_EVT_TRIGGERED` | `EVT_ALARM_TRIGGERED`（param=code） |
| `ALARM_DOMAIN_EVT_CLEARED` | `EVT_ALARM_CLEARED` |

实际删除活动告警时，对每条告警发独立 `CLEARED`，无 `BATCH_CLEARED`。

### 4.4 安全姿态

| 值 | 推导 |
|----|------|
| `SAFETY_POSTURE_NOMINAL` | 无 CRITICAL 活跃 |
| `SAFETY_POSTURE_LOCKOUT` | 存在 CRITICAL 活跃 |

由 `alarm_registry_safety_posture()` 计算；`alarm_event_bridge` 在 drain 后检测边沿并发布 `EVT_SAFETY_LOCKOUT` / `EVT_SAFETY_NOMINAL`。

### 4.5 容量与满池

| 宏 | 默认 | 说明 |
|----|------|------|
| `ALARM_CATALOG_MAX` | 64 | 静态目录 |
| `ALARM_ACTIVE_MAX` | 32 | 活跃池 |
| `ALARM_PENDING_EVENT_MAX` | 32 | pending |
| `ALARM_SESSION_JOURNAL_MAX` | 16 | 洗车会话 journal |

活跃池满时：打日志并返回 `SW_ERR_OVERFLOW`（无元告警、无驱逐）。

### 4.6 会话 journal

洗车会话期间触发的 MAJOR+ 码记入 journal，供后续云/CLI/业务消费；**不参与**洗后停机布尔判定（判定用当前 `alarm_registry_has_blocking_active()`）。

---

## 5. 核心模块行为契约

### 5.1 alarm_registry

下表 API 名省略 `alarm_registry_` 前缀，实际符号为 `alarm_registry_<名>`。

| API | 行为要点 |
|-----|----------|
| `load_catalog` | 重置 active/pending |
| `trigger` | 表示故障条件成立；未知码 `SW_ERR_PARAM`；已 active 时刷新条件状态；池满 `SW_ERR_OVERFLOW` |
| `clear` | 表示故障条件消失；AUTO_STATIC 立即删除，MANUAL_RESET 保持锁存，ON_MOTION 等待重评估 |
| `reset_all` | 仅删除故障条件已消失的 MANUAL_RESET / ON_MOTION；条件仍成立时复位无效 |
| `reevaluate_group` | 对匹配的 ON_MOTION 告警确认条件消失并删除 |
| `has_blocking_active` | 存在 level ≥ MAJOR |
| `get_session_journal` | 拷贝本会话 MAJOR+ 码列表 |
| `pull_events` | 取出 pending（FIFO） |
| `on_wash_session_started/ended` | 开关 journal 窗口 |

### 5.2 alarm_event_bridge

| API | 行为 |
|-----|------|
| `init` | 注册 50ms 周期 drain |
| `drain` | `pull_events` → 发布 `EVT_ALARM_*`；有变更则重算姿态边沿 |

### 5.3 alarm_reeval_bridge

项目提供 `alarm_reeval_binding_t` 表；桥接订阅 `EVT_COMP_MOTION_COMPLETED` / `EVT_WASH_CHECKPOINT_REACHED`，查表后 `reevaluate_group`。

### 5.4 alarm_lifecycle_bridge

订阅 `EVT_WASH_SESSION_STARTED` / `DONE` / `ABORTED`，调用 registry 会话起止 API。

### 5.5 alarm_binding_port

```c
typedef struct {
    sw_err_t (*trigger)(uint32_t alarm_code);
    sw_err_t (*clear)(uint32_t alarm_code);
    sw_err_t (*load_catalog)(const alarm_def_t *defs, unsigned count);
} alarm_binding_ops_t;
```

### 5.6 急停码判定（safety_port）

`op_mode_alarm_port_is_estop(code)`：由项目注册的 `safety_ops_t.alarm_is_estop` 回答（例如项目的急停 DI 报警码）；
供 `op_mode_bridge` 识别急停报警，与 `EVT_HW_ESTOP_*` 硬件路径并存。报警编码表属项目资产，故由项目实现。

### 5.7 通讯 cutout（项目）

周期通讯丢失：若该条目 `level == CRITICAL`，watchdog 在 trigger 前同步 `safety_cutout_execute()`（不再单独配置 cutout 列）。

---

## 6. 与运行模式的衔接

| 衔接点 | 说明 |
|--------|------|
| START_WASH | `alarm_registry_has_blocking_active()` 则拒绝 |
| 洗车正常完成 | `op_mode_on_wash_session_completed`：仍 blocking → `EXCEPTION`，否则 → `WASH_DONE` |
| LOCKOUT | `EVT_SAFETY_LOCKOUT` → abort 洗车 + `op_mode_on_critical_alarm`（WASHING 中不立刻切模式，走中止/ABORT_HOMING） |
| Recover | 清可恢复告警 + 异步归位 → IDLE / EXCEPTION |
| 自检 | 查 LOCKOUT 与 blocking |

急停双通道：急停采集通路（`estop_poll` 或项目自有采样）与 DI 急停报警码（registry）均可进入 EXCEPTION。

---

## 7. 测试覆盖

| 测试 | 覆盖点 |
|------|--------|
| `test_alarm_registry` | 目录加载、触发/清除、三种清除策略、池满、会话 journal、手动复位 |
| `test_safety_matrix` | 等级行为与清除策略两组 3×3 格点逐一断言，并校验等级单调性不变量 |
| `test_safety_posture` | CRITICAL 边沿到 `EVT_SAFETY_*` 的姿态聚合 |
| `test_alarm_event_bridge` | pending → `EVT_ALARM_*`，姿态边沿发布 |
| `test_alarm_reeval_bridge` | 生命周期事件触发 ON_MOTION 分组重评估 |
| `test_alarm_lifecycle_bridge` | 会话 journal 开关、跨会话清空、MINOR 不记录、重复触发去重 |
| `test_op_mode_alarm_port` | 急停码判定端口 |

`test_safety_matrix` 已用变异验证确认矩阵是行为的单一来源：把 MAJOR 的
`blocks_wash_start` 改为 false 后，它与 `test_alarm_registry`、`test_operational_mode`
三者同时失败。断言只能证明今天的值对，证不出别处没有重复表达同一决策。

用例数与通过情况以 `scripts/check_all.sh` 生成的 `build-check/test-results/report.html` 为准，本文不记录动态结论（原则见 `tests/reports/README.md`）。

---

## 8. 扩展指南

### 8.1 新增报警码

1. 在项目 config 表追加目录项（level / clear / reeval_group）。
2. `load_catalog` 加载。
3. detector / 调用点经 `alarm_binding_port` 触发。
4. 按等级语义评估对开洗、洗中、洗后的影响。

### 8.2 新增 ON_MOTION 绑定

1. 定义 `reeval_group`。
2. 目录项 `clear=ON_MOTION` 并填 group。
3. wiring 提供 binding 表并 `alarm_reeval_bridge_init`。

### 8.3 禁止

- detector 直接依赖 registry 实现文件。
- 在 bridge 内硬编码机型分支。
- 用事件 param 传递指针。
- 重新引入与等级重复的「响应策略」枚举，除非有可交叉的真实语义。

---

## 9. 当前落地状态

| 能力 | 状态 |
|------|------|
| `alarm_registry` | ✅ |
| `alarm_binding_port` | ✅ |
| `alarm_event_bridge`（含姿态边沿） | ✅ |
| `alarm_reeval_bridge` | ✅ |
| `alarm_lifecycle_bridge` + session journal | ✅（journal 消费待业务接入） |
| `op_mode_alarm_port` | ✅ |
| MAJOR 洗后评估 | ✅ |
| 单元测试 | ✅ registry / posture 边沿 / event bridge / reeval / projection |
| 项目 DI detector / CommWatchdog | 项目侧职责 |

已删除（刻意不做）：`response_strategy`、独立 `safety_posture` 模块、`alarm_detector_helper`、overflow 元告警、`estop_skip_fn`、`EVT_ALARM_BATCH_CLEARED`。

---

## 10. 相关文档

- `doc/architecture/05-安全与报警架构.md` — 架构总览
- `doc/module-design/domain/命令网关模块设计.md` — 模式与命令裁决
- `doc/module-design/runtime/EventBus模块设计.md` — 事件分发
- `doc/module-design/domain/状态投影与设备快照模块设计.md` — 安全快照投影

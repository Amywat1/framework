# 命令网关模块设计

**版本**：v1.1  
**状态**：已落地（命令网关 + 运行模式 + 副作用路由 + 事件桥接 + recovery/emergency + 洗车编排器）  
**最后同步代码**：2026-07-14（`command_gateway`、`operational_mode`、`op_mode_bridge`、`bootstrap` Demo 集成）  
**适用范围**：`wash-device-framework/application/command_gateway.*`、`application/op_mode_bridge.*`、`domain/command_gateway/`、`ports/inbound/command/`  
**架构基线**：Ports & Adapters + 事件总线串行化（`event_dispatch` 线程）  
**关键词**：device_command_port、command_gateway、OperationalMode、命令矩阵、receipt、`EVT_CMD_*` / `EVT_OP_MODE_*`

---

## 1. 设计目标与核心理念

命令网关是外部控制请求的**唯一业务入口**：云端、CLI、仿真等来源解析为 `dev_cmd_t` 后，经端口提交，在 `event_dispatch` 线程完成模式裁决与副作用执行，并同步返回回执。

### 1.1 设计目标

- **统一入站契约**：所有外部命令走 `device_command_port.submit()`，返回 `dev_cmd_receipt_t`。
- **线程安全串行化**：RPC/CLI 线程只入队；`operational_mode` 仅在 dispatch 线程访问。
- **裁决与执行分离**：domain 层 `operational_mode` 做权限与模式转移；application 层 `side_effect_router` 调 orchestrator/HAL。
- **同步回执**：调用方阻塞等待 sem，获得 ACCEPTED / REJECTED / FAILED / TIMEOUT / BUSY。
- **可观测**：裁决拒绝、模式变更、命令处理完成经 `event_bus` 广播。

### 1.2 核心理念

| 原则 | 规定 |
|------|------|
| 命令词汇在 domain | `dev_cmd_kind_t`、`dev_cmd_t` 定义于 `device_command.h`；port 只引用 |
| 表驱动权限 | `k_cmd_matrix[kind][mode]` 静态矩阵 + 条件分支（急停、报警、service_enabled） |
| 副作用枚举 | 允许执行的命令映射为 `dev_cmd_effect_t`，由 router 执行，domain 不直接调 HAL |
| 模式单线程 | `operational_mode` 不订阅 event；外部生命周期通过显式 `op_mode_on_*` 钩子驱动 |
| 唤醒不携带命令 | `EVT_CMD_GATEWAY_WAKE` param=0；命令载荷保存在网关队列 slot |

### 1.3 处理流水线

```text
  CLI / Cloud / Sim
        │
        ▼
  device_command_port.submit(cmd, receipt, timeout_ms)    ← 调用方线程
        │
        ├─ 分配 queue slot（最多 4 条并发等待）
        ├─ event_publish(EVT_CMD_GATEWAY_WAKE)
        └─ sem_timedwait(done) 阻塞等待
                    │
                    ▼
  event_dispatch 线程：on_gateway_wake → command_gateway_drain()
        │
        ├─ op_mode_handle_command(cmd)     ← 裁决 + 模式转移
        │       ├─ DENIED → receipt REJECTED
        │       └─ ALLOWED → pending_effect
        │
        ├─ side_effect_router_run(effect, cmd)   ← 副作用（若 ALLOWED）
        │
        ├─ publish EVT_OP_MODE_CMD_HANDLED
        └─ sem_post(done) → 唤醒 submit 线程
                    │
                    ▼
  调用方收到 receipt，submit 返回 sw_err_t
```

---

## 2. 分层归属与依赖方向

### 2.1 模块落位

| 层次 | 路径 | 职责 |
|------|------|------|
| **入站端口** | `ports/inbound/command/command_port.h` | `submit` 契约、register/get |
| **网关** | `application/command_gateway.{h,c}` | 队列、唤醒、drain、注册 port ops |
| **副作用路由** | `application/side_effect_router.{h,c}` | 按 `dev_cmd_effect_t` 调 orchestrator/报警/HAL |
| **运行模式** | `domain/command_gateway/operational_mode.{h,c}` | 状态机、命令矩阵、模式转移 |
| **命令词汇** | `domain/command_gateway/device_command.h` | kind/source/body/receipt 类型 |
| **裁决结果** | `domain/command_gateway/command_decision.h` | `dev_cmd_decision_t` |
| **回执** | `domain/command_gateway/command_receipt.h` | `dev_cmd_status_t`、`dev_cmd_receipt_t` |
| **共享类型** | `domain/command_gateway/op_mode_types.h` | 拒绝原因、洗车中止、恢复结果 |
| **模式枚举** | `domain/device_control/model/device_state.h` | `operational_mode_t` 七态 |
| **模式桥接** | `application/op_mode_bridge.{h,c}` | 异步事件 → `op_mode_on_*` / alarm session |
| **端口注册** | `ports/port_registry.c` | `device_command_port_register` |
| **事件契约** | `common/event_types.h` | `EVT_CMD_*`、`EVT_OP_MODE_*`、`EVT_WASH_*` 等 |

### 2.2 依赖方向

```text
adapters / CLI / cloud submit 回调
        │ device_command_port
        ▼
   application/command_gateway
        ├── domain/command_gateway/operational_mode
        ├── application/side_effect_router
        │       ├── wash_orchestrator / self_check_service
        │       ├── alarm_registry
        │       └── machine_ops_port
        └── runtime/event_bus
```

**依赖禁令**：

- `operational_mode` 不得调用 `side_effect_router` 或 HAL；只产出 `pending_effect`。
- `side_effect_router` 不得做命令权限判断（假设 gateway 已裁决 ALLOWED）。
- port 层不得包含业务裁决逻辑。
- 除 `command_gateway` 外，模块不得直接写 `operational_mode` 内部静态状态（须走 `op_mode_on_*` API）。

### 2.3 启动顺序契约

完整启动由 `bootstrap_run()` 编排；命令网关相关阶段如下（须在 `scheduler_start_all()` 之前完成 init）：

```text
bootstrap_register()
    time_util_init() → event_bus_init() → wiring()
    ↓
bootstrap_configure_storage() / bootstrap_load_storage()
    ↓
bootstrap_configure()
    project_configure_safety()
    ↓
bootstrap_bind()
    project_bind_machine()               → machine_ops 等
    alarm_registry_init()                → side_effect RESET_FAULT 依赖
    safety_posture_init()
    project_bind_alarm_catalog()
    ↓
bootstrap_validate()
    ↓
bootstrap_init()
    alarm_event_bridge_init()
    safety_thread_init()                 → 注册（start 延后）
    operational_mode_init()              → 默认 OP_MODE_IDLE
    command_gateway_init()               → 订阅 EVT_CMD_GATEWAY_WAKE，注册 port
    self_check_service_init()
    op_mode_bridge_init()                → 订阅 WASH/SAFETY/ALARM/HW 等
    project_init_adapters()
    project_register_runtime_tasks()
    ↓
bootstrap_start()
    event_dispatch 线程                  → drain gateway + 分发 op_mode_bridge
    scheduler_start_all()
```

`operational_mode_init()` 须在 `command_gateway_init()` 之前；`op_mode_bridge_init()` 须在二者之后。

---

## 3. 文件清单

| 文件 | 职责 |
|------|------|
| `ports/inbound/command/command_port.h` | 入站 port 接口 |
| `application/command_gateway.h` | init / drain 对外 API |
| `application/command_gateway.c` | 队列 slot、submit、drain、事件订阅 |
| `application/side_effect_router.h` | 副作用执行 API |
| `application/side_effect_router.c` | effect → orchestrator/HAL 映射 |
| `domain/command_gateway/device_command.h` | 命令闭集与构造辅助 |
| `domain/command_gateway/command_decision.h` | 裁决 + pending_effect |
| `domain/command_gateway/command_receipt.h` | 回执状态 |
| `domain/command_gateway/op_mode_types.h` | 仲裁/拒绝/中止类型 |
| `domain/command_gateway/operational_mode.h` | 聚合根公开 API |
| `domain/command_gateway/operational_mode.c` | 命令矩阵、模式转移、事件发布 |
| `domain/device_control/model/device_state.h` | `operational_mode_t` |
| `application/op_mode_bridge.h` | 事件桥接 init |
| `application/op_mode_bridge.c` | 订阅表与 handler |
| `tests/application/test_command_gateway.c` | 网关端到端（6 用例） |
| `tests/application/test_op_mode_bridge.c` | 事件驱动模式转移（5 用例） |
| `tests/domain/test_operational_mode.c` | 命令矩阵（11 用例） |
| `tests/ports/test_cloud_ports.c` | command port register 测试 |

---

## 4. 数据模型

### 4.1 设备命令（dev_cmd_t）

```c
typedef struct {
    dev_cmd_meta_t meta;   /* request_id, source */
    dev_cmd_body_t body;   /* kind + payload union */
} dev_cmd_t;
```

**命令种类（dev_cmd_kind_t，闭集）**

| 枚举 | 载荷 | 说明 |
|------|------|------|
| `DEV_CMD_START_WASH` | `start_wash.mode` | 启动洗车 |
| `DEV_CMD_STOP_WASH` | 无 | 中止洗车会话 |
| `DEV_CMD_STOP_OPERATION` | 无 | 关闭运营接单 |
| `DEV_CMD_RESUME_OPERATION` | 无 | 恢复运营接单 |
| `DEV_CMD_RESET_FAULT` | 无 | 复位报警/异常 |
| `DEV_CMD_HOME_DEVICE` | 无 | 设备归位 |
| `DEV_CMD_ENTER_MANUAL` | 无 | 进入手动模式 |
| `DEV_CMD_MANUAL_ACTUATOR` | `act_id`, `param` | 手动点动 |
| `DEV_CMD_START_SELF_CHECK` | 无 | 启动自检 |
| `DEV_CMD_RECOVER` | 无 | 进入恢复流程 |
| `DEV_CMD_STOP_ALL_OUTPUTS` | 无 | 停止全部输出 |

**命令来源（dev_cmd_source_t）**：`CLOUD` / `CLI` / `SIM` / `TEST`（元数据，不参与矩阵）。

### 4.2 运行模式（operational_mode_t）

| 模式 | 含义 |
|------|------|
| `OP_MODE_INIT` | 初始化前占位 |
| `OP_MODE_IDLE` | 待机 |
| `OP_MODE_WASHING` | 洗车执行中 |
| `OP_MODE_MANUAL` | 手动维护 |
| `OP_MODE_SELF_CHECK` | 自检 |
| `OP_MODE_EXCEPTION` | 异常停机 |
| `OP_MODE_RECOVERING` | 恢复过渡 |

聚合内还维护：

- `s_estop_active`：急停标志（影响 CONDITIONAL 命令）
- `s_service_enabled`：运营接单开关（Stop/Resume Operation）

### 4.3 裁决结果（dev_cmd_decision_t）

```c
typedef struct {
    op_cmd_result_t    verdict;        /* ALLOWED / DENIED */
    op_reject_reason_t reason;
    dev_cmd_effect_t   pending_effect; /* ALLOWED 时待执行副作用 */
} dev_cmd_decision_t;
```

**拒绝原因（op_reject_reason_t）**

| 枚举 | 典型场景 |
|------|----------|
| `OP_REJECT_WRONG_MODE` | 矩阵 DENIED 或 START_WASH 有 blocking 报警 |
| `OP_REJECT_ESTOP_ACTIVE` | CONDITIONAL 命令且急停有效 |
| `OP_REJECT_SERVICE_DISABLED` | START_WASH 但 service 关闭 |
| `OP_REJECT_UNKNOWN_CMD` | kind 非法 |

### 4.4 回执（dev_cmd_receipt_t）

| status | 含义 | submit 返回 |
|--------|------|-------------|
| `DEV_CMD_STATUS_ACCEPTED` | 裁决通过且副作用 SW_OK | `SW_OK` |
| `DEV_CMD_STATUS_REJECTED` | 裁决 DENIED | `SW_ERR_STATE` |
| `DEV_CMD_STATUS_FAILED` | ALLOWED 但副作用失败 | `effect_error` |
| `DEV_CMD_STATUS_TIMEOUT` | sem 等待超时 | `SW_ERR_TIMEOUT` |
| `DEV_CMD_STATUS_BUSY` | 队列满或未入队 | `SW_ERR_BUSY` |

### 4.5 副作用种类（dev_cmd_effect_t）

| effect | 触发命令 | router 行为 |
|--------|----------|-------------|
| `DEV_CMD_EFFECT_START_WASH` | START_WASH | `wash_orchestrator_start(mode)` |
| `DEV_CMD_EFFECT_STOP_WASH` | STOP_WASH | `wash_orchestrator_abort(MANUAL)` |
| `DEV_CMD_EFFECT_SELF_CHECK` | START_SELF_CHECK | `self_check_service_start()` |
| `DEV_CMD_EFFECT_RESET_FAULT` | RESET_FAULT | `alarm_registry_recover_all()` + `op_mode_on_legacy_reset_fault()` |
| `DEV_CMD_EFFECT_HOME_DEVICE` | HOME_DEVICE | `machine_ops.home_device()` |
| `DEV_CMD_EFFECT_MANUAL_ACTUATOR` | MANUAL_ACTUATOR | `machine_ops.execute_manual_actuator()` |
| `DEV_CMD_EFFECT_STOP_ALL_OUTPUTS` | STOP_ALL_OUTPUTS | `machine_ops.stop_all_outputs()` |
| `DEV_CMD_EFFECT_NONE` | ENTER_MANUAL、STOP/RESUME_OPERATION、RECOVER 等 | 仅模式/标志变更，router 直接 SW_OK |

### 4.6 相关事件

| 事件 | 发布方 | `param` 编码 |
|------|--------|--------------|
| `EVT_CMD_GATEWAY_WAKE` | command_gateway submit | 0 |
| `EVT_OP_MODE_CHANGED` | operational_mode | `(from << 8) \| to` |
| `EVT_OP_MODE_CMD_REJECTED` | operational_mode | `(kind << 8) \| reason` |
| `EVT_OP_MODE_CMD_HANDLED` | command_gateway drain | `(kind << 16) \| (status << 8) \| reason` |
| `EVT_OP_MODE_RECOVERY_REQUESTED` | RECOVER 命令 | 0 |
| `EVT_OP_MODE_CONTEXT_SYNC` | service_enabled / 急停清除 | 0 |

---

## 5. 核心模块行为契约

### 5.1 command_gateway

**队列参数**

| 常量 | 值 | 说明 |
|------|-----|------|
| `CMD_GATEWAY_QUEUE_SIZE` | 4 | 最大并发等待 submit |
| `CMD_GATEWAY_WAIT_MS` | 5000 | `timeout_ms==0` 时的默认等待 |

**gateway_submit 行为**

1. 在 mutex 下分配空闲 slot，写入 cmd，置 `DEV_CMD_STATUS_BUSY`。
2. 队列满 → 立即返回 `SW_ERR_BUSY`（可选填 receipt）。
3. `event_publish(EVT_CMD_GATEWAY_WAKE)` 失败 → 释放 slot，`SW_ERR_BUSY`。
4. `sem_timedwait` 等待 drain 完成。
5. 超时或 generation 不匹配 → `SW_ERR_TIMEOUT`。
6. 拷贝 receipt，按 status 映射 `sw_err_t` 返回。

**command_gateway_drain 行为**

- 在 **event_dispatch 线程**执行（由 `on_gateway_wake` 或直接测试调用）。
- 扫描所有 used slot：裁决 → 副作用 → 写 receipt → `sem_post`。
- generation 机制防止超时释放后误 post 旧 slot。

### 5.2 operational_mode — 命令矩阵

矩阵值：`OP_PERM_DENIED` / `OP_PERM_ALLOWED` / `OP_PERM_CONDITIONAL`。

**CONDITIONAL 附加规则**：若 `s_estop_active`，拒绝并返回 `OP_REJECT_ESTOP_ACTIVE`。

**业务附加规则（节选）**

| 命令 | 附加条件 |
|------|----------|
| `DEV_CMD_START_WASH` | `service_enabled` 且无 blocking 报警 |
| `DEV_CMD_RESUME_OPERATION` | 仅当 `!service_enabled` |
| `DEV_CMD_RESET_FAULT` | 矩阵 CONDITIONAL；副作用侧再查报警/姿态 |

**允许时的模式转移（无 effect 或 effect 并行）**

| 命令 | 模式/标志变更 |
|------|---------------|
| `DEV_CMD_ENTER_MANUAL` | → `OP_MODE_MANUAL` |
| `DEV_CMD_START_SELF_CHECK` | → `OP_MODE_SELF_CHECK` |
| `DEV_CMD_RECOVER` | → `OP_MODE_RECOVERING` + `EVT_OP_MODE_RECOVERY_REQUESTED` |
| `DEV_CMD_STOP_OPERATION` | `service_enabled = false` |
| `DEV_CMD_RESUME_OPERATION` | `service_enabled = true` |

**外部生命周期钩子（非命令路径）**

| API | 触发场景 |
|-----|----------|
| `op_mode_on_wash_session_started` | IDLE → WASHING |
| `op_mode_on_wash_session_completed` | WASHING → IDLE |
| `op_mode_on_wash_session_aborted` | 手动 → IDLE；其它 → EXCEPTION |
| `op_mode_on_self_check_completed` | → IDLE 或 EXCEPTION |
| `op_mode_on_critical_alarm` | 非 WASHING/RECOVERING → EXCEPTION |
| `op_mode_on_estop_triggered/cleared` | EXCEPTION + 标志；清除后 CONTEXT_SYNC |
| `op_mode_on_recovery_completed` | → IDLE 或 EXCEPTION |

### 5.3 side_effect_router

- 入参 `effect == DEV_CMD_EFFECT_NONE` 直接 `SW_OK`。
- `DEV_CMD_EFFECT_RESET_FAULT`：IDLE 且无 blocking 且非 LOCKOUT 时返回 `SW_ERR_STATE`（防止空复位）。
- HAL 类 effect：`machine_ops_get()` 为 NULL 或函数指针 NULL → `SW_ERR_NOT_INIT`。

### 5.4 op_mode_bridge — 事件驱动模式转移

`operational_mode` **不订阅** event_bus；异步生命周期由 `op_mode_bridge` 在 dispatch 线程调用 `op_mode_on_*` 完成。

**订阅表**

| 事件 | handler 行为 |
|------|--------------|
| `EVT_WASH_SESSION_STARTED` | journal 开始 + `op_mode_on_wash_session_started` |
| `EVT_WASH_DONE` | 会话完成 + journal 结束 + PostWash 评估（blocking/estop → EXCEPTION） |
| `EVT_WASH_ABORTED` | 按 `param` 解码 cause 中止；非 MANUAL 时 PostWash 评估 |
| `EVT_SAFETY_LOCKOUT` | `op_mode_on_critical_alarm` |
| `EVT_ALARM_TRIGGERED` | 若 `op_mode_alarm_port_is_estop(code)` → 急停触发 |
| `EVT_ALARM_CLEARED` | 若 is_estop → 急停清除 |
| `EVT_HW_ESTOP_ON/OFF` | 硬件急停边沿 → estop 触发/清除 |
| `EVT_OP_MODE_RECOVERY_COMPLETED` | `op_mode_on_recovery_completed(param)` |
| `EVT_OP_MODE_SELF_CHECK_COMPLETED` | `op_mode_on_self_check_completed(param!=0)` |

PostWash 评估规则：`enter_exception = alarm_registry_has_blocking_active() || op_mode_is_estop_active()`。

### 5.5 device_command_port API

```c
sw_err_t (*submit)(const dev_cmd_t *cmd,
                   dev_cmd_receipt_t *receipt,
                   uint32_t timeout_ms);
```

| 条件 | 返回值 |
|------|--------|
| 成功进入业务流水线 | `SW_OK`（receipt 内可能是 REJECTED/FAILED） |
| 队列/事件/硬件错误 | `SW_ERR_BUSY` / `SW_ERR_TIMEOUT` / `SW_ERR_HW` 等 |
| ops 未注册 | 调用方应检查 `device_command_port_get_ops() == NULL` |

---

## 6. 与云端模块的衔接

云端物模型 `DEVICE_CMD` 语义经 wiring 注册 `cloud_point_set_device_cmd_submit()`，内部构造 `dev_cmd_t` 并调用本 port。云端脉冲命令通常只有 `kind`，无 `start_wash.mode` 等载荷；带载荷命令（如 START_WASH）须在 submit 回调中按云平台协议填充 `body.payload`。

详见 `doc/module-design/ports-adapters/CloudModel模块设计.md` §6。

---

## 7. 与事件总线的衔接

| 角色 | 说明 |
|------|------|
| 串行化 | submit 线程与 dispatch 线程解耦；模式状态只在 dispatch 线程变更 |
| 唤醒 | `EVT_CMD_GATEWAY_WAKE` 无载荷；避免在 event param 传递命令指针 |
| 广播 | 模式变更、拒绝、处理完成供上报、日志等订阅 |
| 非命令事件 | 洗车/急停/报警等经 **`op_mode_bridge`** 调用 `op_mode_on_*`，不经过 command_gateway 队列 |

`operational_mode` **不订阅** event_bus；`op_mode_bridge` 已在 `wash-device-framework` 落地，须与 `safety_posture`、`alarm_event_bridge` 同处于 dispatch 线程语义下初始化（见 §2.3）。

---

## 8. 完整接入示例

> **示例（单元测试 / 完整框架，仅供参考）**

```c
/* 启动 */
operational_mode_init();
command_gateway_init();
safety_posture_init();
op_mode_bridge_init();
/* 启动 event_dispatch 线程 */

/* 任意来源提交 */
dev_cmd_t cmd = dev_cmd_make_simple(DEV_CMD_STOP_OPERATION);
dev_cmd_receipt_t receipt;
sw_err_t ret = device_command_port_get_ops()->submit(&cmd, &receipt, 1000U);
/* ret==SW_OK && receipt.status==DEV_CMD_STATUS_ACCEPTED */
```

---

## 9. 扩展指南

### 9.1 新增命令种类

1. `device_command.h`：追加 `dev_cmd_kind_t`（保持 `DEV_CMD_MAX` 边界）。
2. `operational_mode.c`：扩展 `k_cmd_matrix` 行、`effect_for_kind()`、`op_mode_handle_command` switch。
3. `side_effect_router.c`：追加 `dev_cmd_effect_t` case（若需副作用）。
4. 更新单元测试与本文档矩阵说明。

### 9.2 新增模式或转移规则

1. 修改 `device_state.h` 中 `operational_mode_t`（影响矩阵列宽，全表审查）。
2. 补充 `op_mode_on_*` 钩子供 orchestrator/bridge 调用。
3. 评估 `EVT_OP_MODE_CHANGED` 消费方。

### 9.3 禁止的扩展方式

- 在 CLI/MQTT 线程直接调用 `op_mode_handle_command` 或 orchestrator。
- 在 `operational_mode` 内调用 `machine_ops` 或 `wash_orchestrator_start`。
- 扩大 `CMD_GATEWAY_QUEUE_SIZE` 而不评估 dispatch drain 延迟。
- 用 `EVT_CMD_ORDER`（遗留）替代 port submit 生产路径。

---

## 10. 当前落地状态（wash-device-framework）

| 能力 | 状态 |
|------|------|
| `device_command` 领域词汇 | ✅ |
| `operational_mode` 矩阵与转移 | ✅ |
| `command_gateway` 队列 + 唤醒 | ✅ |
| `side_effect_router` | ✅（START/STOP 已接 `wash_orchestrator`） |
| `device_command_port` | ✅ |
| 单元测试 | ✅ gateway(6) / op_mode_bridge(5) / operational_mode(11) / side_effect_router(9) |
| `op_mode_bridge`（event → op_mode_on_*） | ✅ |
| `bootstrap_run()` Demo 集成 | ✅ 命令 submit + estop + 报警链验证 |
| recovery_service / emergency_handler 联调 | ✅ 已迁入并有事件链单测 |
| 与 cloud DEVICE_CMD 端到端 wiring | 项目侧 wiring 职责，Demo 未接入完整云物模型 |

---

## 11. 相关文档

- `doc/module-design/ports-adapters/CloudModel模块设计.md` — 物模型 DEVICE_CMD → port 衔接
- `doc/module-design/runtime/EventBus模块设计.md` — dispatch 线程与 `EVT_CMD_*` / `EVT_OP_MODE_*`
- `doc/module-design/domain/报警系统模块设计.md` — PostWash 评估、LOCKOUT、RESET_FAULT 与 registry 衔接
- `domain/device_control/model/device_state.h` — 模式枚举定义

---

## 附录 A：命令矩阵速查（ALLOWED / CONDITIONAL / DENIED）

> 完整逻辑以 `operational_mode.c` 中 `k_cmd_matrix` 为准；下表为摘要。

| 命令 \\ 模式 | IDLE | WASHING | MANUAL | SELF_CHECK | EXCEPTION | RECOVERING |
|--------------|------|---------|--------|------------|-----------|------------|
| START_WASH | A | D | D | D | D | D |
| STOP_WASH | D | A | D | D | D | D |
| STOP_OPERATION | A | D | D | D | D | D |
| RESUME_OPERATION | A* | D | D | D | D | D |
| RESET_FAULT | C | D | D | D | C | D |
| HOME_DEVICE | D | D | D | D | A | D |
| ENTER_MANUAL | A | D | D | D | D | D |
| MANUAL_ACTUATOR | D | D | A | D | A | D |
| START_SELF_CHECK | C | D | D | D | C | D |
| RECOVER | D | D | C | D | C | D |
| STOP_ALL_OUTPUTS | A | A | A | D | A | A |

图例：A=ALLOWED，C=CONDITIONAL（急停时拒），D=DENIED；*RESUME 另需 `!service_enabled`。

---

## 附录 B：自检对照（module-design-spec B 类）

| 检查项 | 状态 |
|--------|------|
| 分层图 + 依赖禁令 | §2 |
| 文件清单 | §3 |
| 数据模型 + API 行为 | §4、§5 |
| 与云端/总线衔接 | §6、§7 |
| 扩展指南 | §9 |
| 落地状态 | §10 |
| 无函数级 walkthrough | 已遵守 |
| 无测试/构建命令 | 已遵守 |
| 示例已标注「仅供参考」 | §8 |

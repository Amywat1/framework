# 驱动层单元测试报告

**测试日期**：2026-06-30  
**测试范围**：`adapters/hal/linux_hw/drv/` — drv_vfd、drv_voice、drv_io  
**测试结果**：118 个用例全部通过，0 失败

---

## drv_vfd（变频器驱动）— 50 个用例，全部通过

> 当前厂商：士林 VFD。寄存器：STATE=0x1001，FAULT_CODE=0x1007，CURRENT=0x1004，
> CLEAR_FAULT=0x1000（数据固定 0x1101）；频率设定寄存器未定义。

| 测试功能 | 结果 |
|----------|------|
| init 传入 NULL vfd / NULL 串口路径 / NULL do_set 回调，正确返回参数错误 | ✓ |
| init 时 modbus_new_rtu 返回 NULL，返回 SW_ERR_HW，实例 serial_port 被清零 | ✓ |
| config_speed_io 传入 NULL 挡位配置 / NULL 速度引脚，正确返回参数错误 | ✓ |
| config_speed_io 挡位映射含 VFD_SPD_IO(0,0)（与停止态冲突），正确返回参数错误 | ✓ |
| run 传入超出范围挡位（±4），正确返回参数错误 | ✓ |
| run 在未配置反转引脚时请求反转挡，正确返回参数错误 | ✓ |
| run / write / read / fault_reset 在未初始化实例上调用，正确返回未初始化错误 | ✓ |
| set_monitor_mask 在未初始化实例上调用，正确返回未初始化错误 | ✓ |
| get_state(NULL) 返回 STOPPED，不崩溃 | ✓ |
| get_cached(NULL, reg) 对任意寄存器返回 0，不崩溃 | ✓ |
| get_cached(vfd, STATE/FREQ) 等不可缓存寄存器返回 0 | ✓ |
| run(FWD_1) 后 get_state 返回 FWD；run(REV_1) 返回 REV；run(STOP) 返回 STOPPED | ✓ |
| run(FWD_1)：FWD 引脚拉高，REV 引脚拉低，速度引脚按挡位配置正确输出 | ✓ |
| run(REV_1)：REV 引脚拉高，FWD 引脚拉低 | ✓ |
| run(STOP)：所有控制引脚（FWD / REV / SPD1 / SPD2）全部拉低 | ✓ |
| 对已停止的 VFD 再次发送 STOP，不崩溃，状态不变 | ✓ |
| 正转中切反转：立即关断所有引脚，不提前拉高新方向引脚 | ✓ |
| 方向切换 300ms 等待中发送 STOP：立即停止，取消等待 | ✓ |
| 方向切换 300ms 等待中重新发送反转（任意反转挡）：只更新目标挡位，300ms 从第一次切换时开始计算不重置 | ✓ |
| 方向切换 300ms 等待中重新发送正转：取消等待，立即执行正转 | ✓ |
| fault_reset 取消待处理的方向切换（pending_gear 清零）| ✓ |
| fault_reset（pin_rst 有效）：先关断所有运行引脚，再拉高 RST 引脚 | ✓ |
| fault_reset（pin_rst 为 IO_HANDLE_NULL）：走 Modbus 路径，写清故障寄存器 0x1000，数据固定 0x1101 | ✓ |
| read(FAULT_CODE) 读取成功，同步更新 cached_fault_code，fault_active 置为 true | ✓ |
| read(FAULT_CODE) 故障码从非零变为 0：fault_active 清零，触发 FAULT_CLEARED 事件 | ✓ |
| read(FAULT_CODE) 故障码从 0 变为非零：fault_active 置位，触发 FAULT_DETECTED 事件 | ✓ |
| read(FAULT_CODE) 传入 NULL 输出指针返回参数错误 | ✓ |
| read(CURRENT) 读取成功，返回正确电流值 | ✓ |
| read(CURRENT) 传入 NULL 输出指针返回参数错误 | ✓ |
| read(CURRENT) 在 Modbus 读失败时返回通信错误 | ✓ |
| read(STATE) 读取成功，返回正确状态字 | ✓ |
| read(STATE) 传入 NULL 输出指针返回参数错误 | ✓ |
| read 传入只写寄存器（FREQ / CLEAR_FAULT）返回参数错误 | ✓ |
| write(FREQ)：当前厂商（士林）未定义频率寄存器，返回参数错误 | ✓ |
| write 传入只读寄存器（STATE / FAULT_CODE / CURRENT）返回参数错误 | ✓ |
| write(CLEAR_FAULT)：写寄存器 0x1000，数据固定为 0x1101（val 参数被忽略） | ✓ |
| write(CLEAR_FAULT) 在 Modbus 写失败时返回通信错误 | ✓ |
| get_cached(FAULT_CODE) / get_cached(CURRENT) 返回内部缓存值 | ✓ |
| Modbus 连续失败 3 次后事件回调触发 COMM_LOST | ✓ |
| 通信恢复后事件回调触发 COMM_RESTORED | ✓ |
| event_cb 注销为 NULL 后触发通信失败，不崩溃 | ✓ |

---

## drv_voice（语音模块驱动）— 24 个用例，全部通过

| 测试功能 | 结果 |
|----------|------|
| init 传入 NULL 实例 / NULL 串口路径，正确返回参数错误 | ✓ |
| init Modbus 地址为 0 或 248（超出 1~247 合法范围），正确返回参数错误 | ✓ |
| init 时 modbus_new_rtu 返回 NULL，返回 SW_ERR_HW，serial_port 被清零 | ✓ |
| init 时 Modbus connect 失败走延迟连接，init 仍返回成功 | ✓ |
| 对同一实例重复 init（模拟配置变更），不崩溃，新地址生效 | ✓ |
| play / stop / pause / set_volume / volume_up / volume_down 在未初始化实例上调用，返回未初始化错误 | ✓ |
| drv_voice_play(track=3) 写入寄存器 0x0004，值为 3 | ✓ |
| drv_voice_stop 写入寄存器 0x000A，值为触发值 0x0001 | ✓ |
| drv_voice_pause 写入寄存器 0x0009 | ✓ |
| drv_voice_set_volume(15) 写入寄存器 0x0002，值为 15 | ✓ |
| drv_voice_volume_up 写入寄存器 0x0005 | ✓ |
| drv_voice_volume_down 写入寄存器 0x0006 | ✓ |
| Modbus 写失败时各操作返回通信错误 | ✓ |
| 连续 3 次通信失败后事件回调触发 COMM_LOST，comm_ok 置为 false | ✓ |
| 通信恢复后事件回调触发 COMM_RESTORED，comm_ok 置为 true | ✓ |
| event_cb 注销为 NULL 后触发通信失败，不崩溃 | ✓ |
| 连续 10 次通信失败后 comm_fail_count 自动重置为 0（防无限累积的安全阀） | ✓ |

---

## drv_io（CAN IO 子板驱动）— 44 个用例，全部通过

| 测试功能 | 结果 |
|----------|------|
| init 传入 NULL 配置，正确返回参数错误 | ✓ |
| init board_count 为 0 或 ≥7（超出内部上限），正确返回参数错误 | ✓ |
| init pin_count 为 0 或 >32（超出上限），正确返回参数错误 | ✓ |
| init 合法参数返回成功，drv_io_board_count 与配置一致 | ✓ |
| init 传入 NULL 名称表 + count=0，成功返回，名称查找优雅失败不崩溃 | ✓ |
| try_parse_di 支持完整名称（"DI_ESTOP"）和省略前缀（"ESTOP"）两种写法 | ✓ |
| try_parse_di 对未注册名称返回 false；传入 NULL 不崩溃；输出指针为 NULL 时仅检查存在性 | ✓ |
| try_parse_do 支持完整名称和省略前缀，未注册返回 false | ✓ |
| di_name / do_name 对已注册句柄返回正确名称字符串，未注册返回 NULL | ✓ |
| 初始化后 di_read 默认返回 false（输入缓存为零） | ✓ |
| set_test_override(pin, 1) 后 di_read 返回 true，绕过硬件缓存 | ✓ |
| set_test_override(pin, 0) 强制 di_read 返回 false | ✓ |
| clear_test_override 后 di_read 恢复读硬件缓存 | ✓ |
| 覆盖一个引脚不影响其他引脚的读取结果 | ✓ |
| set_test_override 传入非 0/1 的值（如 99），自动清除覆盖而非强制开 | ✓ |
| do_set 写入新值后 output_request_count 递增；写入相同值不重复计数 | ✓ |
| do_set 传入 DI 类型句柄（类型位错误）返回参数错误 | ✓ |
| di_read 传入 DO 类型句柄（类型位错误）返回 false，不崩溃 | ✓ |
| do_set 引脚 board_id 超出 board_count 范围，返回参数错误 | ✓ |
| di_read 引脚 board_id 超出范围，返回 false，不崩溃 | ✓ |
| board_is_online：有效 board_id 但子板尚未上线，返回 false | ✓ |
| board_is_online：board_id=0 或超出范围，返回 false | ✓ |
| get_stats 传入 NULL 输出指针返回参数错误 | ✓ |
| get_stats board_id=0 返回参数错误 | ✓ |
| flush_outputs_now 在无在线子板时返回 SW_OK，不崩溃 | ✓ |
| register_board_error_cb / register_panic_cb 注册和注销（传 NULL）不崩溃 | ✓ |

---

## 测试中发现并修复的代码问题

| 问题 | 文件 | 描述 | 修复 |
|------|------|------|------|
| 缺少 `return NULL` | `drv_io.c` | `drv_io_poll_loop`（`void *`）在 `while(1)` 后无 return，`-Werror=return-type` 报错 | 补充 `return NULL` 注明为不可达路径 |
| init 失败后实例可用 | `drv_voice.c` | `voice_mb_ctx_create` 失败时未清零 `serial_port`，导致 `voice_is_initialized` 对失败实例仍返回 true | 失败路径补充 `v->serial_port = NULL` |
| unused-parameter 警告 | `drv_vfd.c` | `drv_vfd_write` 的 `val` 参数在士林厂商配置下（无频率寄存器）未被使用，`-Werror=unused-parameter` 报错 | 补充 `(void)val` 并加注释说明条件编译原因 |

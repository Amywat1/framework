# 框架行为契约

## 1. 本文档解决什么问题

框架的正确性主张与应用不同。应用主张"这台机器行为正确"；框架主张"任何符合契约的
项目建立在我之上都行为正确"——这是对项目全集的全称命题，无法靠测试自带的仿真或
demo 这一个实例来证明。

因此每条行为都必须回答两个问题，且答案唯一：

- **归属**：这条行为由谁保证？
- **级别**：在哪一层验证？

缺了这两个答案，框架与项目之间会出现互相以为对方在验的行为。这类缝隙里的行为
往往正好是安全降级路径——因为它们天然跨越两边。项目侧 M8 的契约中已有两条标注
"由 framework 自有测试验证"，而框架此前没有契约，也就没有任何机制去验它们。
本文档消除这个缝隙。

## 2. 归属三分

| 归属 | 含义 | 谁写测试 | 允许的级别 |
|------|------|----------|-----------|
| `框架保证` | 与项目无关的框架自身不变量 | 框架 | L0–L4 |
| `框架要求` | 框架对项目适配器的要求，交付为一致性套件由项目运行 | 框架提供套件，项目运行 | L2 |
| `项目自负` | 框架只定契约边界，行为由项目实现 | 项目 | 不由本契约判定 |

`框架要求` 只允许 L2：它的被测对象是项目适配器，而端口是唯一的契约边界。若某条
"框架要求"需要在 L3 或 L4 才能验证，说明它实际依赖了端口之外的东西，应当先修
契约本身而不是降低验证级别。

## 3. 验证级别

| 级别 | 名称 | 被测对象 | 判定方式 |
|------|------|----------|----------|
| L0 | 静态 | 全部源码 | 脚本审计，无需运行 |
| L1 | 纯单元 | `common/` `domain/` | 确定性单元测试，可穷举 |
| L2 | 契约 | `ports/` 与适配器 | 端口替身 + 一致性套件 |
| L3 | 运行时 | `runtime/` | 并发、时序、容量、故障注入 |
| L4 | 参考项目 | 全栈 | 无头驱动最小整机 |
| L5 | 现场 | 物理特性 | **CI 不判**，仅登记 |

级别不是难度分级，而是"只能在哪里被观测"。选择依据：

- 结构性质（依赖方向、层内禁用设施）运行时观测不到 → L0
- `common/` 与 `domain/` 设计上无 IO、无线程、无分配 → L1 可完全确定且可穷举
- 端口是设计认可的替换点，也是唯一该做替身的地方 → L2
- 只有 `runtime/` 有线程和时间 → L3
- 跨层链路的存在性证明 → L4

## 4. 类别

| 类别 | 含义 |
|------|------|
| `接口契约` | 入参校验、返回码、调用前提 |
| `功能行为` | 可观测的功能结果 |
| `状态机` | 状态迁移与迁移条件 |
| `并发` | 竞态、锁协议、可见性 |
| `时序` | 周期、超时、漂移 |
| `容量` | 上限、水位、超限行为 |
| `故障` | 失败路径、降级、上报 |
| `结构` | 分层、依赖、层内约束 |
| `物理特性` | 需实物标定 |

## 5. 状态列的含义

`状态` 列描述的是**验证是否已建立**，不是行为是否已实现：

| 状态 | 含义 |
|------|------|
| `已验证` | 已有测试覆盖该行为，且测试确实会因该行为失效而失败 |
| `待验证` | 行为已在设计中确立，验证尚未建立 |
| `待实现` | 行为由设计推导得出，代码尚未提供 |

刻意把"实现状态"排除在契约之外：契约描述框架承诺什么，而不是当前完成度。完成度
记在 `architecture/08-框架优化方向.md`。若在此处也记一份，两处必然失同步——这正是
本仓已发生过的问题（`tests/reports/` 的手写覆盖表声称 event_bus 8 例、实际 13 例）。

`已验证` 的判定标准是"测试确实会因该行为失效而失败"，不是"存在一个名字相关的
测试"。对宣称唯一事实来源的行为（安全矩阵、事件编解码、容量常量），必须用变异
验证：改动来源后确认依赖它的测试全部失败。断言只能证明今天的值对，证不出别处
没有重复表达同一决策。

## 6. 契约条目

编号前缀按子系统划分，与验证级别无关：

`BOOT` 启动序列 | `EBUS` 事件总线 | `SCHED` 调度与线程 | `CMD` 命令网关
`MODE` 运行模式 | `ALRM` 报警注册表 | `SAFE` 安全输出与急停 | `ENGN` 方案引擎
`PROJ` 状态投影 | `PORT` 端口注册与契约 | `ERRM` 错误模型 | `ASSET` 资产版本
`ARCH` 架构边界 | `LOG` 日志与诊断

### 6.1 架构边界 ARCH

依据 `architecture/01-分层架构与依赖边界.md`。这些是结构性质，运行时观测不到，
且腐化是渐进的——单看每次改动都合理，因此必须机器审计。

| 编号 | 行为 | 类别 | 归属 | 级别 | 状态 |
|------|------|------|------|------|------|
| ARCH-01 | `common/` 不依赖任何上层目录 | 结构 | 框架保证 | L0 | 已验证 |
| ARCH-02 | `domain/` 不依赖 `adapters/` 与 `application/` | 结构 | 框架保证 | L0 | 已验证 |
| ARCH-03 | `ports/` 不依赖 `adapters/`、`application/`、`runtime/` | 结构 | 框架保证 | L0 | 已验证 |
| ARCH-04 | `runtime/` 的总线与调度不依赖业务层 | 结构 | 框架保证 | L0 | 已验证 |
| ARCH-05 | 框架各层不引用项目专属头与机型前缀文件 | 结构 | 框架保证 | L0 | 已验证 |
| ARCH-06 | 全框架不使用 `../` 相对 include（否则前缀规则可被绕过） | 结构 | 框架保证 | L0 | 已验证 |
| ARCH-07 | `domain/` 不做文件 IO、序列化解析、线程注册 | 结构 | 框架保证 | L0 | 已验证 |
| ARCH-08 | `domain/` 不使用动态内存（豁免须显式登记） | 结构 | 框架保证 | L0 | 已验证 |
| ARCH-09 | `adapters/` 不承载跨领域编排（bridge/projection/coordinator） | 结构 | 框架保证 | L0 | 已验证 |
| ARCH-10 | tick 路径不分配动态内存 | 结构 | 框架保证 | L0 | 待验证 |
| ARCH-11 | 急停热路径可达的每把互斥量都启用优先级继承 | 并发 | 框架保证 | L0 | 待验证 |

ARCH-10 与 ARCH-11 是设计已确立、验证尚缺的两条。它们的共同点是**功能测试永远抓
不到**：违反后不表现为功能失败，只表现为偶发时序异常。ARCH-10 可用测试构建挂分配
器钩子使 tick 期间任何分配直接失败；ARCH-11 需静态审计从急停入口可达的锁集合。

### 6.2 启动序列 BOOT

依据 `architecture/04-运行时与线程模型.md` 第 2、4.1 节。

| 编号 | 行为 | 类别 | 归属 | 级别 | 状态 |
|------|------|------|------|------|------|
| BOOT-01 | 启动按固定阶段推进，前一阶段失败不进入下一阶段 | 状态机 | 框架保证 | L3 | 待验证 |
| BOOT-02 | 任一阶段失败时 `bootstrap_run()` 返回非 `SW_OK` 并指明阶段 | 故障 | 框架保证 | L3 | 待验证 |
| BOOT-03 | 启动失败后不启动任何运行线程 | 故障 | 框架保证 | L3 | 待验证 |
| BOOT-04 | 缺失必需端口在 validate 阶段一次报出全部缺失项 | 故障 | 框架保证 | L2 | 已验证 |
| BOOT-05 | 项目钩子缺失任一必填项时拒绝注册 | 接口契约 | 框架保证 | L2 | 待验证 |
| BOOT-06 | 调用方在 `bootstrap_run()` 失败后终止进程，不重试 | 故障 | 项目自负 | — | — |

BOOT-06 归为项目自负是因为框架无法强制调用方的行为，只能在文档与返回码上表达。
框架侧能做的是让失败尽可能落在 validate 阶段（此时未初始化硬件、未启动线程），
这一点由 BOOT-04 保证。

BOOT-01 至 BOOT-03 需要逐阶段注入失败，共 8 个阶段 × 失败一次。这是 L3 而非 L2，
因为 bootstrap 会真实启动线程，需要观测"线程未被启动"这一副作用的缺失。

### 6.3 端口注册与契约 PORT

依据 `ports/port_registry.h` 的统一注册语义。

| 编号 | 行为 | 类别 | 归属 | 级别 | 状态 |
|------|------|------|------|------|------|
| PORT-01 | `ops == NULL` 为显式解除注册，返回 `SW_OK` | 接口契约 | 框架保证 | L2 | 已验证 |
| PORT-02 | `ops` 非空但缺必填字段时返回 `SW_ERR_PARAM` 且保持原注册不变 | 接口契约 | 框架保证 | L2 | 已验证 |
| PORT-03 | 重复注册合法函数表以最后一次为准，返回 `SW_OK` | 接口契约 | 框架保证 | L2 | 已验证 |
| PORT-04 | 未注册端口被调用时行为明确（故障安全值 + 首次告警） | 故障 | 框架保证 | L2 | 已验证 |
| PORT-05 | 每个注册表提供复位入口，复位后回到未注册态 | 接口契约 | 框架保证 | L2 | 已验证 |
| PORT-06 | 所有 `*_register()` 返回 `sw_err_t`，不静默忽略校验失败 | 接口契约 | 框架保证 | L2 | 已验证 |

PORT-02 的"保持原注册不变"必须单独断言：只验证返回码不足以发现"拒绝的同时已经
覆盖了半个 ops"这类实现缺陷。

### 6.4 事件总线 EBUS

依据 `architecture/02` 第 3 节与 `module-design/runtime/EventBus模块设计.md`。

| 编号 | 行为 | 类别 | 归属 | 级别 | 状态 |
|------|------|------|------|------|------|
| EBUS-01 | 安全类事件走高优先级队列，优先于普通事件被分发 | 功能行为 | 框架保证 | L3 | 已验证 |
| EBUS-02 | 队列满时发布返回明确错误并计入丢弃统计 | 容量 | 框架保证 | L3 | 已验证 |
| EBUS-03 | 慢 handler 被计数并告警，快 handler 不误判 | 时序 | 框架保证 | L3 | 已验证 |
| EBUS-04 | 分类别发布/消费/丢弃计数可通过统计接口读取 | 功能行为 | 框架保证 | L3 | 已验证 |
| EBUS-05 | 队列水位与峰值可在运行期读取 | 容量 | 框架保证 | L3 | 已验证 |
| EBUS-06 | dispatch 线程 fatal 时通过回调上报，不静默退出 | 故障 | 框架保证 | L3 | 已验证 |
| EBUS-07 | 非法事件类型在发布侧被拦截，不进入队列 | 接口契约 | 框架保证 | L2 | 已验证 |
| EBUS-08 | 单事件订阅者达上限后拒绝新订阅并返回错误 | 容量 | 框架保证 | L2 | 已验证 |
| EBUS-09 | 慢 handler 阻塞期间普通队列可完整填至标称容量 | 容量 | 框架保证 | L3 | 已验证 |
| EBUS-10 | 普通队列耗尽时高优先级队列仍可完整接受安全事件 | 容量 | 框架保证 | L3 | 已验证 |
| EBUS-11 | 事件 param 的编解码往返一致，布局契约被锁定 | 接口契约 | 框架保证 | L1 | 已验证 |
| EBUS-12 | 可被实时线程发布而不产生无界优先级反转 | 并发 | 框架保证 | L0 | 待验证 |
| EBUS-13 | 提供同步排空入口，使事件链路可在单线程下确定推进 | 接口契约 | 框架保证 | L2 | 待实现 |

EBUS-09 与 EBUS-10 验证的是两个**设计前提**而非功能：有效容量等于标称容量（不提前
失效），以及双队列隔离有效。这类"前提成立性"必须显式验证，否则容量数字的意义
无从保证。

EBUS-13 标 `待实现`：总线设计上有独立 dispatch 线程，若测试只能"发布→sleep→断言"，
则每一个涉及事件的测试都是构造性易崩的，而事件链路是框架核心。这是本契约推导出的
设计缺口，详见第 7 节。

### 6.5 调度与线程 SCHED

依据 `architecture/04` 第 3 节。

| 编号 | 行为 | 类别 | 归属 | 级别 | 状态 |
|------|------|------|------|------|------|
| SCHED-01 | 周期任务按绝对下一拍推进，回调耗时不累积进周期 | 时序 | 框架保证 | L3 | 已验证 |
| SCHED-02 | 单次回调超过一个周期时跳过已错过的拍，不追赶 | 时序 | 框架保证 | L3 | 待验证 |
| SCHED-03 | 线程登记超过容量上限时注册失败并返回错误 | 容量 | 框架保证 | L2 | 已验证 |
| SCHED-04 | 周期任务容量与线程表容量一致，不构成第二道独立上限 | 容量 | 框架保证 | L0 | 已验证 |
| SCHED-05 | 注册参数非法时拒绝并返回错误，不登记半条记录 | 接口契约 | 框架保证 | L2 | 已验证 |
| SCHED-06 | 线程按登记的调度策略与优先级创建 | 功能行为 | 框架保证 | L3 | 已验证 |
| SCHED-07 | 提供测试复位入口，且仅允许测试调用 | 接口契约 | 框架保证 | L2 | 已验证 |

SCHED-04 是编译期断言而非运行期检查，故为 L0：两个容量常量失配属于结构性错误，
应当在编译期就失败，而不是等到第 N 个任务注册时才暴露。

### 6.6 命令网关 CMD

依据 `architecture/02` 第 1 节与 `ports/inbound/command/command_port.h`。

| 编号 | 行为 | 类别 | 归属 | 级别 | 状态 |
|------|------|------|------|------|------|
| CMD-01 | 命令经裁决后才产生副作用，被拒命令不触达执行层 | 功能行为 | 框架保证 | L2 | 已验证 |
| CMD-02 | 槽位耗尽时返回 `SW_ERR_BUSY`，不阻塞调用方 | 容量 | 框架保证 | L3 | 已验证 |
| CMD-03 | 等待被信号打断时重试，不误判为超时 | 并发 | 框架保证 | L3 | 已验证 |
| CMD-04 | 重试不延长总等待时长（截止时间为绝对值） | 时序 | 框架保证 | L3 | 待验证 |
| CMD-05 | 槽位回收后残留信号量计数不会被下一任槽主读到 | 并发 | 框架保证 | L3 | 已验证 |
| CMD-06 | 从 dispatch 线程自身提交命令时立即返回错误，不自锁 | 并发 | 框架保证 | L3 | 已验证 |
| CMD-07 | 命令回执含裁决结果与错误原因 | 接口契约 | 框架保证 | L2 | 已验证 |

CMD-05 是竞态而非功能缺陷：错误表现为"命令被静默丢弃、调用方却收到 BUSY"。这类
行为无法用顺序执行的测试发现，必须构造超时与回收的时间窗重叠。

### 6.7 运行模式 MODE

依据 `architecture/05` 第 3 节的命令允许矩阵。

| 编号 | 行为 | 类别 | 归属 | 级别 | 状态 |
|------|------|------|------|------|------|
| MODE-01 | 命令许可按 8 命令 × 10 状态矩阵裁决，逐格可断言 | 状态机 | 框架保证 | L1 | 已验证 |
| MODE-02 | `CONDITIONAL` 格位额外检查急停与运营开关等运行期条件 | 状态机 | 框架保证 | L1 | 已验证 |
| MODE-03 | 急停置位时进入 EXCEPTION，解除不自动恢复运营 | 状态机 | 框架保证 | L1 | 已验证 |
| MODE-04 | 阻塞类报警活跃时拒绝开洗 | 状态机 | 框架保证 | L1 | 已验证 |
| MODE-05 | CRITICAL 报警使设备进入 LOCKOUT | 状态机 | 框架保证 | L1 | 已验证 |
| MODE-06 | 上电默认状态为 STOPPED，运营开关默认开 | 状态机 | 框架保证 | L1 | 已验证 |
| MODE-07 | 未知状态或未知命令取拒绝，不取放行 | 故障 | 框架保证 | L1 | 已验证 |

MODE-01 用穷举而非抽样：矩阵的错误模式是"某一格填错"，抽样必然漏。`domain/` 设计上
无 IO 无线程，穷举 80 格的成本极低——这是分层带来的直接红利。

### 6.8 报警注册表 ALRM

依据 `architecture/05` 第 3 节的两张行为属性表。

| 编号 | 行为 | 类别 | 归属 | 级别 | 状态 |
|------|------|------|------|------|------|
| ALRM-01 | 等级行为按 3×3 属性表判定（阻塞开洗/进 LOCKOUT/记会话日志） | 状态机 | 框架保证 | L1 | 已验证 |
| ALRM-02 | 清除策略按 3×3 属性表判定，与等级正交 | 状态机 | 框架保证 | L1 | 已验证 |
| ALRM-03 | 更高等级不得比更低等级宽松（单调性不变量） | 状态机 | 框架保证 | L1 | 已验证 |
| ALRM-04 | 未知等级取最严格值，未知策略不自动清除 | 故障 | 框架保证 | L1 | 已验证 |
| ALRM-05 | 矩阵是等级语义的唯一来源（变异验证） | 结构 | 框架保证 | L1 | 已验证 |
| ALRM-06 | 触发与清除幂等，重复触发不产生重复活动条目 | 功能行为 | 框架保证 | L1 | 已验证 |
| ALRM-07 | `ON_MOTION` 策略在运动完成后重评估，未发生运动时允许手动复位 | 状态机 | 框架保证 | L1 | 已验证 |
| ALRM-08 | 活动报警池达上限后的行为明确 | 容量 | 框架保证 | L1 | 待验证 |
| ALRM-09 | 会话日志随会话开关，新会话清空上轮 | 状态机 | 框架保证 | L1 | 已验证 |
| ALRM-10 | 报警码语义与等级映射由项目提供 | 功能行为 | 项目自负 | — | — |

ALRM-05 的验证方式是变异而非断言：把 MAJOR 的 `blocks_wash_start` 改为 false，
应当看到矩阵测试、报警注册表测试、运行模式测试**同时**失败。若只有矩阵测试失败，
说明其他模块仍在各自表达同一决策，"唯一来源"这一主张不成立。

### 6.9 安全输出与急停 SAFE

依据 `architecture/05` 第 4 节与 `ports/outbound/safety/safety_port.h`。

| 编号 | 行为 | 类别 | 归属 | 级别 | 状态 |
|------|------|------|------|------|------|
| SAFE-01 | 急停边沿触发切断，且切断先于状态收敛 | 功能行为 | 框架保证 | L4 | 已验证 |
| SAFE-02 | 切断失败时返回错误码并计数，不静默 | 故障 | 框架保证 | L2 | 已验证 |
| SAFE-03 | 切断失败不阻断急停事件发布 | 故障 | 框架保证 | L3 | 已验证 |
| SAFE-04 | 急停热路径不重试切断（避免延长不确定窗口） | 时序 | 框架保证 | L2 | 已验证 |
| SAFE-05 | 安全端口四字段全必填，缺任一即拒绝注册 | 接口契约 | 框架保证 | L2 | 已验证 |
| SAFE-06 | 安全端口未注册时启动期可被拦住 | 故障 | 框架保证 | L2 | 已验证 |
| SAFE-07 | 未注册时急停查询返回"未激活"而非"激活" | 故障 | 框架保证 | L2 | 已验证 |
| SAFE-08 | 急停采集线程以实时优先级运行 | 时序 | 框架保证 | L3 | 已验证 |
| SAFE-09 | 切断实现须幂等、无阻塞、不申请动态内存 | 并发 | 框架要求 | L2 | 待实现 |
| SAFE-10 | 切断实现须尽力做完全部动作再返回，不在首个失败处中断 | 故障 | 框架要求 | L2 | 待实现 |
| SAFE-11 | 切断失败后升级到何种姿态 | 故障 | 项目自负 | — | — |
| SAFE-12 | 实际切断动作的物理有效性 | 物理特性 | 项目自负 | L5 | — |

SAFE-07 的取向与其他"故障安全"相反：未接入时返回"急停激活"会让设备永久停机且
无法复位，反而掩盖接入缺失。真正的兜底是启动期契约校验（SAFE-06）。

SAFE-09 与 SAFE-10 是 `框架要求`：被测对象是项目的切断实现，框架无法代验，必须
交付成一致性套件由项目运行。这两条正是最需要一致性套件的地方——一个永远成功的
仿真适配器，永远无法证明这些要求生效。

SAFE-11 归项目自负而非框架保证：升级到何种姿态取决于项目能提供哪些冗余切断手段
（第二路继电器、独立看门狗、动力回路硬连锁），框架无法单方面规定。

### 6.10 方案引擎 ENGN

依据 `architecture/02` 第 2 节。

| 编号 | 行为 | 类别 | 归属 | 级别 | 状态 |
|------|------|------|------|------|------|
| ENGN-01 | 方案在加载期完成校验，非法方案不进入执行 | 接口契约 | 框架保证 | L1 | 已验证 |
| ENGN-02 | 加载失败时解绑方案并进入停止态，不带着半个方案运行 | 故障 | 框架保证 | L1 | 已验证 |
| ENGN-03 | 阶段推进不申请动态内存（加载期一次性分配） | 结构 | 框架保证 | L0 | 已验证 |
| ENGN-04 | 表达式求值不产生副作用（不写 IO、不触发报警、不改状态） | 功能行为 | 框架保证 | L1 | 待验证 |
| ENGN-05 | 未知函数与参数个数错误在加载期被发现 | 接口契约 | 框架保证 | L1 | 待实现 |
| ENGN-06 | 方案资产 schema 版本不兼容时加载失败 | 故障 | 框架保证 | L1 | 已验证 |
| ENGN-07 | tick 推进按登记周期驱动，领域层不自建线程 | 结构 | 框架保证 | L0 | 已验证 |
| ENGN-08 | 具体方案内容、轮廓数据与执行器映射 | 功能行为 | 项目自负 | — | — |

ENGN-05 标 `待实现`：当前未知函数与参数个数错误在**求值期**才被发现（逐个 `strcmp`
比较函数名，不匹配时置失败标志）。设计意图是加载期校验——运行到某个阶段才因方案
写错而失败，现场表现为"洗到一半停了"，比加载期拒绝难排查得多。

ENGN-04 标 `待验证`：这条是"表达式只用于条件与简单计算"这一设计约束的可执行表达。
验证方式是给求值环境注入会记录调用的 IO 与报警替身，断言求值全程零调用。

### 6.11 状态投影 PROJ

依据 `architecture/02` 第 3 节。

| 编号 | 行为 | 类别 | 归属 | 级别 | 状态 |
|------|------|------|------|------|------|
| PROJ-01 | 快照一次持锁读出，各子域来自同一时刻 | 并发 | 框架保证 | L1 | 已验证 |
| PROJ-02 | 快照随领域事件更新，事件到达后可读到新值 | 功能行为 | 框架保证 | L2 | 已验证 |
| PROJ-03 | 写侧事实源仍归领域模块，投影只读 | 结构 | 框架保证 | L0 | 待验证 |
| PROJ-04 | 提供测试复位入口 | 接口契约 | 框架保证 | L1 | 已验证 |
| PROJ-05 | 快照允许短暂滞后于事实源，滞后上限明确 | 时序 | 框架保证 | L3 | 待实现 |
| PROJ-06 | 事件丢失后投影的重新同步方式 | 故障 | 框架保证 | L3 | 待实现 |

PROJ-05 与 PROJ-06 标 `待实现`：投影为云端、CLI、诊断提供只读快照，但"允许滞后
多久"与"事件丢失后如何重新同步"尚未形成规则。事件总线在队列满时会丢弃事件
（EBUS-02），所以"事件丢失"不是假想场景而是已定义的行为，其下游后果必须有答案。

### 6.12 错误模型 ERRM

依据 `common/sw_error.h` 与 `architecture/04` 第 4 节。

| 编号 | 行为 | 类别 | 归属 | 级别 | 状态 |
|------|------|------|------|------|------|
| ERRM-01 | 错误码分为瞬时可重试、调用方缺陷、接入缺失三类，三类互斥 | 接口契约 | 框架保证 | L1 | 已验证 |
| ERRM-02 | 判定用统一 helper，调用点不重新枚举错误码 | 结构 | 框架保证 | L0 | 待验证 |
| ERRM-03 | 错误码数值跨进程稳定，不因增删枚举而前移 | 接口契约 | 框架保证 | L1 | 已验证 |
| ERRM-04 | 硬件、通信、存储操作返回明确错误，不静默失败 | 故障 | 框架要求 | L2 | 待实现 |
| ERRM-05 | 所有等待操作有超时上限 | 时序 | 框架保证 | L3 | 待验证 |

ERRM-02 标 `待验证`：这条可以静态审计——在调用点出现 `== SW_ERR_BUSY ||
== SW_ERR_TIMEOUT` 这类手工枚举即为违规。但需区分合法例外：错误码本身参与状态
判定时（如用 `SW_ERR_COMM` 驱动通信丢失状态机）应保留具体码，此时 helper 表达的
"该不该重试"不能替代"具体哪种故障"。审计规则须允许显式登记这类例外。

### 6.13 资产版本 ASSET

| 编号 | 行为 | 类别 | 归属 | 级别 | 状态 |
|------|------|------|------|------|------|
| ASSET-01 | 运行期读入的资产必须声明 schema 版本，缺失即拒绝加载 | 故障 | 框架保证 | L2 | 已验证 |
| ASSET-02 | 主版本不同或资产次版本更高时判为不兼容 | 接口契约 | 框架保证 | L1 | 已验证 |
| ASSET-03 | 修订号不参与兼容判定 | 接口契约 | 框架保证 | L1 | 已验证 |
| ASSET-04 | 版本无法解析与版本不兼容返回不同错误码 | 接口契约 | 框架保证 | L2 | 已验证 |
| ASSET-05 | 版本不兼容时整份拒绝，不部分接受 | 故障 | 框架保证 | L2 | 已验证 |
| ASSET-06 | 不做未知字段自动忽略 | 接口契约 | 框架保证 | L1 | 待验证 |
| ASSET-07 | 编译期资产不做运行期版本校验 | 结构 | 框架保证 | — | — |

ASSET-07 无级别：它是一条**否定性设计决策**，不是可验证行为。编译期资产与框架编译
进同一个二进制，结构变了编译期就失败，比运行期比对版本串更强。判据是"资产能否
独立于框架二进制变化"——能，才需要版本校验。登记它是为了防止后来者误以为这是遗漏。

ASSET-06 标 `待验证`：对报警目录、水路拓扑这类安全相关资产，静默忽略未识别字段
等于让缺失的约束通过校验。需要一个"资产含未知字段时被拒绝"的用例。

### 6.14 日志与诊断 LOG

| 编号 | 行为 | 类别 | 归属 | 级别 | 状态 |
|------|------|------|------|------|------|
| LOG-01 | 级别过滤在格式化之前完成，被丢弃的日志不付出格式化开销 | 时序 | 框架保证 | L1 | 已验证 |
| LOG-02 | 单条日志整条组装后一次写出，多线程并发不交错 | 并发 | 框架保证 | L3 | 待验证 |
| LOG-03 | sink 表读写有锁保护，且 sink 内部再写日志不自锁 | 并发 | 框架保证 | L3 | 待验证 |
| LOG-04 | 级别越界不用于数组下标 | 接口契约 | 框架保证 | L1 | 已验证 |
| LOG-05 | 日志锁可被实时线程取得而不产生无界优先级反转 | 并发 | 框架保证 | L0 | 待验证 |
| LOG-06 | 超长日志被截断而非溢出缓冲区 | 接口契约 | 框架保证 | L1 | 已验证 |

LOG-02 与 LOG-03 标 `待验证`：两者都只在并发下暴露。LOG-02 的错误表现是同一行被
其他线程输出插入，需多线程并发写入后校验每行完整性；LOG-03 需注册一个会反过来写
日志的 sink，验证不死锁。

## 7. 本契约推导出的设计缺口

编写契约时，有三条行为无法分派到任何现有级别——不是因为验证难做，而是因为设计上
缺少使其可测的入口。它们是契约推导出的结论，而非实现遗漏：

**EBUS-13 事件总线同步排空。** 总线设计上有独立 dispatch 线程。缺少同步入口时，
任何涉及事件的测试只能"发布 → sleep → 断言"，于是每个此类测试都同时在验证功能与
赌调度器时序。事件链路是框架核心，这意味着框架最重要的部分只能被易崩的测试覆盖。
异步语义本身仍由 L3 专门验证，同步入口只服务 L2/L4 的确定性推进。

**时间尚未成为端口。** 周期任务、超时、防抖、确认时长全部依赖时间。时间若是直接
读时钟的模块而非可注入端口，则所有时序逻辑的测试都必须依赖真实 sleep——测试慢，
且必然间歇失败，因为是在用真实调度器的抖动去断言时序。这不是"测试快一点"的优化，
而决定了 SCHED-02、CMD-04、ERRM-05、PROJ-05 这批时序行为**是否可被确定地验证**。

**一致性套件尚不存在。** `框架要求` 类共 3 条（SAFE-09、SAFE-10、ERRM-04），
被测对象都是项目适配器。没有可交付的套件时，这几条只是文档里的话——项目无从自查，
框架也无从代验。这直接关系到"可复用"这一主张能否被验证：当前框架只测自带的仿真
适配器，第二个项目接入时，没有任何东西能告诉它"你的适配器是否满足框架的假设"。

## 8. 统计

| 归属 | 条目数 |
|------|--------|
| 框架保证 | 103 |
| 框架要求 | 3 |
| 项目自负 | 5 |
| 合计 | 111 |

| 级别 | 条目数 |
|------|--------|
| L0 静态 | 18 |
| L1 纯单元 | 32 |
| L2 契约 | 28 |
| L3 运行时 | 26 |
| L4 参考项目 | 1 |
| L5 现场 | 1 |

（5 条不判定级别：项目自负 4 条与 ASSET-07 这条否定性设计决策。）

按状态：`已验证` 80 条、`待验证` 18 条、`待实现` 7 条、不判定 6 条。

`待实现` 的 7 条尤其值得单独看——它们不是待做的功能，而是**契约推导出的设计缺口**：
EBUS-13 同步排空、ENGN-05 加载期函数校验、PROJ-05/06 投影一致性、
SAFE-09/10 一致性套件、ERRM-04 适配器错误要求。前四条是框架自身能力缺失，
后三条是"框架要求"类无处可验，详见第 7 节。

上表由 `scripts/check_behaviour_contract.sh` 统计，数字失配即视为契约与统计不同步。
手工维护的统计一定会失真——本仓 `tests/reports/` 的手写覆盖表已经发生过（声称
event_bus 8 例、实际 13 例），故本表由脚本核对。

